// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.33.4
// source: proto/party.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";
import { ErrorCode, errorCodeFromJSON, errorCodeToJSON } from "./common";

export const protobufPackage = "chirp.party";

/**
 * A party member as seen on the wire. `ready` is the ready-check flag
 * (SET_READY); always false right after a join.
 */
export interface PartyMember {
  userId: string;
  ready: boolean;
  joinedAt: number;
}

/** Full party snapshot; PARTY_STATE_CHANGED carries it whole. */
export interface PartyInfo {
  partyId: string;
  leaderId: string;
  /** clamped to [1, 8] by the service */
  maxMembers: number;
  members: PartyMember[];
  createdAt: number;
}

/** Create a party; the creator is its only member and leader. */
export interface CreatePartyRequest {
  userId: string;
  /** 0 => default 5; > 8 rejected */
  maxMembers: number;
}

export interface CreatePartyResponse {
  code: ErrorCode;
  party: PartyInfo | undefined;
  serverTime: number;
}

/**
 * Disband the whole party (leader-only); remaining members get
 * PARTY_DISBANDED_NOTIFY.
 */
export interface DisbandPartyRequest {
  userId: string;
  partyId: string;
}

export interface DisbandPartyResponse {
  code: ErrorCode;
  serverTime: number;
}

/**
 * Invite someone (any member may invite). Idempotent: re-inviting the same
 * target while an unexpired invite is open returns the original invite_id
 * and re-pings the invitee.
 */
export interface InviteToPartyRequest {
  userId: string;
  partyId: string;
  targetUserId: string;
}

export interface InviteToPartyResponse {
  code: ErrorCode;
  inviteId: string;
  expiresAt: number;
  serverTime: number;
}

/**
 * Accepting an invite joins the party: the response carries the new
 * snapshot, the invite is consumed and every other open invite to this user
 * is dropped too (joining one party invalidates the rest). Accepting on a
 * second device after already joining is an idempotent OK.
 */
export interface AcceptInviteRequest {
  userId: string;
  inviteId: string;
}

export interface AcceptInviteResponse {
  code: ErrorCode;
  party: PartyInfo | undefined;
  serverTime: number;
}

export interface DeclineInviteRequest {
  userId: string;
  inviteId: string;
}

export interface DeclineInviteResponse {
  code: ErrorCode;
  serverTime: number;
}

/**
 * Leave the party. The last member leaving disbands silently:
 * PARTY_DISBANDED isn't sent because nobody is left to tell;
 * resp.party_disbanded tells the leaver instead.
 */
export interface LeavePartyRequest {
  userId: string;
  partyId: string;
}

export interface LeavePartyResponse {
  code: ErrorCode;
  partyDisbanded: boolean;
  serverTime: number;
}

/**
 * Remove a member (leader-only); the kicked user's devices get
 * PARTY_KICKED_NOTIFY, the remaining members get PARTY_LEFT_NOTIFY with
 * reason "kicked".
 */
export interface KickMemberRequest {
  userId: string;
  partyId: string;
  targetUserId: string;
}

export interface KickMemberResponse {
  code: ErrorCode;
  serverTime: number;
}

/** Hand leadership to another member (leader-only). */
export interface TransferLeaderRequest {
  userId: string;
  partyId: string;
  targetUserId: string;
}

export interface TransferLeaderResponse {
  code: ErrorCode;
  leaderId: string;
  serverTime: number;
}

/** Flip the caller's ready-check flag. */
export interface SetReadyRequest {
  userId: string;
  partyId: string;
  ready: boolean;
}

export interface SetReadyResponse {
  code: ErrorCode;
  serverTime: number;
}

/**
 * Get the caller's current party, if any (GET_* convention, see
 * proto/social.proto:90-94: no server_time).
 */
export interface GetMyPartyRequest {
  userId: string;
}

export interface GetMyPartyResponse {
  code: ErrorCode;
  inParty: boolean;
  party: PartyInfo | undefined;
}

/**
 * New invite for the invitee (all their devices). Offline invitees see
 * nothing — there is no catch-up query yet (see the reserved 7028+ block).
 */
export interface InviteNotify {
  inviteId: string;
  fromUserId: string;
  party: PartyInfo | undefined;
  expiresAt: number;
  timestamp: number;
}

/** To the inviter: the invitee accepted or declined. */
export interface InviteResultNotify {
  inviteId: string;
  targetUserId: string;
  accepted: boolean;
  timestamp: number;
}

/** Someone joined (everyone except the joiner). */
export interface PartyJoinedNotify {
  partyId: string;
  member: PartyMember | undefined;
  timestamp: number;
}

/**
 * Someone left, for every reason ("left" | "kicked" | "offline"); everyone
 * except the leaver.
 */
export interface PartyLeftNotify {
  partyId: string;
  userId: string;
  reason: string;
  timestamp: number;
}

/** To the kicked user only (their remaining membership view is gone). */
export interface PartyKickedNotify {
  partyId: string;
  actorUserId: string;
  timestamp: number;
}

/** Full snapshot to ALL members including the actor. */
export interface PartyStateChangedNotify {
  party: PartyInfo | undefined;
  timestamp: number;
}

/**
 * The party is gone because the leader disbanded it; remaining members get
 * this. (The last member leaving disbands silently — nobody is left to
 * tell.) actor_user_id is the ex-leader; the service never auto-disbands
 * loudly, so it is never empty in practice.
 */
export interface PartyDisbandedNotify {
  partyId: string;
  actorUserId: string;
  timestamp: number;
}

/**
 * ---------------------------------------------------------------------------
 * Server-side storage snapshots. These are Redis VALUES (one key per party,
 * e.g. "chirp:party:party:<party_id>"), never wire messages. Kept separate
 * from PartyInfo so the storage schema can evolve without touching the
 * client-facing protocol. Invites are deliberately NOT stored — they are
 * short-lived and lost on restart by design.
 */
export interface StoredParty {
  partyId: string;
  leaderId: string;
  maxMembers: number;
  createdAt: number;
  members: StoredMember[];
}

export interface StoredMember {
  userId: string;
  ready: boolean;
  joinedAt: number;
}

function createBasePartyMember(): PartyMember {
  return { userId: "", ready: false, joinedAt: 0 };
}

export const PartyMember = {
  encode(message: PartyMember, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.ready !== false) {
      writer.uint32(16).bool(message.ready);
    }
    if (message.joinedAt !== 0) {
      writer.uint32(24).int64(message.joinedAt);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PartyMember {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePartyMember();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.ready = reader.bool();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.joinedAt = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PartyMember {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      ready: isSet(object.ready) ? globalThis.Boolean(object.ready) : false,
      joinedAt: isSet(object.joinedAt) ? globalThis.Number(object.joinedAt) : 0,
    };
  },

  toJSON(message: PartyMember): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.ready !== false) {
      obj.ready = message.ready;
    }
    if (message.joinedAt !== 0) {
      obj.joinedAt = Math.round(message.joinedAt);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PartyMember>, I>>(base?: I): PartyMember {
    return PartyMember.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PartyMember>, I>>(object: I): PartyMember {
    const message = createBasePartyMember();
    message.userId = object.userId ?? "";
    message.ready = object.ready ?? false;
    message.joinedAt = object.joinedAt ?? 0;
    return message;
  },
};

function createBasePartyInfo(): PartyInfo {
  return { partyId: "", leaderId: "", maxMembers: 0, members: [], createdAt: 0 };
}

export const PartyInfo = {
  encode(message: PartyInfo, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.partyId !== "") {
      writer.uint32(10).string(message.partyId);
    }
    if (message.leaderId !== "") {
      writer.uint32(18).string(message.leaderId);
    }
    if (message.maxMembers !== 0) {
      writer.uint32(24).int32(message.maxMembers);
    }
    for (const v of message.members) {
      PartyMember.encode(v!, writer.uint32(34).fork()).ldelim();
    }
    if (message.createdAt !== 0) {
      writer.uint32(40).int64(message.createdAt);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PartyInfo {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePartyInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.partyId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.leaderId = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.maxMembers = reader.int32();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.members.push(PartyMember.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.createdAt = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PartyInfo {
    return {
      partyId: isSet(object.partyId) ? globalThis.String(object.partyId) : "",
      leaderId: isSet(object.leaderId) ? globalThis.String(object.leaderId) : "",
      maxMembers: isSet(object.maxMembers) ? globalThis.Number(object.maxMembers) : 0,
      members: globalThis.Array.isArray(object?.members) ? object.members.map((e: any) => PartyMember.fromJSON(e)) : [],
      createdAt: isSet(object.createdAt) ? globalThis.Number(object.createdAt) : 0,
    };
  },

  toJSON(message: PartyInfo): unknown {
    const obj: any = {};
    if (message.partyId !== "") {
      obj.partyId = message.partyId;
    }
    if (message.leaderId !== "") {
      obj.leaderId = message.leaderId;
    }
    if (message.maxMembers !== 0) {
      obj.maxMembers = Math.round(message.maxMembers);
    }
    if (message.members?.length) {
      obj.members = message.members.map((e) => PartyMember.toJSON(e));
    }
    if (message.createdAt !== 0) {
      obj.createdAt = Math.round(message.createdAt);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PartyInfo>, I>>(base?: I): PartyInfo {
    return PartyInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PartyInfo>, I>>(object: I): PartyInfo {
    const message = createBasePartyInfo();
    message.partyId = object.partyId ?? "";
    message.leaderId = object.leaderId ?? "";
    message.maxMembers = object.maxMembers ?? 0;
    message.members = object.members?.map((e) => PartyMember.fromPartial(e)) || [];
    message.createdAt = object.createdAt ?? 0;
    return message;
  },
};

function createBaseCreatePartyRequest(): CreatePartyRequest {
  return { userId: "", maxMembers: 0 };
}

export const CreatePartyRequest = {
  encode(message: CreatePartyRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.maxMembers !== 0) {
      writer.uint32(16).int32(message.maxMembers);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CreatePartyRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreatePartyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.maxMembers = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreatePartyRequest {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      maxMembers: isSet(object.maxMembers) ? globalThis.Number(object.maxMembers) : 0,
    };
  },

  toJSON(message: CreatePartyRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.maxMembers !== 0) {
      obj.maxMembers = Math.round(message.maxMembers);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreatePartyRequest>, I>>(base?: I): CreatePartyRequest {
    return CreatePartyRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreatePartyRequest>, I>>(object: I): CreatePartyRequest {
    const message = createBaseCreatePartyRequest();
    message.userId = object.userId ?? "";
    message.maxMembers = object.maxMembers ?? 0;
    return message;
  },
};

function createBaseCreatePartyResponse(): CreatePartyResponse {
  return { code: 0, party: undefined, serverTime: 0 };
}

export const CreatePartyResponse = {
  encode(message: CreatePartyResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.party !== undefined) {
      PartyInfo.encode(message.party, writer.uint32(18).fork()).ldelim();
    }
    if (message.serverTime !== 0) {
      writer.uint32(24).int64(message.serverTime);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CreatePartyResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreatePartyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.party = PartyInfo.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.serverTime = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreatePartyResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      party: isSet(object.party) ? PartyInfo.fromJSON(object.party) : undefined,
      serverTime: isSet(object.serverTime) ? globalThis.Number(object.serverTime) : 0,
    };
  },

  toJSON(message: CreatePartyResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.party !== undefined) {
      obj.party = PartyInfo.toJSON(message.party);
    }
    if (message.serverTime !== 0) {
      obj.serverTime = Math.round(message.serverTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreatePartyResponse>, I>>(base?: I): CreatePartyResponse {
    return CreatePartyResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreatePartyResponse>, I>>(object: I): CreatePartyResponse {
    const message = createBaseCreatePartyResponse();
    message.code = object.code ?? 0;
    message.party = (object.party !== undefined && object.party !== null)
      ? PartyInfo.fromPartial(object.party)
      : undefined;
    message.serverTime = object.serverTime ?? 0;
    return message;
  },
};

function createBaseDisbandPartyRequest(): DisbandPartyRequest {
  return { userId: "", partyId: "" };
}

export const DisbandPartyRequest = {
  encode(message: DisbandPartyRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.partyId !== "") {
      writer.uint32(18).string(message.partyId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DisbandPartyRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDisbandPartyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.partyId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DisbandPartyRequest {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      partyId: isSet(object.partyId) ? globalThis.String(object.partyId) : "",
    };
  },

  toJSON(message: DisbandPartyRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.partyId !== "") {
      obj.partyId = message.partyId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DisbandPartyRequest>, I>>(base?: I): DisbandPartyRequest {
    return DisbandPartyRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DisbandPartyRequest>, I>>(object: I): DisbandPartyRequest {
    const message = createBaseDisbandPartyRequest();
    message.userId = object.userId ?? "";
    message.partyId = object.partyId ?? "";
    return message;
  },
};

function createBaseDisbandPartyResponse(): DisbandPartyResponse {
  return { code: 0, serverTime: 0 };
}

export const DisbandPartyResponse = {
  encode(message: DisbandPartyResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.serverTime !== 0) {
      writer.uint32(16).int64(message.serverTime);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DisbandPartyResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDisbandPartyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.serverTime = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DisbandPartyResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      serverTime: isSet(object.serverTime) ? globalThis.Number(object.serverTime) : 0,
    };
  },

  toJSON(message: DisbandPartyResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.serverTime !== 0) {
      obj.serverTime = Math.round(message.serverTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DisbandPartyResponse>, I>>(base?: I): DisbandPartyResponse {
    return DisbandPartyResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DisbandPartyResponse>, I>>(object: I): DisbandPartyResponse {
    const message = createBaseDisbandPartyResponse();
    message.code = object.code ?? 0;
    message.serverTime = object.serverTime ?? 0;
    return message;
  },
};

function createBaseInviteToPartyRequest(): InviteToPartyRequest {
  return { userId: "", partyId: "", targetUserId: "" };
}

export const InviteToPartyRequest = {
  encode(message: InviteToPartyRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.partyId !== "") {
      writer.uint32(18).string(message.partyId);
    }
    if (message.targetUserId !== "") {
      writer.uint32(26).string(message.targetUserId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): InviteToPartyRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInviteToPartyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.partyId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.targetUserId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InviteToPartyRequest {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      partyId: isSet(object.partyId) ? globalThis.String(object.partyId) : "",
      targetUserId: isSet(object.targetUserId) ? globalThis.String(object.targetUserId) : "",
    };
  },

  toJSON(message: InviteToPartyRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.partyId !== "") {
      obj.partyId = message.partyId;
    }
    if (message.targetUserId !== "") {
      obj.targetUserId = message.targetUserId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InviteToPartyRequest>, I>>(base?: I): InviteToPartyRequest {
    return InviteToPartyRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InviteToPartyRequest>, I>>(object: I): InviteToPartyRequest {
    const message = createBaseInviteToPartyRequest();
    message.userId = object.userId ?? "";
    message.partyId = object.partyId ?? "";
    message.targetUserId = object.targetUserId ?? "";
    return message;
  },
};

function createBaseInviteToPartyResponse(): InviteToPartyResponse {
  return { code: 0, inviteId: "", expiresAt: 0, serverTime: 0 };
}

export const InviteToPartyResponse = {
  encode(message: InviteToPartyResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.inviteId !== "") {
      writer.uint32(18).string(message.inviteId);
    }
    if (message.expiresAt !== 0) {
      writer.uint32(24).int64(message.expiresAt);
    }
    if (message.serverTime !== 0) {
      writer.uint32(32).int64(message.serverTime);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): InviteToPartyResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInviteToPartyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.inviteId = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.expiresAt = longToNumber(reader.int64() as Long);
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.serverTime = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InviteToPartyResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      inviteId: isSet(object.inviteId) ? globalThis.String(object.inviteId) : "",
      expiresAt: isSet(object.expiresAt) ? globalThis.Number(object.expiresAt) : 0,
      serverTime: isSet(object.serverTime) ? globalThis.Number(object.serverTime) : 0,
    };
  },

  toJSON(message: InviteToPartyResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.inviteId !== "") {
      obj.inviteId = message.inviteId;
    }
    if (message.expiresAt !== 0) {
      obj.expiresAt = Math.round(message.expiresAt);
    }
    if (message.serverTime !== 0) {
      obj.serverTime = Math.round(message.serverTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InviteToPartyResponse>, I>>(base?: I): InviteToPartyResponse {
    return InviteToPartyResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InviteToPartyResponse>, I>>(object: I): InviteToPartyResponse {
    const message = createBaseInviteToPartyResponse();
    message.code = object.code ?? 0;
    message.inviteId = object.inviteId ?? "";
    message.expiresAt = object.expiresAt ?? 0;
    message.serverTime = object.serverTime ?? 0;
    return message;
  },
};

function createBaseAcceptInviteRequest(): AcceptInviteRequest {
  return { userId: "", inviteId: "" };
}

export const AcceptInviteRequest = {
  encode(message: AcceptInviteRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.inviteId !== "") {
      writer.uint32(18).string(message.inviteId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AcceptInviteRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAcceptInviteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.inviteId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AcceptInviteRequest {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      inviteId: isSet(object.inviteId) ? globalThis.String(object.inviteId) : "",
    };
  },

  toJSON(message: AcceptInviteRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.inviteId !== "") {
      obj.inviteId = message.inviteId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AcceptInviteRequest>, I>>(base?: I): AcceptInviteRequest {
    return AcceptInviteRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AcceptInviteRequest>, I>>(object: I): AcceptInviteRequest {
    const message = createBaseAcceptInviteRequest();
    message.userId = object.userId ?? "";
    message.inviteId = object.inviteId ?? "";
    return message;
  },
};

function createBaseAcceptInviteResponse(): AcceptInviteResponse {
  return { code: 0, party: undefined, serverTime: 0 };
}

export const AcceptInviteResponse = {
  encode(message: AcceptInviteResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.party !== undefined) {
      PartyInfo.encode(message.party, writer.uint32(18).fork()).ldelim();
    }
    if (message.serverTime !== 0) {
      writer.uint32(24).int64(message.serverTime);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AcceptInviteResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAcceptInviteResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.party = PartyInfo.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.serverTime = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AcceptInviteResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      party: isSet(object.party) ? PartyInfo.fromJSON(object.party) : undefined,
      serverTime: isSet(object.serverTime) ? globalThis.Number(object.serverTime) : 0,
    };
  },

  toJSON(message: AcceptInviteResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.party !== undefined) {
      obj.party = PartyInfo.toJSON(message.party);
    }
    if (message.serverTime !== 0) {
      obj.serverTime = Math.round(message.serverTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AcceptInviteResponse>, I>>(base?: I): AcceptInviteResponse {
    return AcceptInviteResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AcceptInviteResponse>, I>>(object: I): AcceptInviteResponse {
    const message = createBaseAcceptInviteResponse();
    message.code = object.code ?? 0;
    message.party = (object.party !== undefined && object.party !== null)
      ? PartyInfo.fromPartial(object.party)
      : undefined;
    message.serverTime = object.serverTime ?? 0;
    return message;
  },
};

function createBaseDeclineInviteRequest(): DeclineInviteRequest {
  return { userId: "", inviteId: "" };
}

export const DeclineInviteRequest = {
  encode(message: DeclineInviteRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.inviteId !== "") {
      writer.uint32(18).string(message.inviteId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DeclineInviteRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeclineInviteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.inviteId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeclineInviteRequest {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      inviteId: isSet(object.inviteId) ? globalThis.String(object.inviteId) : "",
    };
  },

  toJSON(message: DeclineInviteRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.inviteId !== "") {
      obj.inviteId = message.inviteId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeclineInviteRequest>, I>>(base?: I): DeclineInviteRequest {
    return DeclineInviteRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeclineInviteRequest>, I>>(object: I): DeclineInviteRequest {
    const message = createBaseDeclineInviteRequest();
    message.userId = object.userId ?? "";
    message.inviteId = object.inviteId ?? "";
    return message;
  },
};

function createBaseDeclineInviteResponse(): DeclineInviteResponse {
  return { code: 0, serverTime: 0 };
}

export const DeclineInviteResponse = {
  encode(message: DeclineInviteResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.serverTime !== 0) {
      writer.uint32(16).int64(message.serverTime);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DeclineInviteResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeclineInviteResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.serverTime = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeclineInviteResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      serverTime: isSet(object.serverTime) ? globalThis.Number(object.serverTime) : 0,
    };
  },

  toJSON(message: DeclineInviteResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.serverTime !== 0) {
      obj.serverTime = Math.round(message.serverTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeclineInviteResponse>, I>>(base?: I): DeclineInviteResponse {
    return DeclineInviteResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeclineInviteResponse>, I>>(object: I): DeclineInviteResponse {
    const message = createBaseDeclineInviteResponse();
    message.code = object.code ?? 0;
    message.serverTime = object.serverTime ?? 0;
    return message;
  },
};

function createBaseLeavePartyRequest(): LeavePartyRequest {
  return { userId: "", partyId: "" };
}

export const LeavePartyRequest = {
  encode(message: LeavePartyRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.partyId !== "") {
      writer.uint32(18).string(message.partyId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): LeavePartyRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLeavePartyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.partyId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LeavePartyRequest {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      partyId: isSet(object.partyId) ? globalThis.String(object.partyId) : "",
    };
  },

  toJSON(message: LeavePartyRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.partyId !== "") {
      obj.partyId = message.partyId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LeavePartyRequest>, I>>(base?: I): LeavePartyRequest {
    return LeavePartyRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LeavePartyRequest>, I>>(object: I): LeavePartyRequest {
    const message = createBaseLeavePartyRequest();
    message.userId = object.userId ?? "";
    message.partyId = object.partyId ?? "";
    return message;
  },
};

function createBaseLeavePartyResponse(): LeavePartyResponse {
  return { code: 0, partyDisbanded: false, serverTime: 0 };
}

export const LeavePartyResponse = {
  encode(message: LeavePartyResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.partyDisbanded !== false) {
      writer.uint32(16).bool(message.partyDisbanded);
    }
    if (message.serverTime !== 0) {
      writer.uint32(24).int64(message.serverTime);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): LeavePartyResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLeavePartyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.partyDisbanded = reader.bool();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.serverTime = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LeavePartyResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      partyDisbanded: isSet(object.partyDisbanded) ? globalThis.Boolean(object.partyDisbanded) : false,
      serverTime: isSet(object.serverTime) ? globalThis.Number(object.serverTime) : 0,
    };
  },

  toJSON(message: LeavePartyResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.partyDisbanded !== false) {
      obj.partyDisbanded = message.partyDisbanded;
    }
    if (message.serverTime !== 0) {
      obj.serverTime = Math.round(message.serverTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LeavePartyResponse>, I>>(base?: I): LeavePartyResponse {
    return LeavePartyResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LeavePartyResponse>, I>>(object: I): LeavePartyResponse {
    const message = createBaseLeavePartyResponse();
    message.code = object.code ?? 0;
    message.partyDisbanded = object.partyDisbanded ?? false;
    message.serverTime = object.serverTime ?? 0;
    return message;
  },
};

function createBaseKickMemberRequest(): KickMemberRequest {
  return { userId: "", partyId: "", targetUserId: "" };
}

export const KickMemberRequest = {
  encode(message: KickMemberRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.partyId !== "") {
      writer.uint32(18).string(message.partyId);
    }
    if (message.targetUserId !== "") {
      writer.uint32(26).string(message.targetUserId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): KickMemberRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKickMemberRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.partyId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.targetUserId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KickMemberRequest {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      partyId: isSet(object.partyId) ? globalThis.String(object.partyId) : "",
      targetUserId: isSet(object.targetUserId) ? globalThis.String(object.targetUserId) : "",
    };
  },

  toJSON(message: KickMemberRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.partyId !== "") {
      obj.partyId = message.partyId;
    }
    if (message.targetUserId !== "") {
      obj.targetUserId = message.targetUserId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KickMemberRequest>, I>>(base?: I): KickMemberRequest {
    return KickMemberRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KickMemberRequest>, I>>(object: I): KickMemberRequest {
    const message = createBaseKickMemberRequest();
    message.userId = object.userId ?? "";
    message.partyId = object.partyId ?? "";
    message.targetUserId = object.targetUserId ?? "";
    return message;
  },
};

function createBaseKickMemberResponse(): KickMemberResponse {
  return { code: 0, serverTime: 0 };
}

export const KickMemberResponse = {
  encode(message: KickMemberResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.serverTime !== 0) {
      writer.uint32(16).int64(message.serverTime);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): KickMemberResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKickMemberResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.serverTime = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KickMemberResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      serverTime: isSet(object.serverTime) ? globalThis.Number(object.serverTime) : 0,
    };
  },

  toJSON(message: KickMemberResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.serverTime !== 0) {
      obj.serverTime = Math.round(message.serverTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KickMemberResponse>, I>>(base?: I): KickMemberResponse {
    return KickMemberResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KickMemberResponse>, I>>(object: I): KickMemberResponse {
    const message = createBaseKickMemberResponse();
    message.code = object.code ?? 0;
    message.serverTime = object.serverTime ?? 0;
    return message;
  },
};

function createBaseTransferLeaderRequest(): TransferLeaderRequest {
  return { userId: "", partyId: "", targetUserId: "" };
}

export const TransferLeaderRequest = {
  encode(message: TransferLeaderRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.partyId !== "") {
      writer.uint32(18).string(message.partyId);
    }
    if (message.targetUserId !== "") {
      writer.uint32(26).string(message.targetUserId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TransferLeaderRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTransferLeaderRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.partyId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.targetUserId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TransferLeaderRequest {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      partyId: isSet(object.partyId) ? globalThis.String(object.partyId) : "",
      targetUserId: isSet(object.targetUserId) ? globalThis.String(object.targetUserId) : "",
    };
  },

  toJSON(message: TransferLeaderRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.partyId !== "") {
      obj.partyId = message.partyId;
    }
    if (message.targetUserId !== "") {
      obj.targetUserId = message.targetUserId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TransferLeaderRequest>, I>>(base?: I): TransferLeaderRequest {
    return TransferLeaderRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TransferLeaderRequest>, I>>(object: I): TransferLeaderRequest {
    const message = createBaseTransferLeaderRequest();
    message.userId = object.userId ?? "";
    message.partyId = object.partyId ?? "";
    message.targetUserId = object.targetUserId ?? "";
    return message;
  },
};

function createBaseTransferLeaderResponse(): TransferLeaderResponse {
  return { code: 0, leaderId: "", serverTime: 0 };
}

export const TransferLeaderResponse = {
  encode(message: TransferLeaderResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.leaderId !== "") {
      writer.uint32(18).string(message.leaderId);
    }
    if (message.serverTime !== 0) {
      writer.uint32(24).int64(message.serverTime);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TransferLeaderResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTransferLeaderResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.leaderId = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.serverTime = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TransferLeaderResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      leaderId: isSet(object.leaderId) ? globalThis.String(object.leaderId) : "",
      serverTime: isSet(object.serverTime) ? globalThis.Number(object.serverTime) : 0,
    };
  },

  toJSON(message: TransferLeaderResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.leaderId !== "") {
      obj.leaderId = message.leaderId;
    }
    if (message.serverTime !== 0) {
      obj.serverTime = Math.round(message.serverTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TransferLeaderResponse>, I>>(base?: I): TransferLeaderResponse {
    return TransferLeaderResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TransferLeaderResponse>, I>>(object: I): TransferLeaderResponse {
    const message = createBaseTransferLeaderResponse();
    message.code = object.code ?? 0;
    message.leaderId = object.leaderId ?? "";
    message.serverTime = object.serverTime ?? 0;
    return message;
  },
};

function createBaseSetReadyRequest(): SetReadyRequest {
  return { userId: "", partyId: "", ready: false };
}

export const SetReadyRequest = {
  encode(message: SetReadyRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.partyId !== "") {
      writer.uint32(18).string(message.partyId);
    }
    if (message.ready !== false) {
      writer.uint32(24).bool(message.ready);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SetReadyRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetReadyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.partyId = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.ready = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetReadyRequest {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      partyId: isSet(object.partyId) ? globalThis.String(object.partyId) : "",
      ready: isSet(object.ready) ? globalThis.Boolean(object.ready) : false,
    };
  },

  toJSON(message: SetReadyRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.partyId !== "") {
      obj.partyId = message.partyId;
    }
    if (message.ready !== false) {
      obj.ready = message.ready;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetReadyRequest>, I>>(base?: I): SetReadyRequest {
    return SetReadyRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetReadyRequest>, I>>(object: I): SetReadyRequest {
    const message = createBaseSetReadyRequest();
    message.userId = object.userId ?? "";
    message.partyId = object.partyId ?? "";
    message.ready = object.ready ?? false;
    return message;
  },
};

function createBaseSetReadyResponse(): SetReadyResponse {
  return { code: 0, serverTime: 0 };
}

export const SetReadyResponse = {
  encode(message: SetReadyResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.serverTime !== 0) {
      writer.uint32(16).int64(message.serverTime);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SetReadyResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetReadyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.serverTime = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetReadyResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      serverTime: isSet(object.serverTime) ? globalThis.Number(object.serverTime) : 0,
    };
  },

  toJSON(message: SetReadyResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.serverTime !== 0) {
      obj.serverTime = Math.round(message.serverTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetReadyResponse>, I>>(base?: I): SetReadyResponse {
    return SetReadyResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetReadyResponse>, I>>(object: I): SetReadyResponse {
    const message = createBaseSetReadyResponse();
    message.code = object.code ?? 0;
    message.serverTime = object.serverTime ?? 0;
    return message;
  },
};

function createBaseGetMyPartyRequest(): GetMyPartyRequest {
  return { userId: "" };
}

export const GetMyPartyRequest = {
  encode(message: GetMyPartyRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetMyPartyRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetMyPartyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetMyPartyRequest {
    return { userId: isSet(object.userId) ? globalThis.String(object.userId) : "" };
  },

  toJSON(message: GetMyPartyRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetMyPartyRequest>, I>>(base?: I): GetMyPartyRequest {
    return GetMyPartyRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetMyPartyRequest>, I>>(object: I): GetMyPartyRequest {
    const message = createBaseGetMyPartyRequest();
    message.userId = object.userId ?? "";
    return message;
  },
};

function createBaseGetMyPartyResponse(): GetMyPartyResponse {
  return { code: 0, inParty: false, party: undefined };
}

export const GetMyPartyResponse = {
  encode(message: GetMyPartyResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.inParty !== false) {
      writer.uint32(16).bool(message.inParty);
    }
    if (message.party !== undefined) {
      PartyInfo.encode(message.party, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetMyPartyResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetMyPartyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.inParty = reader.bool();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.party = PartyInfo.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetMyPartyResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      inParty: isSet(object.inParty) ? globalThis.Boolean(object.inParty) : false,
      party: isSet(object.party) ? PartyInfo.fromJSON(object.party) : undefined,
    };
  },

  toJSON(message: GetMyPartyResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.inParty !== false) {
      obj.inParty = message.inParty;
    }
    if (message.party !== undefined) {
      obj.party = PartyInfo.toJSON(message.party);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetMyPartyResponse>, I>>(base?: I): GetMyPartyResponse {
    return GetMyPartyResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetMyPartyResponse>, I>>(object: I): GetMyPartyResponse {
    const message = createBaseGetMyPartyResponse();
    message.code = object.code ?? 0;
    message.inParty = object.inParty ?? false;
    message.party = (object.party !== undefined && object.party !== null)
      ? PartyInfo.fromPartial(object.party)
      : undefined;
    return message;
  },
};

function createBaseInviteNotify(): InviteNotify {
  return { inviteId: "", fromUserId: "", party: undefined, expiresAt: 0, timestamp: 0 };
}

export const InviteNotify = {
  encode(message: InviteNotify, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.inviteId !== "") {
      writer.uint32(10).string(message.inviteId);
    }
    if (message.fromUserId !== "") {
      writer.uint32(18).string(message.fromUserId);
    }
    if (message.party !== undefined) {
      PartyInfo.encode(message.party, writer.uint32(26).fork()).ldelim();
    }
    if (message.expiresAt !== 0) {
      writer.uint32(32).int64(message.expiresAt);
    }
    if (message.timestamp !== 0) {
      writer.uint32(40).int64(message.timestamp);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): InviteNotify {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInviteNotify();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.inviteId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.fromUserId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.party = PartyInfo.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.expiresAt = longToNumber(reader.int64() as Long);
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.timestamp = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InviteNotify {
    return {
      inviteId: isSet(object.inviteId) ? globalThis.String(object.inviteId) : "",
      fromUserId: isSet(object.fromUserId) ? globalThis.String(object.fromUserId) : "",
      party: isSet(object.party) ? PartyInfo.fromJSON(object.party) : undefined,
      expiresAt: isSet(object.expiresAt) ? globalThis.Number(object.expiresAt) : 0,
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
    };
  },

  toJSON(message: InviteNotify): unknown {
    const obj: any = {};
    if (message.inviteId !== "") {
      obj.inviteId = message.inviteId;
    }
    if (message.fromUserId !== "") {
      obj.fromUserId = message.fromUserId;
    }
    if (message.party !== undefined) {
      obj.party = PartyInfo.toJSON(message.party);
    }
    if (message.expiresAt !== 0) {
      obj.expiresAt = Math.round(message.expiresAt);
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InviteNotify>, I>>(base?: I): InviteNotify {
    return InviteNotify.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InviteNotify>, I>>(object: I): InviteNotify {
    const message = createBaseInviteNotify();
    message.inviteId = object.inviteId ?? "";
    message.fromUserId = object.fromUserId ?? "";
    message.party = (object.party !== undefined && object.party !== null)
      ? PartyInfo.fromPartial(object.party)
      : undefined;
    message.expiresAt = object.expiresAt ?? 0;
    message.timestamp = object.timestamp ?? 0;
    return message;
  },
};

function createBaseInviteResultNotify(): InviteResultNotify {
  return { inviteId: "", targetUserId: "", accepted: false, timestamp: 0 };
}

export const InviteResultNotify = {
  encode(message: InviteResultNotify, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.inviteId !== "") {
      writer.uint32(10).string(message.inviteId);
    }
    if (message.targetUserId !== "") {
      writer.uint32(18).string(message.targetUserId);
    }
    if (message.accepted !== false) {
      writer.uint32(24).bool(message.accepted);
    }
    if (message.timestamp !== 0) {
      writer.uint32(32).int64(message.timestamp);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): InviteResultNotify {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInviteResultNotify();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.inviteId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.targetUserId = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.accepted = reader.bool();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.timestamp = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InviteResultNotify {
    return {
      inviteId: isSet(object.inviteId) ? globalThis.String(object.inviteId) : "",
      targetUserId: isSet(object.targetUserId) ? globalThis.String(object.targetUserId) : "",
      accepted: isSet(object.accepted) ? globalThis.Boolean(object.accepted) : false,
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
    };
  },

  toJSON(message: InviteResultNotify): unknown {
    const obj: any = {};
    if (message.inviteId !== "") {
      obj.inviteId = message.inviteId;
    }
    if (message.targetUserId !== "") {
      obj.targetUserId = message.targetUserId;
    }
    if (message.accepted !== false) {
      obj.accepted = message.accepted;
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InviteResultNotify>, I>>(base?: I): InviteResultNotify {
    return InviteResultNotify.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InviteResultNotify>, I>>(object: I): InviteResultNotify {
    const message = createBaseInviteResultNotify();
    message.inviteId = object.inviteId ?? "";
    message.targetUserId = object.targetUserId ?? "";
    message.accepted = object.accepted ?? false;
    message.timestamp = object.timestamp ?? 0;
    return message;
  },
};

function createBasePartyJoinedNotify(): PartyJoinedNotify {
  return { partyId: "", member: undefined, timestamp: 0 };
}

export const PartyJoinedNotify = {
  encode(message: PartyJoinedNotify, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.partyId !== "") {
      writer.uint32(10).string(message.partyId);
    }
    if (message.member !== undefined) {
      PartyMember.encode(message.member, writer.uint32(18).fork()).ldelim();
    }
    if (message.timestamp !== 0) {
      writer.uint32(24).int64(message.timestamp);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PartyJoinedNotify {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePartyJoinedNotify();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.partyId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.member = PartyMember.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.timestamp = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PartyJoinedNotify {
    return {
      partyId: isSet(object.partyId) ? globalThis.String(object.partyId) : "",
      member: isSet(object.member) ? PartyMember.fromJSON(object.member) : undefined,
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
    };
  },

  toJSON(message: PartyJoinedNotify): unknown {
    const obj: any = {};
    if (message.partyId !== "") {
      obj.partyId = message.partyId;
    }
    if (message.member !== undefined) {
      obj.member = PartyMember.toJSON(message.member);
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PartyJoinedNotify>, I>>(base?: I): PartyJoinedNotify {
    return PartyJoinedNotify.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PartyJoinedNotify>, I>>(object: I): PartyJoinedNotify {
    const message = createBasePartyJoinedNotify();
    message.partyId = object.partyId ?? "";
    message.member = (object.member !== undefined && object.member !== null)
      ? PartyMember.fromPartial(object.member)
      : undefined;
    message.timestamp = object.timestamp ?? 0;
    return message;
  },
};

function createBasePartyLeftNotify(): PartyLeftNotify {
  return { partyId: "", userId: "", reason: "", timestamp: 0 };
}

export const PartyLeftNotify = {
  encode(message: PartyLeftNotify, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.partyId !== "") {
      writer.uint32(10).string(message.partyId);
    }
    if (message.userId !== "") {
      writer.uint32(18).string(message.userId);
    }
    if (message.reason !== "") {
      writer.uint32(26).string(message.reason);
    }
    if (message.timestamp !== 0) {
      writer.uint32(32).int64(message.timestamp);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PartyLeftNotify {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePartyLeftNotify();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.partyId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.reason = reader.string();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.timestamp = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PartyLeftNotify {
    return {
      partyId: isSet(object.partyId) ? globalThis.String(object.partyId) : "",
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
    };
  },

  toJSON(message: PartyLeftNotify): unknown {
    const obj: any = {};
    if (message.partyId !== "") {
      obj.partyId = message.partyId;
    }
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PartyLeftNotify>, I>>(base?: I): PartyLeftNotify {
    return PartyLeftNotify.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PartyLeftNotify>, I>>(object: I): PartyLeftNotify {
    const message = createBasePartyLeftNotify();
    message.partyId = object.partyId ?? "";
    message.userId = object.userId ?? "";
    message.reason = object.reason ?? "";
    message.timestamp = object.timestamp ?? 0;
    return message;
  },
};

function createBasePartyKickedNotify(): PartyKickedNotify {
  return { partyId: "", actorUserId: "", timestamp: 0 };
}

export const PartyKickedNotify = {
  encode(message: PartyKickedNotify, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.partyId !== "") {
      writer.uint32(10).string(message.partyId);
    }
    if (message.actorUserId !== "") {
      writer.uint32(18).string(message.actorUserId);
    }
    if (message.timestamp !== 0) {
      writer.uint32(24).int64(message.timestamp);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PartyKickedNotify {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePartyKickedNotify();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.partyId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.actorUserId = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.timestamp = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PartyKickedNotify {
    return {
      partyId: isSet(object.partyId) ? globalThis.String(object.partyId) : "",
      actorUserId: isSet(object.actorUserId) ? globalThis.String(object.actorUserId) : "",
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
    };
  },

  toJSON(message: PartyKickedNotify): unknown {
    const obj: any = {};
    if (message.partyId !== "") {
      obj.partyId = message.partyId;
    }
    if (message.actorUserId !== "") {
      obj.actorUserId = message.actorUserId;
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PartyKickedNotify>, I>>(base?: I): PartyKickedNotify {
    return PartyKickedNotify.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PartyKickedNotify>, I>>(object: I): PartyKickedNotify {
    const message = createBasePartyKickedNotify();
    message.partyId = object.partyId ?? "";
    message.actorUserId = object.actorUserId ?? "";
    message.timestamp = object.timestamp ?? 0;
    return message;
  },
};

function createBasePartyStateChangedNotify(): PartyStateChangedNotify {
  return { party: undefined, timestamp: 0 };
}

export const PartyStateChangedNotify = {
  encode(message: PartyStateChangedNotify, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.party !== undefined) {
      PartyInfo.encode(message.party, writer.uint32(10).fork()).ldelim();
    }
    if (message.timestamp !== 0) {
      writer.uint32(16).int64(message.timestamp);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PartyStateChangedNotify {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePartyStateChangedNotify();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.party = PartyInfo.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.timestamp = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PartyStateChangedNotify {
    return {
      party: isSet(object.party) ? PartyInfo.fromJSON(object.party) : undefined,
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
    };
  },

  toJSON(message: PartyStateChangedNotify): unknown {
    const obj: any = {};
    if (message.party !== undefined) {
      obj.party = PartyInfo.toJSON(message.party);
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PartyStateChangedNotify>, I>>(base?: I): PartyStateChangedNotify {
    return PartyStateChangedNotify.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PartyStateChangedNotify>, I>>(object: I): PartyStateChangedNotify {
    const message = createBasePartyStateChangedNotify();
    message.party = (object.party !== undefined && object.party !== null)
      ? PartyInfo.fromPartial(object.party)
      : undefined;
    message.timestamp = object.timestamp ?? 0;
    return message;
  },
};

function createBasePartyDisbandedNotify(): PartyDisbandedNotify {
  return { partyId: "", actorUserId: "", timestamp: 0 };
}

export const PartyDisbandedNotify = {
  encode(message: PartyDisbandedNotify, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.partyId !== "") {
      writer.uint32(10).string(message.partyId);
    }
    if (message.actorUserId !== "") {
      writer.uint32(18).string(message.actorUserId);
    }
    if (message.timestamp !== 0) {
      writer.uint32(24).int64(message.timestamp);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PartyDisbandedNotify {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePartyDisbandedNotify();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.partyId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.actorUserId = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.timestamp = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PartyDisbandedNotify {
    return {
      partyId: isSet(object.partyId) ? globalThis.String(object.partyId) : "",
      actorUserId: isSet(object.actorUserId) ? globalThis.String(object.actorUserId) : "",
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
    };
  },

  toJSON(message: PartyDisbandedNotify): unknown {
    const obj: any = {};
    if (message.partyId !== "") {
      obj.partyId = message.partyId;
    }
    if (message.actorUserId !== "") {
      obj.actorUserId = message.actorUserId;
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PartyDisbandedNotify>, I>>(base?: I): PartyDisbandedNotify {
    return PartyDisbandedNotify.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PartyDisbandedNotify>, I>>(object: I): PartyDisbandedNotify {
    const message = createBasePartyDisbandedNotify();
    message.partyId = object.partyId ?? "";
    message.actorUserId = object.actorUserId ?? "";
    message.timestamp = object.timestamp ?? 0;
    return message;
  },
};

function createBaseStoredParty(): StoredParty {
  return { partyId: "", leaderId: "", maxMembers: 0, createdAt: 0, members: [] };
}

export const StoredParty = {
  encode(message: StoredParty, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.partyId !== "") {
      writer.uint32(10).string(message.partyId);
    }
    if (message.leaderId !== "") {
      writer.uint32(18).string(message.leaderId);
    }
    if (message.maxMembers !== 0) {
      writer.uint32(24).int32(message.maxMembers);
    }
    if (message.createdAt !== 0) {
      writer.uint32(32).int64(message.createdAt);
    }
    for (const v of message.members) {
      StoredMember.encode(v!, writer.uint32(42).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): StoredParty {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStoredParty();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.partyId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.leaderId = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.maxMembers = reader.int32();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.createdAt = longToNumber(reader.int64() as Long);
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.members.push(StoredMember.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StoredParty {
    return {
      partyId: isSet(object.partyId) ? globalThis.String(object.partyId) : "",
      leaderId: isSet(object.leaderId) ? globalThis.String(object.leaderId) : "",
      maxMembers: isSet(object.maxMembers) ? globalThis.Number(object.maxMembers) : 0,
      createdAt: isSet(object.createdAt) ? globalThis.Number(object.createdAt) : 0,
      members: globalThis.Array.isArray(object?.members)
        ? object.members.map((e: any) => StoredMember.fromJSON(e))
        : [],
    };
  },

  toJSON(message: StoredParty): unknown {
    const obj: any = {};
    if (message.partyId !== "") {
      obj.partyId = message.partyId;
    }
    if (message.leaderId !== "") {
      obj.leaderId = message.leaderId;
    }
    if (message.maxMembers !== 0) {
      obj.maxMembers = Math.round(message.maxMembers);
    }
    if (message.createdAt !== 0) {
      obj.createdAt = Math.round(message.createdAt);
    }
    if (message.members?.length) {
      obj.members = message.members.map((e) => StoredMember.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StoredParty>, I>>(base?: I): StoredParty {
    return StoredParty.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StoredParty>, I>>(object: I): StoredParty {
    const message = createBaseStoredParty();
    message.partyId = object.partyId ?? "";
    message.leaderId = object.leaderId ?? "";
    message.maxMembers = object.maxMembers ?? 0;
    message.createdAt = object.createdAt ?? 0;
    message.members = object.members?.map((e) => StoredMember.fromPartial(e)) || [];
    return message;
  },
};

function createBaseStoredMember(): StoredMember {
  return { userId: "", ready: false, joinedAt: 0 };
}

export const StoredMember = {
  encode(message: StoredMember, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.ready !== false) {
      writer.uint32(16).bool(message.ready);
    }
    if (message.joinedAt !== 0) {
      writer.uint32(24).int64(message.joinedAt);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): StoredMember {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStoredMember();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.ready = reader.bool();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.joinedAt = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StoredMember {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      ready: isSet(object.ready) ? globalThis.Boolean(object.ready) : false,
      joinedAt: isSet(object.joinedAt) ? globalThis.Number(object.joinedAt) : 0,
    };
  },

  toJSON(message: StoredMember): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.ready !== false) {
      obj.ready = message.ready;
    }
    if (message.joinedAt !== 0) {
      obj.joinedAt = Math.round(message.joinedAt);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StoredMember>, I>>(base?: I): StoredMember {
    return StoredMember.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StoredMember>, I>>(object: I): StoredMember {
    const message = createBaseStoredMember();
    message.userId = object.userId ?? "";
    message.ready = object.ready ?? false;
    message.joinedAt = object.joinedAt ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

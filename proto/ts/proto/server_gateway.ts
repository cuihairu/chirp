// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.33.4
// source: proto/server_gateway.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";
import { ErrorCode, errorCodeFromJSON, errorCodeToJSON } from "./common";

export const protobufPackage = "chirp.server_gateway";

/**
 * Identity of an injected message's sender. Server-plane senders are not user
 * accounts; chat applies permission rules distinct from user accounts to them.
 */
export enum SenderKind {
  SENDER_UNKNOWN = 0,
  /** SENDER_SYSTEM - platform announcements */
  SENDER_SYSTEM = 1,
  /** SENDER_NPC - in-game NPC dialogue */
  SENDER_NPC = 2,
  /** SENDER_SERVICE - game service logic (trade state, matchmaking, ...) */
  SENDER_SERVICE = 3,
  UNRECOGNIZED = -1,
}

export function senderKindFromJSON(object: any): SenderKind {
  switch (object) {
    case 0:
    case "SENDER_UNKNOWN":
      return SenderKind.SENDER_UNKNOWN;
    case 1:
    case "SENDER_SYSTEM":
      return SenderKind.SENDER_SYSTEM;
    case 2:
    case "SENDER_NPC":
      return SenderKind.SENDER_NPC;
    case 3:
    case "SENDER_SERVICE":
      return SenderKind.SENDER_SERVICE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SenderKind.UNRECOGNIZED;
  }
}

export function senderKindToJSON(object: SenderKind): string {
  switch (object) {
    case SenderKind.SENDER_UNKNOWN:
      return "SENDER_UNKNOWN";
    case SenderKind.SENDER_SYSTEM:
      return "SENDER_SYSTEM";
    case SenderKind.SENDER_NPC:
      return "SENDER_NPC";
    case SenderKind.SENDER_SERVICE:
      return "SENDER_SERVICE";
    case SenderKind.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface ServerAuthRequest {
  serviceId: string;
  secret: string;
  /** must match the server's supported version */
  protocolVersion: number;
}

export interface ServerAuthResponse {
  code: ErrorCode;
  serverTimeMs: number;
  /**
   * Keepalive cadence the server assigns; connections silent for more than
   * twice this interval are closed.
   */
  heartbeatIntervalSeconds: number;
}

export interface ServerHeartbeatPing {
  clientTimeMs: number;
}

export interface ServerHeartbeatPong {
  serverTimeMs: number;
}

/**
 * Uplink: a trusted service asks chirp to deliver a message whose sender is
 * not a user. Routed to the chat service, which owns storage and delivery.
 */
export interface MessageInjectRequest {
  /** caller-supplied idempotency key */
  injectId: string;
  senderKind: SenderKind;
  /** e.g. "npc:blacksmith_01", "system", "trade" */
  senderId: string;
  channelType: number;
  /** set for channel injections */
  channelId: string;
  /** set for 1:1 injections */
  receiverId: string;
  content: Uint8Array;
}

export interface MessageInjectResponse {
  code: ErrorCode;
  injectId: string;
}

/** server_gateway -> chat (internal peer): the forwarded injection. */
export interface InjectMessageNotify {
  message: MessageInjectRequest | undefined;
}

/**
 * Uplink: publish an event that must reach the target service reliably.
 * Events are queued per target while it is offline and redelivered on
 * reconnect until acknowledged (at-least-once).
 */
export interface EventPublishRequest {
  /** optional caller-supplied idempotency key */
  eventId: string;
  targetServiceId: string;
  /** e.g. "quest.trigger", "trade.state" */
  eventType: string;
  payload: Uint8Array;
}

export interface EventPublishResponse {
  code: ErrorCode;
  eventId: string;
  /**
   * True when the target service was offline and the event was queued for
   * later delivery.
   */
  queued: boolean;
}

/** Downlink: an event delivered (or redelivered) to its target service. */
export interface EventDeliverNotify {
  eventId: string;
  eventType: string;
  payload: Uint8Array;
  publishedAtMs: number;
  /** 1 on first delivery, incremented per redelivery */
  attempt: number;
}

export interface EventAckRequest {
  eventIds: string[];
}

export interface EventAckResponse {
  code: ErrorCode;
}

/** Persistence record (Redis, not a wire message). */
export interface StoredIdentityBinding {
  bindingId: string;
  playerId: string;
  gameId: string;
  gameUserId: string;
  boundAtMs: number;
}

export interface BindPlayerIdentityRequest {
  /** caller-supplied idempotency key */
  bindingId: string;
  /** platform-scoped player identity */
  playerId: string;
  gameId: string;
  gameUserId: string;
}

export interface BindPlayerIdentityResponse {
  code: ErrorCode;
  bindingId: string;
  /**
   * True when this exact binding was already stored (idempotent no-op).
   * False with code=OK means newly bound; re-binding a game user to a
   * different player under a new binding_id overwrites (the backend is
   * the authority) and also reports OK.
   */
  existed: boolean;
}

export interface UnbindPlayerIdentityRequest {
  /** Select exactly one: binding_id, or the pair (game_id, game_user_id). */
  bindingId: string;
  gameId: string;
  gameUserId: string;
}

export interface UnbindPlayerIdentityResponse {
  code: ErrorCode;
}

export interface GetPlayerIdentitiesRequest {
  playerId: string;
}

export interface GetPlayerIdentitiesResponse {
  code: ErrorCode;
  bindings: StoredIdentityBinding[];
}

export interface ResolveGameUserRequest {
  gameId: string;
  gameUserId: string;
}

export interface ResolveGameUserResponse {
  code: ErrorCode;
  /** Empty when code=OK but the game user is unbound. */
  playerId: string;
}

/** Persistence record (Redis, not a wire message). */
export interface StoredChannelSubscription {
  subscriptionId: string;
  playerId: string;
  gameId: string;
  channelId: string;
  subscribedAtMs: number;
}

export interface SubscribePlayerChannelRequest {
  /**
   * Caller-supplied idempotency key; empty means the server mints one
   * (the player self-service path through app_gateway always mints).
   */
  subscriptionId: string;
  playerId: string;
  gameId: string;
  channelId: string;
}

export interface SubscribePlayerChannelResponse {
  code: ErrorCode;
  /** Echoed or minted id of the subscription now holding the tuple. */
  subscriptionId: string;
  /** True when the tuple was already subscribed (idempotent no-op). */
  existed: boolean;
}

export interface UnsubscribePlayerChannelRequest {
  /**
   * Select exactly one: subscription_id, or the full triple
   * (player_id, game_id, channel_id).
   */
  subscriptionId: string;
  playerId: string;
  gameId: string;
  channelId: string;
}

export interface UnsubscribePlayerChannelResponse {
  code: ErrorCode;
}

export interface GetPlayerSubscriptionsRequest {
  playerId: string;
  /** Optional filter: only subscriptions of this game when set. */
  gameId: string;
}

export interface GetPlayerSubscriptionsResponse {
  code: ErrorCode;
  subscriptions: StoredChannelSubscription[];
}

function createBaseServerAuthRequest(): ServerAuthRequest {
  return { serviceId: "", secret: "", protocolVersion: 0 };
}

export const ServerAuthRequest = {
  encode(message: ServerAuthRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.serviceId !== "") {
      writer.uint32(10).string(message.serviceId);
    }
    if (message.secret !== "") {
      writer.uint32(18).string(message.secret);
    }
    if (message.protocolVersion !== 0) {
      writer.uint32(24).int32(message.protocolVersion);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ServerAuthRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerAuthRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.serviceId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.secret = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.protocolVersion = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerAuthRequest {
    return {
      serviceId: isSet(object.serviceId) ? globalThis.String(object.serviceId) : "",
      secret: isSet(object.secret) ? globalThis.String(object.secret) : "",
      protocolVersion: isSet(object.protocolVersion) ? globalThis.Number(object.protocolVersion) : 0,
    };
  },

  toJSON(message: ServerAuthRequest): unknown {
    const obj: any = {};
    if (message.serviceId !== "") {
      obj.serviceId = message.serviceId;
    }
    if (message.secret !== "") {
      obj.secret = message.secret;
    }
    if (message.protocolVersion !== 0) {
      obj.protocolVersion = Math.round(message.protocolVersion);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServerAuthRequest>, I>>(base?: I): ServerAuthRequest {
    return ServerAuthRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServerAuthRequest>, I>>(object: I): ServerAuthRequest {
    const message = createBaseServerAuthRequest();
    message.serviceId = object.serviceId ?? "";
    message.secret = object.secret ?? "";
    message.protocolVersion = object.protocolVersion ?? 0;
    return message;
  },
};

function createBaseServerAuthResponse(): ServerAuthResponse {
  return { code: 0, serverTimeMs: 0, heartbeatIntervalSeconds: 0 };
}

export const ServerAuthResponse = {
  encode(message: ServerAuthResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.serverTimeMs !== 0) {
      writer.uint32(16).int64(message.serverTimeMs);
    }
    if (message.heartbeatIntervalSeconds !== 0) {
      writer.uint32(24).int32(message.heartbeatIntervalSeconds);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ServerAuthResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerAuthResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.serverTimeMs = longToNumber(reader.int64() as Long);
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.heartbeatIntervalSeconds = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerAuthResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      serverTimeMs: isSet(object.serverTimeMs) ? globalThis.Number(object.serverTimeMs) : 0,
      heartbeatIntervalSeconds: isSet(object.heartbeatIntervalSeconds)
        ? globalThis.Number(object.heartbeatIntervalSeconds)
        : 0,
    };
  },

  toJSON(message: ServerAuthResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.serverTimeMs !== 0) {
      obj.serverTimeMs = Math.round(message.serverTimeMs);
    }
    if (message.heartbeatIntervalSeconds !== 0) {
      obj.heartbeatIntervalSeconds = Math.round(message.heartbeatIntervalSeconds);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServerAuthResponse>, I>>(base?: I): ServerAuthResponse {
    return ServerAuthResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServerAuthResponse>, I>>(object: I): ServerAuthResponse {
    const message = createBaseServerAuthResponse();
    message.code = object.code ?? 0;
    message.serverTimeMs = object.serverTimeMs ?? 0;
    message.heartbeatIntervalSeconds = object.heartbeatIntervalSeconds ?? 0;
    return message;
  },
};

function createBaseServerHeartbeatPing(): ServerHeartbeatPing {
  return { clientTimeMs: 0 };
}

export const ServerHeartbeatPing = {
  encode(message: ServerHeartbeatPing, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.clientTimeMs !== 0) {
      writer.uint32(8).int64(message.clientTimeMs);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ServerHeartbeatPing {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerHeartbeatPing();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.clientTimeMs = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerHeartbeatPing {
    return { clientTimeMs: isSet(object.clientTimeMs) ? globalThis.Number(object.clientTimeMs) : 0 };
  },

  toJSON(message: ServerHeartbeatPing): unknown {
    const obj: any = {};
    if (message.clientTimeMs !== 0) {
      obj.clientTimeMs = Math.round(message.clientTimeMs);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServerHeartbeatPing>, I>>(base?: I): ServerHeartbeatPing {
    return ServerHeartbeatPing.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServerHeartbeatPing>, I>>(object: I): ServerHeartbeatPing {
    const message = createBaseServerHeartbeatPing();
    message.clientTimeMs = object.clientTimeMs ?? 0;
    return message;
  },
};

function createBaseServerHeartbeatPong(): ServerHeartbeatPong {
  return { serverTimeMs: 0 };
}

export const ServerHeartbeatPong = {
  encode(message: ServerHeartbeatPong, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.serverTimeMs !== 0) {
      writer.uint32(8).int64(message.serverTimeMs);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ServerHeartbeatPong {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerHeartbeatPong();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.serverTimeMs = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerHeartbeatPong {
    return { serverTimeMs: isSet(object.serverTimeMs) ? globalThis.Number(object.serverTimeMs) : 0 };
  },

  toJSON(message: ServerHeartbeatPong): unknown {
    const obj: any = {};
    if (message.serverTimeMs !== 0) {
      obj.serverTimeMs = Math.round(message.serverTimeMs);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServerHeartbeatPong>, I>>(base?: I): ServerHeartbeatPong {
    return ServerHeartbeatPong.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServerHeartbeatPong>, I>>(object: I): ServerHeartbeatPong {
    const message = createBaseServerHeartbeatPong();
    message.serverTimeMs = object.serverTimeMs ?? 0;
    return message;
  },
};

function createBaseMessageInjectRequest(): MessageInjectRequest {
  return {
    injectId: "",
    senderKind: 0,
    senderId: "",
    channelType: 0,
    channelId: "",
    receiverId: "",
    content: new Uint8Array(0),
  };
}

export const MessageInjectRequest = {
  encode(message: MessageInjectRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.injectId !== "") {
      writer.uint32(10).string(message.injectId);
    }
    if (message.senderKind !== 0) {
      writer.uint32(16).int32(message.senderKind);
    }
    if (message.senderId !== "") {
      writer.uint32(26).string(message.senderId);
    }
    if (message.channelType !== 0) {
      writer.uint32(32).int32(message.channelType);
    }
    if (message.channelId !== "") {
      writer.uint32(42).string(message.channelId);
    }
    if (message.receiverId !== "") {
      writer.uint32(50).string(message.receiverId);
    }
    if (message.content.length !== 0) {
      writer.uint32(58).bytes(message.content);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MessageInjectRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMessageInjectRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.injectId = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.senderKind = reader.int32() as any;
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.senderId = reader.string();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.channelType = reader.int32();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.channelId = reader.string();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.receiverId = reader.string();
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.content = reader.bytes();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MessageInjectRequest {
    return {
      injectId: isSet(object.injectId) ? globalThis.String(object.injectId) : "",
      senderKind: isSet(object.senderKind) ? senderKindFromJSON(object.senderKind) : 0,
      senderId: isSet(object.senderId) ? globalThis.String(object.senderId) : "",
      channelType: isSet(object.channelType) ? globalThis.Number(object.channelType) : 0,
      channelId: isSet(object.channelId) ? globalThis.String(object.channelId) : "",
      receiverId: isSet(object.receiverId) ? globalThis.String(object.receiverId) : "",
      content: isSet(object.content) ? bytesFromBase64(object.content) : new Uint8Array(0),
    };
  },

  toJSON(message: MessageInjectRequest): unknown {
    const obj: any = {};
    if (message.injectId !== "") {
      obj.injectId = message.injectId;
    }
    if (message.senderKind !== 0) {
      obj.senderKind = senderKindToJSON(message.senderKind);
    }
    if (message.senderId !== "") {
      obj.senderId = message.senderId;
    }
    if (message.channelType !== 0) {
      obj.channelType = Math.round(message.channelType);
    }
    if (message.channelId !== "") {
      obj.channelId = message.channelId;
    }
    if (message.receiverId !== "") {
      obj.receiverId = message.receiverId;
    }
    if (message.content.length !== 0) {
      obj.content = base64FromBytes(message.content);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MessageInjectRequest>, I>>(base?: I): MessageInjectRequest {
    return MessageInjectRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MessageInjectRequest>, I>>(object: I): MessageInjectRequest {
    const message = createBaseMessageInjectRequest();
    message.injectId = object.injectId ?? "";
    message.senderKind = object.senderKind ?? 0;
    message.senderId = object.senderId ?? "";
    message.channelType = object.channelType ?? 0;
    message.channelId = object.channelId ?? "";
    message.receiverId = object.receiverId ?? "";
    message.content = object.content ?? new Uint8Array(0);
    return message;
  },
};

function createBaseMessageInjectResponse(): MessageInjectResponse {
  return { code: 0, injectId: "" };
}

export const MessageInjectResponse = {
  encode(message: MessageInjectResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.injectId !== "") {
      writer.uint32(18).string(message.injectId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MessageInjectResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMessageInjectResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.injectId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MessageInjectResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      injectId: isSet(object.injectId) ? globalThis.String(object.injectId) : "",
    };
  },

  toJSON(message: MessageInjectResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.injectId !== "") {
      obj.injectId = message.injectId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MessageInjectResponse>, I>>(base?: I): MessageInjectResponse {
    return MessageInjectResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MessageInjectResponse>, I>>(object: I): MessageInjectResponse {
    const message = createBaseMessageInjectResponse();
    message.code = object.code ?? 0;
    message.injectId = object.injectId ?? "";
    return message;
  },
};

function createBaseInjectMessageNotify(): InjectMessageNotify {
  return { message: undefined };
}

export const InjectMessageNotify = {
  encode(message: InjectMessageNotify, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.message !== undefined) {
      MessageInjectRequest.encode(message.message, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): InjectMessageNotify {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInjectMessageNotify();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.message = MessageInjectRequest.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InjectMessageNotify {
    return { message: isSet(object.message) ? MessageInjectRequest.fromJSON(object.message) : undefined };
  },

  toJSON(message: InjectMessageNotify): unknown {
    const obj: any = {};
    if (message.message !== undefined) {
      obj.message = MessageInjectRequest.toJSON(message.message);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InjectMessageNotify>, I>>(base?: I): InjectMessageNotify {
    return InjectMessageNotify.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InjectMessageNotify>, I>>(object: I): InjectMessageNotify {
    const message = createBaseInjectMessageNotify();
    message.message = (object.message !== undefined && object.message !== null)
      ? MessageInjectRequest.fromPartial(object.message)
      : undefined;
    return message;
  },
};

function createBaseEventPublishRequest(): EventPublishRequest {
  return { eventId: "", targetServiceId: "", eventType: "", payload: new Uint8Array(0) };
}

export const EventPublishRequest = {
  encode(message: EventPublishRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.eventId !== "") {
      writer.uint32(10).string(message.eventId);
    }
    if (message.targetServiceId !== "") {
      writer.uint32(18).string(message.targetServiceId);
    }
    if (message.eventType !== "") {
      writer.uint32(26).string(message.eventType);
    }
    if (message.payload.length !== 0) {
      writer.uint32(34).bytes(message.payload);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): EventPublishRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventPublishRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.eventId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.targetServiceId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.eventType = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.payload = reader.bytes();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventPublishRequest {
    return {
      eventId: isSet(object.eventId) ? globalThis.String(object.eventId) : "",
      targetServiceId: isSet(object.targetServiceId) ? globalThis.String(object.targetServiceId) : "",
      eventType: isSet(object.eventType) ? globalThis.String(object.eventType) : "",
      payload: isSet(object.payload) ? bytesFromBase64(object.payload) : new Uint8Array(0),
    };
  },

  toJSON(message: EventPublishRequest): unknown {
    const obj: any = {};
    if (message.eventId !== "") {
      obj.eventId = message.eventId;
    }
    if (message.targetServiceId !== "") {
      obj.targetServiceId = message.targetServiceId;
    }
    if (message.eventType !== "") {
      obj.eventType = message.eventType;
    }
    if (message.payload.length !== 0) {
      obj.payload = base64FromBytes(message.payload);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EventPublishRequest>, I>>(base?: I): EventPublishRequest {
    return EventPublishRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventPublishRequest>, I>>(object: I): EventPublishRequest {
    const message = createBaseEventPublishRequest();
    message.eventId = object.eventId ?? "";
    message.targetServiceId = object.targetServiceId ?? "";
    message.eventType = object.eventType ?? "";
    message.payload = object.payload ?? new Uint8Array(0);
    return message;
  },
};

function createBaseEventPublishResponse(): EventPublishResponse {
  return { code: 0, eventId: "", queued: false };
}

export const EventPublishResponse = {
  encode(message: EventPublishResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.eventId !== "") {
      writer.uint32(18).string(message.eventId);
    }
    if (message.queued !== false) {
      writer.uint32(24).bool(message.queued);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): EventPublishResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventPublishResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.eventId = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.queued = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventPublishResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      eventId: isSet(object.eventId) ? globalThis.String(object.eventId) : "",
      queued: isSet(object.queued) ? globalThis.Boolean(object.queued) : false,
    };
  },

  toJSON(message: EventPublishResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.eventId !== "") {
      obj.eventId = message.eventId;
    }
    if (message.queued !== false) {
      obj.queued = message.queued;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EventPublishResponse>, I>>(base?: I): EventPublishResponse {
    return EventPublishResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventPublishResponse>, I>>(object: I): EventPublishResponse {
    const message = createBaseEventPublishResponse();
    message.code = object.code ?? 0;
    message.eventId = object.eventId ?? "";
    message.queued = object.queued ?? false;
    return message;
  },
};

function createBaseEventDeliverNotify(): EventDeliverNotify {
  return { eventId: "", eventType: "", payload: new Uint8Array(0), publishedAtMs: 0, attempt: 0 };
}

export const EventDeliverNotify = {
  encode(message: EventDeliverNotify, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.eventId !== "") {
      writer.uint32(10).string(message.eventId);
    }
    if (message.eventType !== "") {
      writer.uint32(18).string(message.eventType);
    }
    if (message.payload.length !== 0) {
      writer.uint32(26).bytes(message.payload);
    }
    if (message.publishedAtMs !== 0) {
      writer.uint32(32).int64(message.publishedAtMs);
    }
    if (message.attempt !== 0) {
      writer.uint32(40).int32(message.attempt);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): EventDeliverNotify {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventDeliverNotify();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.eventId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.eventType = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.payload = reader.bytes();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.publishedAtMs = longToNumber(reader.int64() as Long);
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.attempt = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventDeliverNotify {
    return {
      eventId: isSet(object.eventId) ? globalThis.String(object.eventId) : "",
      eventType: isSet(object.eventType) ? globalThis.String(object.eventType) : "",
      payload: isSet(object.payload) ? bytesFromBase64(object.payload) : new Uint8Array(0),
      publishedAtMs: isSet(object.publishedAtMs) ? globalThis.Number(object.publishedAtMs) : 0,
      attempt: isSet(object.attempt) ? globalThis.Number(object.attempt) : 0,
    };
  },

  toJSON(message: EventDeliverNotify): unknown {
    const obj: any = {};
    if (message.eventId !== "") {
      obj.eventId = message.eventId;
    }
    if (message.eventType !== "") {
      obj.eventType = message.eventType;
    }
    if (message.payload.length !== 0) {
      obj.payload = base64FromBytes(message.payload);
    }
    if (message.publishedAtMs !== 0) {
      obj.publishedAtMs = Math.round(message.publishedAtMs);
    }
    if (message.attempt !== 0) {
      obj.attempt = Math.round(message.attempt);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EventDeliverNotify>, I>>(base?: I): EventDeliverNotify {
    return EventDeliverNotify.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventDeliverNotify>, I>>(object: I): EventDeliverNotify {
    const message = createBaseEventDeliverNotify();
    message.eventId = object.eventId ?? "";
    message.eventType = object.eventType ?? "";
    message.payload = object.payload ?? new Uint8Array(0);
    message.publishedAtMs = object.publishedAtMs ?? 0;
    message.attempt = object.attempt ?? 0;
    return message;
  },
};

function createBaseEventAckRequest(): EventAckRequest {
  return { eventIds: [] };
}

export const EventAckRequest = {
  encode(message: EventAckRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.eventIds) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): EventAckRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventAckRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.eventIds.push(reader.string());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventAckRequest {
    return {
      eventIds: globalThis.Array.isArray(object?.eventIds) ? object.eventIds.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: EventAckRequest): unknown {
    const obj: any = {};
    if (message.eventIds?.length) {
      obj.eventIds = message.eventIds;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EventAckRequest>, I>>(base?: I): EventAckRequest {
    return EventAckRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventAckRequest>, I>>(object: I): EventAckRequest {
    const message = createBaseEventAckRequest();
    message.eventIds = object.eventIds?.map((e) => e) || [];
    return message;
  },
};

function createBaseEventAckResponse(): EventAckResponse {
  return { code: 0 };
}

export const EventAckResponse = {
  encode(message: EventAckResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): EventAckResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventAckResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventAckResponse {
    return { code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0 };
  },

  toJSON(message: EventAckResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EventAckResponse>, I>>(base?: I): EventAckResponse {
    return EventAckResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventAckResponse>, I>>(object: I): EventAckResponse {
    const message = createBaseEventAckResponse();
    message.code = object.code ?? 0;
    return message;
  },
};

function createBaseStoredIdentityBinding(): StoredIdentityBinding {
  return { bindingId: "", playerId: "", gameId: "", gameUserId: "", boundAtMs: 0 };
}

export const StoredIdentityBinding = {
  encode(message: StoredIdentityBinding, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.bindingId !== "") {
      writer.uint32(10).string(message.bindingId);
    }
    if (message.playerId !== "") {
      writer.uint32(18).string(message.playerId);
    }
    if (message.gameId !== "") {
      writer.uint32(26).string(message.gameId);
    }
    if (message.gameUserId !== "") {
      writer.uint32(34).string(message.gameUserId);
    }
    if (message.boundAtMs !== 0) {
      writer.uint32(40).int64(message.boundAtMs);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): StoredIdentityBinding {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStoredIdentityBinding();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.bindingId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.playerId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.gameId = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.gameUserId = reader.string();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.boundAtMs = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StoredIdentityBinding {
    return {
      bindingId: isSet(object.bindingId) ? globalThis.String(object.bindingId) : "",
      playerId: isSet(object.playerId) ? globalThis.String(object.playerId) : "",
      gameId: isSet(object.gameId) ? globalThis.String(object.gameId) : "",
      gameUserId: isSet(object.gameUserId) ? globalThis.String(object.gameUserId) : "",
      boundAtMs: isSet(object.boundAtMs) ? globalThis.Number(object.boundAtMs) : 0,
    };
  },

  toJSON(message: StoredIdentityBinding): unknown {
    const obj: any = {};
    if (message.bindingId !== "") {
      obj.bindingId = message.bindingId;
    }
    if (message.playerId !== "") {
      obj.playerId = message.playerId;
    }
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    if (message.gameUserId !== "") {
      obj.gameUserId = message.gameUserId;
    }
    if (message.boundAtMs !== 0) {
      obj.boundAtMs = Math.round(message.boundAtMs);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StoredIdentityBinding>, I>>(base?: I): StoredIdentityBinding {
    return StoredIdentityBinding.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StoredIdentityBinding>, I>>(object: I): StoredIdentityBinding {
    const message = createBaseStoredIdentityBinding();
    message.bindingId = object.bindingId ?? "";
    message.playerId = object.playerId ?? "";
    message.gameId = object.gameId ?? "";
    message.gameUserId = object.gameUserId ?? "";
    message.boundAtMs = object.boundAtMs ?? 0;
    return message;
  },
};

function createBaseBindPlayerIdentityRequest(): BindPlayerIdentityRequest {
  return { bindingId: "", playerId: "", gameId: "", gameUserId: "" };
}

export const BindPlayerIdentityRequest = {
  encode(message: BindPlayerIdentityRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.bindingId !== "") {
      writer.uint32(10).string(message.bindingId);
    }
    if (message.playerId !== "") {
      writer.uint32(18).string(message.playerId);
    }
    if (message.gameId !== "") {
      writer.uint32(26).string(message.gameId);
    }
    if (message.gameUserId !== "") {
      writer.uint32(34).string(message.gameUserId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): BindPlayerIdentityRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBindPlayerIdentityRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.bindingId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.playerId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.gameId = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.gameUserId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BindPlayerIdentityRequest {
    return {
      bindingId: isSet(object.bindingId) ? globalThis.String(object.bindingId) : "",
      playerId: isSet(object.playerId) ? globalThis.String(object.playerId) : "",
      gameId: isSet(object.gameId) ? globalThis.String(object.gameId) : "",
      gameUserId: isSet(object.gameUserId) ? globalThis.String(object.gameUserId) : "",
    };
  },

  toJSON(message: BindPlayerIdentityRequest): unknown {
    const obj: any = {};
    if (message.bindingId !== "") {
      obj.bindingId = message.bindingId;
    }
    if (message.playerId !== "") {
      obj.playerId = message.playerId;
    }
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    if (message.gameUserId !== "") {
      obj.gameUserId = message.gameUserId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BindPlayerIdentityRequest>, I>>(base?: I): BindPlayerIdentityRequest {
    return BindPlayerIdentityRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BindPlayerIdentityRequest>, I>>(object: I): BindPlayerIdentityRequest {
    const message = createBaseBindPlayerIdentityRequest();
    message.bindingId = object.bindingId ?? "";
    message.playerId = object.playerId ?? "";
    message.gameId = object.gameId ?? "";
    message.gameUserId = object.gameUserId ?? "";
    return message;
  },
};

function createBaseBindPlayerIdentityResponse(): BindPlayerIdentityResponse {
  return { code: 0, bindingId: "", existed: false };
}

export const BindPlayerIdentityResponse = {
  encode(message: BindPlayerIdentityResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.bindingId !== "") {
      writer.uint32(18).string(message.bindingId);
    }
    if (message.existed !== false) {
      writer.uint32(24).bool(message.existed);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): BindPlayerIdentityResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBindPlayerIdentityResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.bindingId = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.existed = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BindPlayerIdentityResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      bindingId: isSet(object.bindingId) ? globalThis.String(object.bindingId) : "",
      existed: isSet(object.existed) ? globalThis.Boolean(object.existed) : false,
    };
  },

  toJSON(message: BindPlayerIdentityResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.bindingId !== "") {
      obj.bindingId = message.bindingId;
    }
    if (message.existed !== false) {
      obj.existed = message.existed;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BindPlayerIdentityResponse>, I>>(base?: I): BindPlayerIdentityResponse {
    return BindPlayerIdentityResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BindPlayerIdentityResponse>, I>>(object: I): BindPlayerIdentityResponse {
    const message = createBaseBindPlayerIdentityResponse();
    message.code = object.code ?? 0;
    message.bindingId = object.bindingId ?? "";
    message.existed = object.existed ?? false;
    return message;
  },
};

function createBaseUnbindPlayerIdentityRequest(): UnbindPlayerIdentityRequest {
  return { bindingId: "", gameId: "", gameUserId: "" };
}

export const UnbindPlayerIdentityRequest = {
  encode(message: UnbindPlayerIdentityRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.bindingId !== "") {
      writer.uint32(10).string(message.bindingId);
    }
    if (message.gameId !== "") {
      writer.uint32(18).string(message.gameId);
    }
    if (message.gameUserId !== "") {
      writer.uint32(26).string(message.gameUserId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): UnbindPlayerIdentityRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnbindPlayerIdentityRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.bindingId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.gameId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.gameUserId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UnbindPlayerIdentityRequest {
    return {
      bindingId: isSet(object.bindingId) ? globalThis.String(object.bindingId) : "",
      gameId: isSet(object.gameId) ? globalThis.String(object.gameId) : "",
      gameUserId: isSet(object.gameUserId) ? globalThis.String(object.gameUserId) : "",
    };
  },

  toJSON(message: UnbindPlayerIdentityRequest): unknown {
    const obj: any = {};
    if (message.bindingId !== "") {
      obj.bindingId = message.bindingId;
    }
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    if (message.gameUserId !== "") {
      obj.gameUserId = message.gameUserId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UnbindPlayerIdentityRequest>, I>>(base?: I): UnbindPlayerIdentityRequest {
    return UnbindPlayerIdentityRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UnbindPlayerIdentityRequest>, I>>(object: I): UnbindPlayerIdentityRequest {
    const message = createBaseUnbindPlayerIdentityRequest();
    message.bindingId = object.bindingId ?? "";
    message.gameId = object.gameId ?? "";
    message.gameUserId = object.gameUserId ?? "";
    return message;
  },
};

function createBaseUnbindPlayerIdentityResponse(): UnbindPlayerIdentityResponse {
  return { code: 0 };
}

export const UnbindPlayerIdentityResponse = {
  encode(message: UnbindPlayerIdentityResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): UnbindPlayerIdentityResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnbindPlayerIdentityResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UnbindPlayerIdentityResponse {
    return { code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0 };
  },

  toJSON(message: UnbindPlayerIdentityResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UnbindPlayerIdentityResponse>, I>>(base?: I): UnbindPlayerIdentityResponse {
    return UnbindPlayerIdentityResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UnbindPlayerIdentityResponse>, I>>(object: I): UnbindPlayerIdentityResponse {
    const message = createBaseUnbindPlayerIdentityResponse();
    message.code = object.code ?? 0;
    return message;
  },
};

function createBaseGetPlayerIdentitiesRequest(): GetPlayerIdentitiesRequest {
  return { playerId: "" };
}

export const GetPlayerIdentitiesRequest = {
  encode(message: GetPlayerIdentitiesRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.playerId !== "") {
      writer.uint32(10).string(message.playerId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetPlayerIdentitiesRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPlayerIdentitiesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.playerId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPlayerIdentitiesRequest {
    return { playerId: isSet(object.playerId) ? globalThis.String(object.playerId) : "" };
  },

  toJSON(message: GetPlayerIdentitiesRequest): unknown {
    const obj: any = {};
    if (message.playerId !== "") {
      obj.playerId = message.playerId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPlayerIdentitiesRequest>, I>>(base?: I): GetPlayerIdentitiesRequest {
    return GetPlayerIdentitiesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPlayerIdentitiesRequest>, I>>(object: I): GetPlayerIdentitiesRequest {
    const message = createBaseGetPlayerIdentitiesRequest();
    message.playerId = object.playerId ?? "";
    return message;
  },
};

function createBaseGetPlayerIdentitiesResponse(): GetPlayerIdentitiesResponse {
  return { code: 0, bindings: [] };
}

export const GetPlayerIdentitiesResponse = {
  encode(message: GetPlayerIdentitiesResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    for (const v of message.bindings) {
      StoredIdentityBinding.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetPlayerIdentitiesResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPlayerIdentitiesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.bindings.push(StoredIdentityBinding.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPlayerIdentitiesResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      bindings: globalThis.Array.isArray(object?.bindings)
        ? object.bindings.map((e: any) => StoredIdentityBinding.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetPlayerIdentitiesResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.bindings?.length) {
      obj.bindings = message.bindings.map((e) => StoredIdentityBinding.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPlayerIdentitiesResponse>, I>>(base?: I): GetPlayerIdentitiesResponse {
    return GetPlayerIdentitiesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPlayerIdentitiesResponse>, I>>(object: I): GetPlayerIdentitiesResponse {
    const message = createBaseGetPlayerIdentitiesResponse();
    message.code = object.code ?? 0;
    message.bindings = object.bindings?.map((e) => StoredIdentityBinding.fromPartial(e)) || [];
    return message;
  },
};

function createBaseResolveGameUserRequest(): ResolveGameUserRequest {
  return { gameId: "", gameUserId: "" };
}

export const ResolveGameUserRequest = {
  encode(message: ResolveGameUserRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.gameId !== "") {
      writer.uint32(10).string(message.gameId);
    }
    if (message.gameUserId !== "") {
      writer.uint32(18).string(message.gameUserId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ResolveGameUserRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResolveGameUserRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.gameId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.gameUserId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResolveGameUserRequest {
    return {
      gameId: isSet(object.gameId) ? globalThis.String(object.gameId) : "",
      gameUserId: isSet(object.gameUserId) ? globalThis.String(object.gameUserId) : "",
    };
  },

  toJSON(message: ResolveGameUserRequest): unknown {
    const obj: any = {};
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    if (message.gameUserId !== "") {
      obj.gameUserId = message.gameUserId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResolveGameUserRequest>, I>>(base?: I): ResolveGameUserRequest {
    return ResolveGameUserRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResolveGameUserRequest>, I>>(object: I): ResolveGameUserRequest {
    const message = createBaseResolveGameUserRequest();
    message.gameId = object.gameId ?? "";
    message.gameUserId = object.gameUserId ?? "";
    return message;
  },
};

function createBaseResolveGameUserResponse(): ResolveGameUserResponse {
  return { code: 0, playerId: "" };
}

export const ResolveGameUserResponse = {
  encode(message: ResolveGameUserResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.playerId !== "") {
      writer.uint32(18).string(message.playerId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ResolveGameUserResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResolveGameUserResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.playerId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResolveGameUserResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      playerId: isSet(object.playerId) ? globalThis.String(object.playerId) : "",
    };
  },

  toJSON(message: ResolveGameUserResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.playerId !== "") {
      obj.playerId = message.playerId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResolveGameUserResponse>, I>>(base?: I): ResolveGameUserResponse {
    return ResolveGameUserResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResolveGameUserResponse>, I>>(object: I): ResolveGameUserResponse {
    const message = createBaseResolveGameUserResponse();
    message.code = object.code ?? 0;
    message.playerId = object.playerId ?? "";
    return message;
  },
};

function createBaseStoredChannelSubscription(): StoredChannelSubscription {
  return { subscriptionId: "", playerId: "", gameId: "", channelId: "", subscribedAtMs: 0 };
}

export const StoredChannelSubscription = {
  encode(message: StoredChannelSubscription, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.subscriptionId !== "") {
      writer.uint32(10).string(message.subscriptionId);
    }
    if (message.playerId !== "") {
      writer.uint32(18).string(message.playerId);
    }
    if (message.gameId !== "") {
      writer.uint32(26).string(message.gameId);
    }
    if (message.channelId !== "") {
      writer.uint32(34).string(message.channelId);
    }
    if (message.subscribedAtMs !== 0) {
      writer.uint32(40).int64(message.subscribedAtMs);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): StoredChannelSubscription {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStoredChannelSubscription();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.subscriptionId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.playerId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.gameId = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.channelId = reader.string();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.subscribedAtMs = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StoredChannelSubscription {
    return {
      subscriptionId: isSet(object.subscriptionId) ? globalThis.String(object.subscriptionId) : "",
      playerId: isSet(object.playerId) ? globalThis.String(object.playerId) : "",
      gameId: isSet(object.gameId) ? globalThis.String(object.gameId) : "",
      channelId: isSet(object.channelId) ? globalThis.String(object.channelId) : "",
      subscribedAtMs: isSet(object.subscribedAtMs) ? globalThis.Number(object.subscribedAtMs) : 0,
    };
  },

  toJSON(message: StoredChannelSubscription): unknown {
    const obj: any = {};
    if (message.subscriptionId !== "") {
      obj.subscriptionId = message.subscriptionId;
    }
    if (message.playerId !== "") {
      obj.playerId = message.playerId;
    }
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    if (message.channelId !== "") {
      obj.channelId = message.channelId;
    }
    if (message.subscribedAtMs !== 0) {
      obj.subscribedAtMs = Math.round(message.subscribedAtMs);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StoredChannelSubscription>, I>>(base?: I): StoredChannelSubscription {
    return StoredChannelSubscription.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StoredChannelSubscription>, I>>(object: I): StoredChannelSubscription {
    const message = createBaseStoredChannelSubscription();
    message.subscriptionId = object.subscriptionId ?? "";
    message.playerId = object.playerId ?? "";
    message.gameId = object.gameId ?? "";
    message.channelId = object.channelId ?? "";
    message.subscribedAtMs = object.subscribedAtMs ?? 0;
    return message;
  },
};

function createBaseSubscribePlayerChannelRequest(): SubscribePlayerChannelRequest {
  return { subscriptionId: "", playerId: "", gameId: "", channelId: "" };
}

export const SubscribePlayerChannelRequest = {
  encode(message: SubscribePlayerChannelRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.subscriptionId !== "") {
      writer.uint32(10).string(message.subscriptionId);
    }
    if (message.playerId !== "") {
      writer.uint32(18).string(message.playerId);
    }
    if (message.gameId !== "") {
      writer.uint32(26).string(message.gameId);
    }
    if (message.channelId !== "") {
      writer.uint32(34).string(message.channelId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SubscribePlayerChannelRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSubscribePlayerChannelRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.subscriptionId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.playerId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.gameId = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.channelId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SubscribePlayerChannelRequest {
    return {
      subscriptionId: isSet(object.subscriptionId) ? globalThis.String(object.subscriptionId) : "",
      playerId: isSet(object.playerId) ? globalThis.String(object.playerId) : "",
      gameId: isSet(object.gameId) ? globalThis.String(object.gameId) : "",
      channelId: isSet(object.channelId) ? globalThis.String(object.channelId) : "",
    };
  },

  toJSON(message: SubscribePlayerChannelRequest): unknown {
    const obj: any = {};
    if (message.subscriptionId !== "") {
      obj.subscriptionId = message.subscriptionId;
    }
    if (message.playerId !== "") {
      obj.playerId = message.playerId;
    }
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    if (message.channelId !== "") {
      obj.channelId = message.channelId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SubscribePlayerChannelRequest>, I>>(base?: I): SubscribePlayerChannelRequest {
    return SubscribePlayerChannelRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SubscribePlayerChannelRequest>, I>>(
    object: I,
  ): SubscribePlayerChannelRequest {
    const message = createBaseSubscribePlayerChannelRequest();
    message.subscriptionId = object.subscriptionId ?? "";
    message.playerId = object.playerId ?? "";
    message.gameId = object.gameId ?? "";
    message.channelId = object.channelId ?? "";
    return message;
  },
};

function createBaseSubscribePlayerChannelResponse(): SubscribePlayerChannelResponse {
  return { code: 0, subscriptionId: "", existed: false };
}

export const SubscribePlayerChannelResponse = {
  encode(message: SubscribePlayerChannelResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.subscriptionId !== "") {
      writer.uint32(18).string(message.subscriptionId);
    }
    if (message.existed !== false) {
      writer.uint32(24).bool(message.existed);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SubscribePlayerChannelResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSubscribePlayerChannelResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.subscriptionId = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.existed = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SubscribePlayerChannelResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      subscriptionId: isSet(object.subscriptionId) ? globalThis.String(object.subscriptionId) : "",
      existed: isSet(object.existed) ? globalThis.Boolean(object.existed) : false,
    };
  },

  toJSON(message: SubscribePlayerChannelResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.subscriptionId !== "") {
      obj.subscriptionId = message.subscriptionId;
    }
    if (message.existed !== false) {
      obj.existed = message.existed;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SubscribePlayerChannelResponse>, I>>(base?: I): SubscribePlayerChannelResponse {
    return SubscribePlayerChannelResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SubscribePlayerChannelResponse>, I>>(
    object: I,
  ): SubscribePlayerChannelResponse {
    const message = createBaseSubscribePlayerChannelResponse();
    message.code = object.code ?? 0;
    message.subscriptionId = object.subscriptionId ?? "";
    message.existed = object.existed ?? false;
    return message;
  },
};

function createBaseUnsubscribePlayerChannelRequest(): UnsubscribePlayerChannelRequest {
  return { subscriptionId: "", playerId: "", gameId: "", channelId: "" };
}

export const UnsubscribePlayerChannelRequest = {
  encode(message: UnsubscribePlayerChannelRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.subscriptionId !== "") {
      writer.uint32(10).string(message.subscriptionId);
    }
    if (message.playerId !== "") {
      writer.uint32(18).string(message.playerId);
    }
    if (message.gameId !== "") {
      writer.uint32(26).string(message.gameId);
    }
    if (message.channelId !== "") {
      writer.uint32(34).string(message.channelId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): UnsubscribePlayerChannelRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnsubscribePlayerChannelRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.subscriptionId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.playerId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.gameId = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.channelId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UnsubscribePlayerChannelRequest {
    return {
      subscriptionId: isSet(object.subscriptionId) ? globalThis.String(object.subscriptionId) : "",
      playerId: isSet(object.playerId) ? globalThis.String(object.playerId) : "",
      gameId: isSet(object.gameId) ? globalThis.String(object.gameId) : "",
      channelId: isSet(object.channelId) ? globalThis.String(object.channelId) : "",
    };
  },

  toJSON(message: UnsubscribePlayerChannelRequest): unknown {
    const obj: any = {};
    if (message.subscriptionId !== "") {
      obj.subscriptionId = message.subscriptionId;
    }
    if (message.playerId !== "") {
      obj.playerId = message.playerId;
    }
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    if (message.channelId !== "") {
      obj.channelId = message.channelId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UnsubscribePlayerChannelRequest>, I>>(base?: I): UnsubscribePlayerChannelRequest {
    return UnsubscribePlayerChannelRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UnsubscribePlayerChannelRequest>, I>>(
    object: I,
  ): UnsubscribePlayerChannelRequest {
    const message = createBaseUnsubscribePlayerChannelRequest();
    message.subscriptionId = object.subscriptionId ?? "";
    message.playerId = object.playerId ?? "";
    message.gameId = object.gameId ?? "";
    message.channelId = object.channelId ?? "";
    return message;
  },
};

function createBaseUnsubscribePlayerChannelResponse(): UnsubscribePlayerChannelResponse {
  return { code: 0 };
}

export const UnsubscribePlayerChannelResponse = {
  encode(message: UnsubscribePlayerChannelResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): UnsubscribePlayerChannelResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnsubscribePlayerChannelResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UnsubscribePlayerChannelResponse {
    return { code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0 };
  },

  toJSON(message: UnsubscribePlayerChannelResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UnsubscribePlayerChannelResponse>, I>>(
    base?: I,
  ): UnsubscribePlayerChannelResponse {
    return UnsubscribePlayerChannelResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UnsubscribePlayerChannelResponse>, I>>(
    object: I,
  ): UnsubscribePlayerChannelResponse {
    const message = createBaseUnsubscribePlayerChannelResponse();
    message.code = object.code ?? 0;
    return message;
  },
};

function createBaseGetPlayerSubscriptionsRequest(): GetPlayerSubscriptionsRequest {
  return { playerId: "", gameId: "" };
}

export const GetPlayerSubscriptionsRequest = {
  encode(message: GetPlayerSubscriptionsRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.playerId !== "") {
      writer.uint32(10).string(message.playerId);
    }
    if (message.gameId !== "") {
      writer.uint32(18).string(message.gameId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetPlayerSubscriptionsRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPlayerSubscriptionsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.playerId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.gameId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPlayerSubscriptionsRequest {
    return {
      playerId: isSet(object.playerId) ? globalThis.String(object.playerId) : "",
      gameId: isSet(object.gameId) ? globalThis.String(object.gameId) : "",
    };
  },

  toJSON(message: GetPlayerSubscriptionsRequest): unknown {
    const obj: any = {};
    if (message.playerId !== "") {
      obj.playerId = message.playerId;
    }
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPlayerSubscriptionsRequest>, I>>(base?: I): GetPlayerSubscriptionsRequest {
    return GetPlayerSubscriptionsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPlayerSubscriptionsRequest>, I>>(
    object: I,
  ): GetPlayerSubscriptionsRequest {
    const message = createBaseGetPlayerSubscriptionsRequest();
    message.playerId = object.playerId ?? "";
    message.gameId = object.gameId ?? "";
    return message;
  },
};

function createBaseGetPlayerSubscriptionsResponse(): GetPlayerSubscriptionsResponse {
  return { code: 0, subscriptions: [] };
}

export const GetPlayerSubscriptionsResponse = {
  encode(message: GetPlayerSubscriptionsResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    for (const v of message.subscriptions) {
      StoredChannelSubscription.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetPlayerSubscriptionsResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPlayerSubscriptionsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.subscriptions.push(StoredChannelSubscription.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPlayerSubscriptionsResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      subscriptions: globalThis.Array.isArray(object?.subscriptions)
        ? object.subscriptions.map((e: any) => StoredChannelSubscription.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetPlayerSubscriptionsResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.subscriptions?.length) {
      obj.subscriptions = message.subscriptions.map((e) => StoredChannelSubscription.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPlayerSubscriptionsResponse>, I>>(base?: I): GetPlayerSubscriptionsResponse {
    return GetPlayerSubscriptionsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPlayerSubscriptionsResponse>, I>>(
    object: I,
  ): GetPlayerSubscriptionsResponse {
    const message = createBaseGetPlayerSubscriptionsResponse();
    message.code = object.code ?? 0;
    message.subscriptions = object.subscriptions?.map((e) => StoredChannelSubscription.fromPartial(e)) || [];
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

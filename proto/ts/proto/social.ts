// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.33.4
// source: proto/social.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";
import { ErrorCode, errorCodeFromJSON, errorCodeToJSON } from "./common";

export const protobufPackage = "chirp.social";

/** User presence states */
export enum PresenceStatus {
  /** OFFLINE - User is offline */
  OFFLINE = 0,
  /** ONLINE - User is online */
  ONLINE = 1,
  /** AWAY - User is away */
  AWAY = 2,
  /** DND - Do not disturb */
  DND = 3,
  /** IN_GAME - In a game */
  IN_GAME = 4,
  /** IN_BATTLE - In a battle/match */
  IN_BATTLE = 5,
  UNRECOGNIZED = -1,
}

export function presenceStatusFromJSON(object: any): PresenceStatus {
  switch (object) {
    case 0:
    case "OFFLINE":
      return PresenceStatus.OFFLINE;
    case 1:
    case "ONLINE":
      return PresenceStatus.ONLINE;
    case 2:
    case "AWAY":
      return PresenceStatus.AWAY;
    case 3:
    case "DND":
      return PresenceStatus.DND;
    case 4:
    case "IN_GAME":
      return PresenceStatus.IN_GAME;
    case 5:
    case "IN_BATTLE":
      return PresenceStatus.IN_BATTLE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return PresenceStatus.UNRECOGNIZED;
  }
}

export function presenceStatusToJSON(object: PresenceStatus): string {
  switch (object) {
    case PresenceStatus.OFFLINE:
      return "OFFLINE";
    case PresenceStatus.ONLINE:
      return "ONLINE";
    case PresenceStatus.AWAY:
      return "AWAY";
    case PresenceStatus.DND:
      return "DND";
    case PresenceStatus.IN_GAME:
      return "IN_GAME";
    case PresenceStatus.IN_BATTLE:
      return "IN_BATTLE";
    case PresenceStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Friend relationship */
export enum FriendStatus {
  /** NONE - No relationship */
  NONE = 0,
  /** PENDING - Friend request pending */
  PENDING = 1,
  /** ACCEPTED - Friends */
  ACCEPTED = 2,
  /** BLOCKED - Blocked */
  BLOCKED = 3,
  UNRECOGNIZED = -1,
}

export function friendStatusFromJSON(object: any): FriendStatus {
  switch (object) {
    case 0:
    case "NONE":
      return FriendStatus.NONE;
    case 1:
    case "PENDING":
      return FriendStatus.PENDING;
    case 2:
    case "ACCEPTED":
      return FriendStatus.ACCEPTED;
    case 3:
    case "BLOCKED":
      return FriendStatus.BLOCKED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return FriendStatus.UNRECOGNIZED;
  }
}

export function friendStatusToJSON(object: FriendStatus): string {
  switch (object) {
    case FriendStatus.NONE:
      return "NONE";
    case FriendStatus.PENDING:
      return "PENDING";
    case FriendStatus.ACCEPTED:
      return "ACCEPTED";
    case FriendStatus.BLOCKED:
      return "BLOCKED";
    case FriendStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Friend request */
export interface FriendRequest {
  fromUserId: string;
  toUserId: string;
  /** Optional message with request */
  message: string;
  timestamp: number;
}

/** Friend information */
export interface FriendInfo {
  userId: string;
  username: string;
  avatarUrl: string;
  status: FriendStatus;
  addedAt: number;
}

/** Add friend request */
export interface AddFriendRequest {
  userId: string;
  targetUserId: string;
  message: string;
}

export interface AddFriendResponse {
  code: ErrorCode;
  requestId: string;
  serverTime: number;
}

/** Accept/decline friend request */
export interface FriendRequestAction {
  userId: string;
  requestId: string;
  /** true = accept, false = decline */
  accept: boolean;
}

export interface FriendRequestActionResponse {
  code: ErrorCode;
  serverTime: number;
}

/** Remove friend */
export interface RemoveFriendRequest {
  userId: string;
  friendUserId: string;
}

export interface RemoveFriendResponse {
  code: ErrorCode;
  serverTime: number;
}

/** Get friend list */
export interface GetFriendListRequest {
  userId: string;
  limit: number;
  offset: number;
}

export interface GetFriendListResponse {
  code: ErrorCode;
  friends: FriendInfo[];
  totalCount: number;
}

/** Get pending friend requests */
export interface GetPendingRequestsRequest {
  userId: string;
}

export interface GetPendingRequestsResponse {
  code: ErrorCode;
  requests: FriendRequest[];
}

/** Block user */
export interface BlockUserRequest {
  userId: string;
  targetUserId: string;
}

export interface BlockUserResponse {
  code: ErrorCode;
  serverTime: number;
}

/** Unblock user */
export interface UnblockUserRequest {
  userId: string;
  targetUserId: string;
}

export interface UnblockUserResponse {
  code: ErrorCode;
  serverTime: number;
}

/** Get blocked list */
export interface GetBlockedListRequest {
  userId: string;
}

export interface GetBlockedListResponse {
  code: ErrorCode;
  blockedUserIds: string[];
}

/** Set presence */
export interface SetPresenceRequest {
  userId: string;
  status: PresenceStatus;
  /** Optional custom message */
  statusMessage: string;
  /** Game info, location, etc. */
  metadata: { [key: string]: string };
}

export interface SetPresenceRequest_MetadataEntry {
  key: string;
  value: string;
}

export interface SetPresenceResponse {
  code: ErrorCode;
  serverTime: number;
}

/** Presence information */
export interface PresenceInfo {
  userId: string;
  status: PresenceStatus;
  statusMessage: string;
  lastSeen: number;
  metadata: { [key: string]: string };
}

export interface PresenceInfo_MetadataEntry {
  key: string;
  value: string;
}

/** Get presence of users */
export interface GetPresenceRequest {
  userIds: string[];
}

export interface GetPresenceResponse {
  code: ErrorCode;
  presences: PresenceInfo[];
}

/** Presence notification (broadcast to friends) */
export interface PresenceNotify {
  userId: string;
  status: PresenceStatus;
  statusMessage: string;
  timestamp: number;
  metadata: { [key: string]: string };
}

export interface PresenceNotify_MetadataEntry {
  key: string;
  value: string;
}

/** Friend request notification (sent to target user) */
export interface FriendRequestNotify {
  requestId: string;
  fromUserId: string;
  fromUsername: string;
  message: string;
  timestamp: number;
}

/** Friend request accepted notification */
export interface FriendAcceptedNotify {
  userId: string;
  username: string;
  timestamp: number;
}

/** Friend removed notification */
export interface FriendRemovedNotify {
  userId: string;
  timestamp: number;
}

/**
 * ---------------------------------------------------------------------------
 * Server-side storage snapshots. These are Redis VALUES (one key per user,
 * e.g. "chirp:social:friends:<uid>"), never wire messages. Keeping them
 * separate from the response messages lets the storage schema evolve without
 * touching the client-facing protocol: a friend is stored as just an id set
 * (the server holds no profiles), and pending requests keep both sides of
 * each request keyed by request_id (duplicated across the two users'
 * snapshots; the loader dedupes by request_id).
 */
export interface StoredFriendList {
  friendUserIds: string[];
}

export interface StoredPendingRequests {
  requests: { [key: string]: FriendRequest };
}

export interface StoredPendingRequests_RequestsEntry {
  key: string;
  value: FriendRequest | undefined;
}

export interface StoredBlockedList {
  blockedUserIds: string[];
}

function createBaseFriendRequest(): FriendRequest {
  return { fromUserId: "", toUserId: "", message: "", timestamp: 0 };
}

export const FriendRequest = {
  encode(message: FriendRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.fromUserId !== "") {
      writer.uint32(10).string(message.fromUserId);
    }
    if (message.toUserId !== "") {
      writer.uint32(18).string(message.toUserId);
    }
    if (message.message !== "") {
      writer.uint32(26).string(message.message);
    }
    if (message.timestamp !== 0) {
      writer.uint32(32).int64(message.timestamp);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): FriendRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFriendRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.fromUserId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.toUserId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.message = reader.string();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.timestamp = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FriendRequest {
    return {
      fromUserId: isSet(object.fromUserId) ? globalThis.String(object.fromUserId) : "",
      toUserId: isSet(object.toUserId) ? globalThis.String(object.toUserId) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
    };
  },

  toJSON(message: FriendRequest): unknown {
    const obj: any = {};
    if (message.fromUserId !== "") {
      obj.fromUserId = message.fromUserId;
    }
    if (message.toUserId !== "") {
      obj.toUserId = message.toUserId;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FriendRequest>, I>>(base?: I): FriendRequest {
    return FriendRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FriendRequest>, I>>(object: I): FriendRequest {
    const message = createBaseFriendRequest();
    message.fromUserId = object.fromUserId ?? "";
    message.toUserId = object.toUserId ?? "";
    message.message = object.message ?? "";
    message.timestamp = object.timestamp ?? 0;
    return message;
  },
};

function createBaseFriendInfo(): FriendInfo {
  return { userId: "", username: "", avatarUrl: "", status: 0, addedAt: 0 };
}

export const FriendInfo = {
  encode(message: FriendInfo, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.username !== "") {
      writer.uint32(18).string(message.username);
    }
    if (message.avatarUrl !== "") {
      writer.uint32(26).string(message.avatarUrl);
    }
    if (message.status !== 0) {
      writer.uint32(32).int32(message.status);
    }
    if (message.addedAt !== 0) {
      writer.uint32(40).int64(message.addedAt);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): FriendInfo {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFriendInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.username = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.avatarUrl = reader.string();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.addedAt = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FriendInfo {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      avatarUrl: isSet(object.avatarUrl) ? globalThis.String(object.avatarUrl) : "",
      status: isSet(object.status) ? friendStatusFromJSON(object.status) : 0,
      addedAt: isSet(object.addedAt) ? globalThis.Number(object.addedAt) : 0,
    };
  },

  toJSON(message: FriendInfo): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.avatarUrl !== "") {
      obj.avatarUrl = message.avatarUrl;
    }
    if (message.status !== 0) {
      obj.status = friendStatusToJSON(message.status);
    }
    if (message.addedAt !== 0) {
      obj.addedAt = Math.round(message.addedAt);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FriendInfo>, I>>(base?: I): FriendInfo {
    return FriendInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FriendInfo>, I>>(object: I): FriendInfo {
    const message = createBaseFriendInfo();
    message.userId = object.userId ?? "";
    message.username = object.username ?? "";
    message.avatarUrl = object.avatarUrl ?? "";
    message.status = object.status ?? 0;
    message.addedAt = object.addedAt ?? 0;
    return message;
  },
};

function createBaseAddFriendRequest(): AddFriendRequest {
  return { userId: "", targetUserId: "", message: "" };
}

export const AddFriendRequest = {
  encode(message: AddFriendRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.targetUserId !== "") {
      writer.uint32(18).string(message.targetUserId);
    }
    if (message.message !== "") {
      writer.uint32(26).string(message.message);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AddFriendRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAddFriendRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.targetUserId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.message = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AddFriendRequest {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      targetUserId: isSet(object.targetUserId) ? globalThis.String(object.targetUserId) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: AddFriendRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.targetUserId !== "") {
      obj.targetUserId = message.targetUserId;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AddFriendRequest>, I>>(base?: I): AddFriendRequest {
    return AddFriendRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AddFriendRequest>, I>>(object: I): AddFriendRequest {
    const message = createBaseAddFriendRequest();
    message.userId = object.userId ?? "";
    message.targetUserId = object.targetUserId ?? "";
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseAddFriendResponse(): AddFriendResponse {
  return { code: 0, requestId: "", serverTime: 0 };
}

export const AddFriendResponse = {
  encode(message: AddFriendResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.requestId !== "") {
      writer.uint32(18).string(message.requestId);
    }
    if (message.serverTime !== 0) {
      writer.uint32(24).int64(message.serverTime);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AddFriendResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAddFriendResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.requestId = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.serverTime = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AddFriendResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      requestId: isSet(object.requestId) ? globalThis.String(object.requestId) : "",
      serverTime: isSet(object.serverTime) ? globalThis.Number(object.serverTime) : 0,
    };
  },

  toJSON(message: AddFriendResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    if (message.serverTime !== 0) {
      obj.serverTime = Math.round(message.serverTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AddFriendResponse>, I>>(base?: I): AddFriendResponse {
    return AddFriendResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AddFriendResponse>, I>>(object: I): AddFriendResponse {
    const message = createBaseAddFriendResponse();
    message.code = object.code ?? 0;
    message.requestId = object.requestId ?? "";
    message.serverTime = object.serverTime ?? 0;
    return message;
  },
};

function createBaseFriendRequestAction(): FriendRequestAction {
  return { userId: "", requestId: "", accept: false };
}

export const FriendRequestAction = {
  encode(message: FriendRequestAction, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.requestId !== "") {
      writer.uint32(18).string(message.requestId);
    }
    if (message.accept !== false) {
      writer.uint32(24).bool(message.accept);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): FriendRequestAction {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFriendRequestAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.requestId = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.accept = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FriendRequestAction {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      requestId: isSet(object.requestId) ? globalThis.String(object.requestId) : "",
      accept: isSet(object.accept) ? globalThis.Boolean(object.accept) : false,
    };
  },

  toJSON(message: FriendRequestAction): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    if (message.accept !== false) {
      obj.accept = message.accept;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FriendRequestAction>, I>>(base?: I): FriendRequestAction {
    return FriendRequestAction.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FriendRequestAction>, I>>(object: I): FriendRequestAction {
    const message = createBaseFriendRequestAction();
    message.userId = object.userId ?? "";
    message.requestId = object.requestId ?? "";
    message.accept = object.accept ?? false;
    return message;
  },
};

function createBaseFriendRequestActionResponse(): FriendRequestActionResponse {
  return { code: 0, serverTime: 0 };
}

export const FriendRequestActionResponse = {
  encode(message: FriendRequestActionResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.serverTime !== 0) {
      writer.uint32(16).int64(message.serverTime);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): FriendRequestActionResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFriendRequestActionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.serverTime = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FriendRequestActionResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      serverTime: isSet(object.serverTime) ? globalThis.Number(object.serverTime) : 0,
    };
  },

  toJSON(message: FriendRequestActionResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.serverTime !== 0) {
      obj.serverTime = Math.round(message.serverTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FriendRequestActionResponse>, I>>(base?: I): FriendRequestActionResponse {
    return FriendRequestActionResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FriendRequestActionResponse>, I>>(object: I): FriendRequestActionResponse {
    const message = createBaseFriendRequestActionResponse();
    message.code = object.code ?? 0;
    message.serverTime = object.serverTime ?? 0;
    return message;
  },
};

function createBaseRemoveFriendRequest(): RemoveFriendRequest {
  return { userId: "", friendUserId: "" };
}

export const RemoveFriendRequest = {
  encode(message: RemoveFriendRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.friendUserId !== "") {
      writer.uint32(18).string(message.friendUserId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RemoveFriendRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRemoveFriendRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.friendUserId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RemoveFriendRequest {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      friendUserId: isSet(object.friendUserId) ? globalThis.String(object.friendUserId) : "",
    };
  },

  toJSON(message: RemoveFriendRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.friendUserId !== "") {
      obj.friendUserId = message.friendUserId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RemoveFriendRequest>, I>>(base?: I): RemoveFriendRequest {
    return RemoveFriendRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RemoveFriendRequest>, I>>(object: I): RemoveFriendRequest {
    const message = createBaseRemoveFriendRequest();
    message.userId = object.userId ?? "";
    message.friendUserId = object.friendUserId ?? "";
    return message;
  },
};

function createBaseRemoveFriendResponse(): RemoveFriendResponse {
  return { code: 0, serverTime: 0 };
}

export const RemoveFriendResponse = {
  encode(message: RemoveFriendResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.serverTime !== 0) {
      writer.uint32(16).int64(message.serverTime);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RemoveFriendResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRemoveFriendResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.serverTime = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RemoveFriendResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      serverTime: isSet(object.serverTime) ? globalThis.Number(object.serverTime) : 0,
    };
  },

  toJSON(message: RemoveFriendResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.serverTime !== 0) {
      obj.serverTime = Math.round(message.serverTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RemoveFriendResponse>, I>>(base?: I): RemoveFriendResponse {
    return RemoveFriendResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RemoveFriendResponse>, I>>(object: I): RemoveFriendResponse {
    const message = createBaseRemoveFriendResponse();
    message.code = object.code ?? 0;
    message.serverTime = object.serverTime ?? 0;
    return message;
  },
};

function createBaseGetFriendListRequest(): GetFriendListRequest {
  return { userId: "", limit: 0, offset: 0 };
}

export const GetFriendListRequest = {
  encode(message: GetFriendListRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.limit !== 0) {
      writer.uint32(16).int32(message.limit);
    }
    if (message.offset !== 0) {
      writer.uint32(24).int32(message.offset);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetFriendListRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetFriendListRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.limit = reader.int32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.offset = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetFriendListRequest {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
      offset: isSet(object.offset) ? globalThis.Number(object.offset) : 0,
    };
  },

  toJSON(message: GetFriendListRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetFriendListRequest>, I>>(base?: I): GetFriendListRequest {
    return GetFriendListRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetFriendListRequest>, I>>(object: I): GetFriendListRequest {
    const message = createBaseGetFriendListRequest();
    message.userId = object.userId ?? "";
    message.limit = object.limit ?? 0;
    message.offset = object.offset ?? 0;
    return message;
  },
};

function createBaseGetFriendListResponse(): GetFriendListResponse {
  return { code: 0, friends: [], totalCount: 0 };
}

export const GetFriendListResponse = {
  encode(message: GetFriendListResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    for (const v of message.friends) {
      FriendInfo.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    if (message.totalCount !== 0) {
      writer.uint32(24).int32(message.totalCount);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetFriendListResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetFriendListResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.friends.push(FriendInfo.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.totalCount = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetFriendListResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      friends: globalThis.Array.isArray(object?.friends) ? object.friends.map((e: any) => FriendInfo.fromJSON(e)) : [],
      totalCount: isSet(object.totalCount) ? globalThis.Number(object.totalCount) : 0,
    };
  },

  toJSON(message: GetFriendListResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.friends?.length) {
      obj.friends = message.friends.map((e) => FriendInfo.toJSON(e));
    }
    if (message.totalCount !== 0) {
      obj.totalCount = Math.round(message.totalCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetFriendListResponse>, I>>(base?: I): GetFriendListResponse {
    return GetFriendListResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetFriendListResponse>, I>>(object: I): GetFriendListResponse {
    const message = createBaseGetFriendListResponse();
    message.code = object.code ?? 0;
    message.friends = object.friends?.map((e) => FriendInfo.fromPartial(e)) || [];
    message.totalCount = object.totalCount ?? 0;
    return message;
  },
};

function createBaseGetPendingRequestsRequest(): GetPendingRequestsRequest {
  return { userId: "" };
}

export const GetPendingRequestsRequest = {
  encode(message: GetPendingRequestsRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetPendingRequestsRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPendingRequestsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPendingRequestsRequest {
    return { userId: isSet(object.userId) ? globalThis.String(object.userId) : "" };
  },

  toJSON(message: GetPendingRequestsRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPendingRequestsRequest>, I>>(base?: I): GetPendingRequestsRequest {
    return GetPendingRequestsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPendingRequestsRequest>, I>>(object: I): GetPendingRequestsRequest {
    const message = createBaseGetPendingRequestsRequest();
    message.userId = object.userId ?? "";
    return message;
  },
};

function createBaseGetPendingRequestsResponse(): GetPendingRequestsResponse {
  return { code: 0, requests: [] };
}

export const GetPendingRequestsResponse = {
  encode(message: GetPendingRequestsResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    for (const v of message.requests) {
      FriendRequest.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetPendingRequestsResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPendingRequestsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.requests.push(FriendRequest.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPendingRequestsResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      requests: globalThis.Array.isArray(object?.requests)
        ? object.requests.map((e: any) => FriendRequest.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetPendingRequestsResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.requests?.length) {
      obj.requests = message.requests.map((e) => FriendRequest.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPendingRequestsResponse>, I>>(base?: I): GetPendingRequestsResponse {
    return GetPendingRequestsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPendingRequestsResponse>, I>>(object: I): GetPendingRequestsResponse {
    const message = createBaseGetPendingRequestsResponse();
    message.code = object.code ?? 0;
    message.requests = object.requests?.map((e) => FriendRequest.fromPartial(e)) || [];
    return message;
  },
};

function createBaseBlockUserRequest(): BlockUserRequest {
  return { userId: "", targetUserId: "" };
}

export const BlockUserRequest = {
  encode(message: BlockUserRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.targetUserId !== "") {
      writer.uint32(18).string(message.targetUserId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): BlockUserRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBlockUserRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.targetUserId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BlockUserRequest {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      targetUserId: isSet(object.targetUserId) ? globalThis.String(object.targetUserId) : "",
    };
  },

  toJSON(message: BlockUserRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.targetUserId !== "") {
      obj.targetUserId = message.targetUserId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BlockUserRequest>, I>>(base?: I): BlockUserRequest {
    return BlockUserRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BlockUserRequest>, I>>(object: I): BlockUserRequest {
    const message = createBaseBlockUserRequest();
    message.userId = object.userId ?? "";
    message.targetUserId = object.targetUserId ?? "";
    return message;
  },
};

function createBaseBlockUserResponse(): BlockUserResponse {
  return { code: 0, serverTime: 0 };
}

export const BlockUserResponse = {
  encode(message: BlockUserResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.serverTime !== 0) {
      writer.uint32(16).int64(message.serverTime);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): BlockUserResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBlockUserResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.serverTime = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BlockUserResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      serverTime: isSet(object.serverTime) ? globalThis.Number(object.serverTime) : 0,
    };
  },

  toJSON(message: BlockUserResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.serverTime !== 0) {
      obj.serverTime = Math.round(message.serverTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BlockUserResponse>, I>>(base?: I): BlockUserResponse {
    return BlockUserResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BlockUserResponse>, I>>(object: I): BlockUserResponse {
    const message = createBaseBlockUserResponse();
    message.code = object.code ?? 0;
    message.serverTime = object.serverTime ?? 0;
    return message;
  },
};

function createBaseUnblockUserRequest(): UnblockUserRequest {
  return { userId: "", targetUserId: "" };
}

export const UnblockUserRequest = {
  encode(message: UnblockUserRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.targetUserId !== "") {
      writer.uint32(18).string(message.targetUserId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): UnblockUserRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnblockUserRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.targetUserId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UnblockUserRequest {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      targetUserId: isSet(object.targetUserId) ? globalThis.String(object.targetUserId) : "",
    };
  },

  toJSON(message: UnblockUserRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.targetUserId !== "") {
      obj.targetUserId = message.targetUserId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UnblockUserRequest>, I>>(base?: I): UnblockUserRequest {
    return UnblockUserRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UnblockUserRequest>, I>>(object: I): UnblockUserRequest {
    const message = createBaseUnblockUserRequest();
    message.userId = object.userId ?? "";
    message.targetUserId = object.targetUserId ?? "";
    return message;
  },
};

function createBaseUnblockUserResponse(): UnblockUserResponse {
  return { code: 0, serverTime: 0 };
}

export const UnblockUserResponse = {
  encode(message: UnblockUserResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.serverTime !== 0) {
      writer.uint32(16).int64(message.serverTime);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): UnblockUserResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnblockUserResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.serverTime = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UnblockUserResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      serverTime: isSet(object.serverTime) ? globalThis.Number(object.serverTime) : 0,
    };
  },

  toJSON(message: UnblockUserResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.serverTime !== 0) {
      obj.serverTime = Math.round(message.serverTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UnblockUserResponse>, I>>(base?: I): UnblockUserResponse {
    return UnblockUserResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UnblockUserResponse>, I>>(object: I): UnblockUserResponse {
    const message = createBaseUnblockUserResponse();
    message.code = object.code ?? 0;
    message.serverTime = object.serverTime ?? 0;
    return message;
  },
};

function createBaseGetBlockedListRequest(): GetBlockedListRequest {
  return { userId: "" };
}

export const GetBlockedListRequest = {
  encode(message: GetBlockedListRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetBlockedListRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetBlockedListRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetBlockedListRequest {
    return { userId: isSet(object.userId) ? globalThis.String(object.userId) : "" };
  },

  toJSON(message: GetBlockedListRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetBlockedListRequest>, I>>(base?: I): GetBlockedListRequest {
    return GetBlockedListRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetBlockedListRequest>, I>>(object: I): GetBlockedListRequest {
    const message = createBaseGetBlockedListRequest();
    message.userId = object.userId ?? "";
    return message;
  },
};

function createBaseGetBlockedListResponse(): GetBlockedListResponse {
  return { code: 0, blockedUserIds: [] };
}

export const GetBlockedListResponse = {
  encode(message: GetBlockedListResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    for (const v of message.blockedUserIds) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetBlockedListResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetBlockedListResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.blockedUserIds.push(reader.string());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetBlockedListResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      blockedUserIds: globalThis.Array.isArray(object?.blockedUserIds)
        ? object.blockedUserIds.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: GetBlockedListResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.blockedUserIds?.length) {
      obj.blockedUserIds = message.blockedUserIds;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetBlockedListResponse>, I>>(base?: I): GetBlockedListResponse {
    return GetBlockedListResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetBlockedListResponse>, I>>(object: I): GetBlockedListResponse {
    const message = createBaseGetBlockedListResponse();
    message.code = object.code ?? 0;
    message.blockedUserIds = object.blockedUserIds?.map((e) => e) || [];
    return message;
  },
};

function createBaseSetPresenceRequest(): SetPresenceRequest {
  return { userId: "", status: 0, statusMessage: "", metadata: {} };
}

export const SetPresenceRequest = {
  encode(message: SetPresenceRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    if (message.statusMessage !== "") {
      writer.uint32(26).string(message.statusMessage);
    }
    Object.entries(message.metadata).forEach(([key, value]) => {
      SetPresenceRequest_MetadataEntry.encode({ key: key as any, value }, writer.uint32(34).fork()).ldelim();
    });
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SetPresenceRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetPresenceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.statusMessage = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          const entry4 = SetPresenceRequest_MetadataEntry.decode(reader, reader.uint32());
          if (entry4.value !== undefined) {
            message.metadata[entry4.key] = entry4.value;
          }
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetPresenceRequest {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      status: isSet(object.status) ? presenceStatusFromJSON(object.status) : 0,
      statusMessage: isSet(object.statusMessage) ? globalThis.String(object.statusMessage) : "",
      metadata: isObject(object.metadata)
        ? Object.entries(object.metadata).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
    };
  },

  toJSON(message: SetPresenceRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.status !== 0) {
      obj.status = presenceStatusToJSON(message.status);
    }
    if (message.statusMessage !== "") {
      obj.statusMessage = message.statusMessage;
    }
    if (message.metadata) {
      const entries = Object.entries(message.metadata);
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetPresenceRequest>, I>>(base?: I): SetPresenceRequest {
    return SetPresenceRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetPresenceRequest>, I>>(object: I): SetPresenceRequest {
    const message = createBaseSetPresenceRequest();
    message.userId = object.userId ?? "";
    message.status = object.status ?? 0;
    message.statusMessage = object.statusMessage ?? "";
    message.metadata = Object.entries(object.metadata ?? {}).reduce<{ [key: string]: string }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = globalThis.String(value);
      }
      return acc;
    }, {});
    return message;
  },
};

function createBaseSetPresenceRequest_MetadataEntry(): SetPresenceRequest_MetadataEntry {
  return { key: "", value: "" };
}

export const SetPresenceRequest_MetadataEntry = {
  encode(message: SetPresenceRequest_MetadataEntry, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SetPresenceRequest_MetadataEntry {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetPresenceRequest_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetPresenceRequest_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: SetPresenceRequest_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetPresenceRequest_MetadataEntry>, I>>(
    base?: I,
  ): SetPresenceRequest_MetadataEntry {
    return SetPresenceRequest_MetadataEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetPresenceRequest_MetadataEntry>, I>>(
    object: I,
  ): SetPresenceRequest_MetadataEntry {
    const message = createBaseSetPresenceRequest_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseSetPresenceResponse(): SetPresenceResponse {
  return { code: 0, serverTime: 0 };
}

export const SetPresenceResponse = {
  encode(message: SetPresenceResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.serverTime !== 0) {
      writer.uint32(16).int64(message.serverTime);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SetPresenceResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetPresenceResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.serverTime = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetPresenceResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      serverTime: isSet(object.serverTime) ? globalThis.Number(object.serverTime) : 0,
    };
  },

  toJSON(message: SetPresenceResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.serverTime !== 0) {
      obj.serverTime = Math.round(message.serverTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetPresenceResponse>, I>>(base?: I): SetPresenceResponse {
    return SetPresenceResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetPresenceResponse>, I>>(object: I): SetPresenceResponse {
    const message = createBaseSetPresenceResponse();
    message.code = object.code ?? 0;
    message.serverTime = object.serverTime ?? 0;
    return message;
  },
};

function createBasePresenceInfo(): PresenceInfo {
  return { userId: "", status: 0, statusMessage: "", lastSeen: 0, metadata: {} };
}

export const PresenceInfo = {
  encode(message: PresenceInfo, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    if (message.statusMessage !== "") {
      writer.uint32(26).string(message.statusMessage);
    }
    if (message.lastSeen !== 0) {
      writer.uint32(32).int64(message.lastSeen);
    }
    Object.entries(message.metadata).forEach(([key, value]) => {
      PresenceInfo_MetadataEntry.encode({ key: key as any, value }, writer.uint32(42).fork()).ldelim();
    });
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PresenceInfo {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePresenceInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.statusMessage = reader.string();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.lastSeen = longToNumber(reader.int64() as Long);
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          const entry5 = PresenceInfo_MetadataEntry.decode(reader, reader.uint32());
          if (entry5.value !== undefined) {
            message.metadata[entry5.key] = entry5.value;
          }
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PresenceInfo {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      status: isSet(object.status) ? presenceStatusFromJSON(object.status) : 0,
      statusMessage: isSet(object.statusMessage) ? globalThis.String(object.statusMessage) : "",
      lastSeen: isSet(object.lastSeen) ? globalThis.Number(object.lastSeen) : 0,
      metadata: isObject(object.metadata)
        ? Object.entries(object.metadata).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
    };
  },

  toJSON(message: PresenceInfo): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.status !== 0) {
      obj.status = presenceStatusToJSON(message.status);
    }
    if (message.statusMessage !== "") {
      obj.statusMessage = message.statusMessage;
    }
    if (message.lastSeen !== 0) {
      obj.lastSeen = Math.round(message.lastSeen);
    }
    if (message.metadata) {
      const entries = Object.entries(message.metadata);
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PresenceInfo>, I>>(base?: I): PresenceInfo {
    return PresenceInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PresenceInfo>, I>>(object: I): PresenceInfo {
    const message = createBasePresenceInfo();
    message.userId = object.userId ?? "";
    message.status = object.status ?? 0;
    message.statusMessage = object.statusMessage ?? "";
    message.lastSeen = object.lastSeen ?? 0;
    message.metadata = Object.entries(object.metadata ?? {}).reduce<{ [key: string]: string }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = globalThis.String(value);
      }
      return acc;
    }, {});
    return message;
  },
};

function createBasePresenceInfo_MetadataEntry(): PresenceInfo_MetadataEntry {
  return { key: "", value: "" };
}

export const PresenceInfo_MetadataEntry = {
  encode(message: PresenceInfo_MetadataEntry, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PresenceInfo_MetadataEntry {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePresenceInfo_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PresenceInfo_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: PresenceInfo_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PresenceInfo_MetadataEntry>, I>>(base?: I): PresenceInfo_MetadataEntry {
    return PresenceInfo_MetadataEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PresenceInfo_MetadataEntry>, I>>(object: I): PresenceInfo_MetadataEntry {
    const message = createBasePresenceInfo_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseGetPresenceRequest(): GetPresenceRequest {
  return { userIds: [] };
}

export const GetPresenceRequest = {
  encode(message: GetPresenceRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.userIds) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetPresenceRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPresenceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userIds.push(reader.string());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPresenceRequest {
    return {
      userIds: globalThis.Array.isArray(object?.userIds) ? object.userIds.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: GetPresenceRequest): unknown {
    const obj: any = {};
    if (message.userIds?.length) {
      obj.userIds = message.userIds;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPresenceRequest>, I>>(base?: I): GetPresenceRequest {
    return GetPresenceRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPresenceRequest>, I>>(object: I): GetPresenceRequest {
    const message = createBaseGetPresenceRequest();
    message.userIds = object.userIds?.map((e) => e) || [];
    return message;
  },
};

function createBaseGetPresenceResponse(): GetPresenceResponse {
  return { code: 0, presences: [] };
}

export const GetPresenceResponse = {
  encode(message: GetPresenceResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    for (const v of message.presences) {
      PresenceInfo.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetPresenceResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPresenceResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.presences.push(PresenceInfo.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPresenceResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      presences: globalThis.Array.isArray(object?.presences)
        ? object.presences.map((e: any) => PresenceInfo.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetPresenceResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.presences?.length) {
      obj.presences = message.presences.map((e) => PresenceInfo.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPresenceResponse>, I>>(base?: I): GetPresenceResponse {
    return GetPresenceResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPresenceResponse>, I>>(object: I): GetPresenceResponse {
    const message = createBaseGetPresenceResponse();
    message.code = object.code ?? 0;
    message.presences = object.presences?.map((e) => PresenceInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBasePresenceNotify(): PresenceNotify {
  return { userId: "", status: 0, statusMessage: "", timestamp: 0, metadata: {} };
}

export const PresenceNotify = {
  encode(message: PresenceNotify, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    if (message.statusMessage !== "") {
      writer.uint32(26).string(message.statusMessage);
    }
    if (message.timestamp !== 0) {
      writer.uint32(32).int64(message.timestamp);
    }
    Object.entries(message.metadata).forEach(([key, value]) => {
      PresenceNotify_MetadataEntry.encode({ key: key as any, value }, writer.uint32(42).fork()).ldelim();
    });
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PresenceNotify {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePresenceNotify();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.statusMessage = reader.string();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.timestamp = longToNumber(reader.int64() as Long);
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          const entry5 = PresenceNotify_MetadataEntry.decode(reader, reader.uint32());
          if (entry5.value !== undefined) {
            message.metadata[entry5.key] = entry5.value;
          }
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PresenceNotify {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      status: isSet(object.status) ? presenceStatusFromJSON(object.status) : 0,
      statusMessage: isSet(object.statusMessage) ? globalThis.String(object.statusMessage) : "",
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
      metadata: isObject(object.metadata)
        ? Object.entries(object.metadata).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
    };
  },

  toJSON(message: PresenceNotify): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.status !== 0) {
      obj.status = presenceStatusToJSON(message.status);
    }
    if (message.statusMessage !== "") {
      obj.statusMessage = message.statusMessage;
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    if (message.metadata) {
      const entries = Object.entries(message.metadata);
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PresenceNotify>, I>>(base?: I): PresenceNotify {
    return PresenceNotify.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PresenceNotify>, I>>(object: I): PresenceNotify {
    const message = createBasePresenceNotify();
    message.userId = object.userId ?? "";
    message.status = object.status ?? 0;
    message.statusMessage = object.statusMessage ?? "";
    message.timestamp = object.timestamp ?? 0;
    message.metadata = Object.entries(object.metadata ?? {}).reduce<{ [key: string]: string }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = globalThis.String(value);
      }
      return acc;
    }, {});
    return message;
  },
};

function createBasePresenceNotify_MetadataEntry(): PresenceNotify_MetadataEntry {
  return { key: "", value: "" };
}

export const PresenceNotify_MetadataEntry = {
  encode(message: PresenceNotify_MetadataEntry, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PresenceNotify_MetadataEntry {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePresenceNotify_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PresenceNotify_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: PresenceNotify_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PresenceNotify_MetadataEntry>, I>>(base?: I): PresenceNotify_MetadataEntry {
    return PresenceNotify_MetadataEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PresenceNotify_MetadataEntry>, I>>(object: I): PresenceNotify_MetadataEntry {
    const message = createBasePresenceNotify_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseFriendRequestNotify(): FriendRequestNotify {
  return { requestId: "", fromUserId: "", fromUsername: "", message: "", timestamp: 0 };
}

export const FriendRequestNotify = {
  encode(message: FriendRequestNotify, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.requestId !== "") {
      writer.uint32(10).string(message.requestId);
    }
    if (message.fromUserId !== "") {
      writer.uint32(18).string(message.fromUserId);
    }
    if (message.fromUsername !== "") {
      writer.uint32(26).string(message.fromUsername);
    }
    if (message.message !== "") {
      writer.uint32(34).string(message.message);
    }
    if (message.timestamp !== 0) {
      writer.uint32(40).int64(message.timestamp);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): FriendRequestNotify {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFriendRequestNotify();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.requestId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.fromUserId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.fromUsername = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.message = reader.string();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.timestamp = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FriendRequestNotify {
    return {
      requestId: isSet(object.requestId) ? globalThis.String(object.requestId) : "",
      fromUserId: isSet(object.fromUserId) ? globalThis.String(object.fromUserId) : "",
      fromUsername: isSet(object.fromUsername) ? globalThis.String(object.fromUsername) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
    };
  },

  toJSON(message: FriendRequestNotify): unknown {
    const obj: any = {};
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    if (message.fromUserId !== "") {
      obj.fromUserId = message.fromUserId;
    }
    if (message.fromUsername !== "") {
      obj.fromUsername = message.fromUsername;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FriendRequestNotify>, I>>(base?: I): FriendRequestNotify {
    return FriendRequestNotify.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FriendRequestNotify>, I>>(object: I): FriendRequestNotify {
    const message = createBaseFriendRequestNotify();
    message.requestId = object.requestId ?? "";
    message.fromUserId = object.fromUserId ?? "";
    message.fromUsername = object.fromUsername ?? "";
    message.message = object.message ?? "";
    message.timestamp = object.timestamp ?? 0;
    return message;
  },
};

function createBaseFriendAcceptedNotify(): FriendAcceptedNotify {
  return { userId: "", username: "", timestamp: 0 };
}

export const FriendAcceptedNotify = {
  encode(message: FriendAcceptedNotify, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.username !== "") {
      writer.uint32(18).string(message.username);
    }
    if (message.timestamp !== 0) {
      writer.uint32(24).int64(message.timestamp);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): FriendAcceptedNotify {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFriendAcceptedNotify();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.username = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.timestamp = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FriendAcceptedNotify {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
    };
  },

  toJSON(message: FriendAcceptedNotify): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FriendAcceptedNotify>, I>>(base?: I): FriendAcceptedNotify {
    return FriendAcceptedNotify.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FriendAcceptedNotify>, I>>(object: I): FriendAcceptedNotify {
    const message = createBaseFriendAcceptedNotify();
    message.userId = object.userId ?? "";
    message.username = object.username ?? "";
    message.timestamp = object.timestamp ?? 0;
    return message;
  },
};

function createBaseFriendRemovedNotify(): FriendRemovedNotify {
  return { userId: "", timestamp: 0 };
}

export const FriendRemovedNotify = {
  encode(message: FriendRemovedNotify, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.timestamp !== 0) {
      writer.uint32(16).int64(message.timestamp);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): FriendRemovedNotify {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFriendRemovedNotify();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.timestamp = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FriendRemovedNotify {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
    };
  },

  toJSON(message: FriendRemovedNotify): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FriendRemovedNotify>, I>>(base?: I): FriendRemovedNotify {
    return FriendRemovedNotify.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FriendRemovedNotify>, I>>(object: I): FriendRemovedNotify {
    const message = createBaseFriendRemovedNotify();
    message.userId = object.userId ?? "";
    message.timestamp = object.timestamp ?? 0;
    return message;
  },
};

function createBaseStoredFriendList(): StoredFriendList {
  return { friendUserIds: [] };
}

export const StoredFriendList = {
  encode(message: StoredFriendList, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.friendUserIds) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): StoredFriendList {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStoredFriendList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.friendUserIds.push(reader.string());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StoredFriendList {
    return {
      friendUserIds: globalThis.Array.isArray(object?.friendUserIds)
        ? object.friendUserIds.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: StoredFriendList): unknown {
    const obj: any = {};
    if (message.friendUserIds?.length) {
      obj.friendUserIds = message.friendUserIds;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StoredFriendList>, I>>(base?: I): StoredFriendList {
    return StoredFriendList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StoredFriendList>, I>>(object: I): StoredFriendList {
    const message = createBaseStoredFriendList();
    message.friendUserIds = object.friendUserIds?.map((e) => e) || [];
    return message;
  },
};

function createBaseStoredPendingRequests(): StoredPendingRequests {
  return { requests: {} };
}

export const StoredPendingRequests = {
  encode(message: StoredPendingRequests, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    Object.entries(message.requests).forEach(([key, value]) => {
      StoredPendingRequests_RequestsEntry.encode({ key: key as any, value }, writer.uint32(10).fork()).ldelim();
    });
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): StoredPendingRequests {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStoredPendingRequests();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          const entry1 = StoredPendingRequests_RequestsEntry.decode(reader, reader.uint32());
          if (entry1.value !== undefined) {
            message.requests[entry1.key] = entry1.value;
          }
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StoredPendingRequests {
    return {
      requests: isObject(object.requests)
        ? Object.entries(object.requests).reduce<{ [key: string]: FriendRequest }>((acc, [key, value]) => {
          acc[key] = FriendRequest.fromJSON(value);
          return acc;
        }, {})
        : {},
    };
  },

  toJSON(message: StoredPendingRequests): unknown {
    const obj: any = {};
    if (message.requests) {
      const entries = Object.entries(message.requests);
      if (entries.length > 0) {
        obj.requests = {};
        entries.forEach(([k, v]) => {
          obj.requests[k] = FriendRequest.toJSON(v);
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StoredPendingRequests>, I>>(base?: I): StoredPendingRequests {
    return StoredPendingRequests.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StoredPendingRequests>, I>>(object: I): StoredPendingRequests {
    const message = createBaseStoredPendingRequests();
    message.requests = Object.entries(object.requests ?? {}).reduce<{ [key: string]: FriendRequest }>(
      (acc, [key, value]) => {
        if (value !== undefined) {
          acc[key] = FriendRequest.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseStoredPendingRequests_RequestsEntry(): StoredPendingRequests_RequestsEntry {
  return { key: "", value: undefined };
}

export const StoredPendingRequests_RequestsEntry = {
  encode(message: StoredPendingRequests_RequestsEntry, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      FriendRequest.encode(message.value, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): StoredPendingRequests_RequestsEntry {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStoredPendingRequests_RequestsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.value = FriendRequest.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StoredPendingRequests_RequestsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? FriendRequest.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: StoredPendingRequests_RequestsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = FriendRequest.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StoredPendingRequests_RequestsEntry>, I>>(
    base?: I,
  ): StoredPendingRequests_RequestsEntry {
    return StoredPendingRequests_RequestsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StoredPendingRequests_RequestsEntry>, I>>(
    object: I,
  ): StoredPendingRequests_RequestsEntry {
    const message = createBaseStoredPendingRequests_RequestsEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? FriendRequest.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBaseStoredBlockedList(): StoredBlockedList {
  return { blockedUserIds: [] };
}

export const StoredBlockedList = {
  encode(message: StoredBlockedList, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.blockedUserIds) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): StoredBlockedList {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStoredBlockedList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.blockedUserIds.push(reader.string());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StoredBlockedList {
    return {
      blockedUserIds: globalThis.Array.isArray(object?.blockedUserIds)
        ? object.blockedUserIds.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: StoredBlockedList): unknown {
    const obj: any = {};
    if (message.blockedUserIds?.length) {
      obj.blockedUserIds = message.blockedUserIds;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StoredBlockedList>, I>>(base?: I): StoredBlockedList {
    return StoredBlockedList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StoredBlockedList>, I>>(object: I): StoredBlockedList {
    const message = createBaseStoredBlockedList();
    message.blockedUserIds = object.blockedUserIds?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

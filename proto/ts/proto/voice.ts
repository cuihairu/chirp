// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.33.4
// source: proto/voice.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";
import { ErrorCode, errorCodeFromJSON, errorCodeToJSON } from "./common";

export const protobufPackage = "chirp.voice";

/** Voice room types */
export enum RoomType {
  /** PEER_TO_PEER - 1:1 voice call */
  PEER_TO_PEER = 0,
  /** GROUP - Group voice chat */
  GROUP = 1,
  /** CHANNEL - Persistent voice channel (like Discord) */
  CHANNEL = 2,
  UNRECOGNIZED = -1,
}

export function roomTypeFromJSON(object: any): RoomType {
  switch (object) {
    case 0:
    case "PEER_TO_PEER":
      return RoomType.PEER_TO_PEER;
    case 1:
    case "GROUP":
      return RoomType.GROUP;
    case 2:
    case "CHANNEL":
      return RoomType.CHANNEL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return RoomType.UNRECOGNIZED;
  }
}

export function roomTypeToJSON(object: RoomType): string {
  switch (object) {
    case RoomType.PEER_TO_PEER:
      return "PEER_TO_PEER";
    case RoomType.GROUP:
      return "GROUP";
    case RoomType.CHANNEL:
      return "CHANNEL";
    case RoomType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Participant state in voice room */
export enum ParticipantState {
  /** JOINING - Connecting to room */
  JOINING = 0,
  /** CONNECTED - Connected and can send/receive audio */
  CONNECTED = 1,
  /** MUTED - Connected but mic muted */
  MUTED = 2,
  /** DEAFENED - Connected but speaker muted */
  DEAFENED = 3,
  /** DISCONNECTED - Left the room */
  DISCONNECTED = 4,
  UNRECOGNIZED = -1,
}

export function participantStateFromJSON(object: any): ParticipantState {
  switch (object) {
    case 0:
    case "JOINING":
      return ParticipantState.JOINING;
    case 1:
    case "CONNECTED":
      return ParticipantState.CONNECTED;
    case 2:
    case "MUTED":
      return ParticipantState.MUTED;
    case 3:
    case "DEAFENED":
      return ParticipantState.DEAFENED;
    case 4:
    case "DISCONNECTED":
      return ParticipantState.DISCONNECTED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ParticipantState.UNRECOGNIZED;
  }
}

export function participantStateToJSON(object: ParticipantState): string {
  switch (object) {
    case ParticipantState.JOINING:
      return "JOINING";
    case ParticipantState.CONNECTED:
      return "CONNECTED";
    case ParticipantState.MUTED:
      return "MUTED";
    case ParticipantState.DEAFENED:
      return "DEAFENED";
    case ParticipantState.DISCONNECTED:
      return "DISCONNECTED";
    case ParticipantState.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Create voice room */
export interface CreateRoomRequest {
  userId: string;
  roomType: RoomType;
  roomName: string;
  /** 0 for unlimited */
  maxParticipants: number;
  metadata: { [key: string]: string };
}

export interface CreateRoomRequest_MetadataEntry {
  key: string;
  value: string;
}

export interface CreateRoomResponse {
  code: ErrorCode;
  roomId: string;
  serverTime: number;
}

/**
 * TURN/STUN access for WebRTC gathering, handed out with the join response
 * (coturn REST short-term credentials; empty username/credential = STUN only).
 */
export interface IceServer {
  /** e.g. "turn:host:3478?transport=udp" */
  urls: string[];
  /** "{unix_expiry}:{user_id}" */
  username: string;
  /** base64(HMAC-SHA1(secret, username)) */
  credential: string;
}

/** Join voice room */
export interface JoinRoomRequest {
  userId: string;
  roomId: string;
  /** WebRTC SDP offer */
  sdpOffer: string;
}

export interface JoinRoomResponse {
  code: ErrorCode;
  roomId: string;
  /** WebRTC SDP answer from server/peer */
  sdpAnswer: string;
  participantIds: string[];
  serverTime: number;
  iceServers: IceServer[];
}

/** Leave voice room */
export interface LeaveRoomRequest {
  userId: string;
  roomId: string;
}

export interface LeaveRoomResponse {
  code: ErrorCode;
  serverTime: number;
}

/** WebRTC signaling messages */
export interface IceCandidate {
  candidate: string;
  sdpMid: string;
  sdpMlineIndex: number;
}

export interface IceCandidateMessage {
  roomId: string;
  fromUserId: string;
  /** Empty for broadcast to all */
  toUserId: string;
  candidate: IceCandidate | undefined;
}

export interface SdpOfferMessage {
  roomId: string;
  fromUserId: string;
  toUserId: string;
  sdpOffer: string;
}

export interface SdpAnswerMessage {
  roomId: string;
  fromUserId: string;
  toUserId: string;
  sdpAnswer: string;
}

/** Room participant info */
export interface ParticipantInfo {
  userId: string;
  username: string;
  state: ParticipantState;
  joinedAt: number;
  isSpeaking: boolean;
  /**
   * Independent mute flags; `state` is derived from them
   * (DEAFENED > MUTED > CONNECTED) so unmuting the mic while deafened
   * keeps the participant DEAFENED.
   */
  muted: boolean;
  deafened: boolean;
}

/** Get room info */
export interface GetRoomInfoRequest {
  roomId: string;
}

export interface GetRoomInfoResponse {
  code: ErrorCode;
  roomId: string;
  roomName: string;
  roomType: RoomType;
  participants: ParticipantInfo[];
  maxParticipants: number;
}

/** Get user's current room */
export interface GetUserRoomRequest {
  userId: string;
}

export interface GetUserRoomResponse {
  code: ErrorCode;
  roomId: string;
  participant: ParticipantInfo | undefined;
}

/** Mute/unmute microphone */
export interface SetMuteRequest {
  userId: string;
  roomId: string;
  muted: boolean;
}

export interface SetMuteResponse {
  code: ErrorCode;
  serverTime: number;
}

/** Deafen/undeafen speaker */
export interface SetDeafenRequest {
  userId: string;
  roomId: string;
  deafened: boolean;
}

export interface SetDeafenResponse {
  code: ErrorCode;
  serverTime: number;
}

/** Notifications */
export interface ParticipantJoinedNotify {
  roomId: string;
  participant: ParticipantInfo | undefined;
  timestamp: number;
}

export interface ParticipantLeftNotify {
  roomId: string;
  userId: string;
  timestamp: number;
}

export interface ParticipantStateChangedNotify {
  roomId: string;
  userId: string;
  state: ParticipantState;
  timestamp: number;
}

export interface SpeakingNotify {
  roomId: string;
  userId: string;
  speaking: boolean;
  timestamp: number;
}

function createBaseCreateRoomRequest(): CreateRoomRequest {
  return { userId: "", roomType: 0, roomName: "", maxParticipants: 0, metadata: {} };
}

export const CreateRoomRequest = {
  encode(message: CreateRoomRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.roomType !== 0) {
      writer.uint32(16).int32(message.roomType);
    }
    if (message.roomName !== "") {
      writer.uint32(26).string(message.roomName);
    }
    if (message.maxParticipants !== 0) {
      writer.uint32(32).int32(message.maxParticipants);
    }
    Object.entries(message.metadata).forEach(([key, value]) => {
      CreateRoomRequest_MetadataEntry.encode({ key: key as any, value }, writer.uint32(42).fork()).ldelim();
    });
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CreateRoomRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateRoomRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.roomType = reader.int32() as any;
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.roomName = reader.string();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.maxParticipants = reader.int32();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          const entry5 = CreateRoomRequest_MetadataEntry.decode(reader, reader.uint32());
          if (entry5.value !== undefined) {
            message.metadata[entry5.key] = entry5.value;
          }
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateRoomRequest {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      roomType: isSet(object.roomType) ? roomTypeFromJSON(object.roomType) : 0,
      roomName: isSet(object.roomName) ? globalThis.String(object.roomName) : "",
      maxParticipants: isSet(object.maxParticipants) ? globalThis.Number(object.maxParticipants) : 0,
      metadata: isObject(object.metadata)
        ? Object.entries(object.metadata).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
    };
  },

  toJSON(message: CreateRoomRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.roomType !== 0) {
      obj.roomType = roomTypeToJSON(message.roomType);
    }
    if (message.roomName !== "") {
      obj.roomName = message.roomName;
    }
    if (message.maxParticipants !== 0) {
      obj.maxParticipants = Math.round(message.maxParticipants);
    }
    if (message.metadata) {
      const entries = Object.entries(message.metadata);
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateRoomRequest>, I>>(base?: I): CreateRoomRequest {
    return CreateRoomRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateRoomRequest>, I>>(object: I): CreateRoomRequest {
    const message = createBaseCreateRoomRequest();
    message.userId = object.userId ?? "";
    message.roomType = object.roomType ?? 0;
    message.roomName = object.roomName ?? "";
    message.maxParticipants = object.maxParticipants ?? 0;
    message.metadata = Object.entries(object.metadata ?? {}).reduce<{ [key: string]: string }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = globalThis.String(value);
      }
      return acc;
    }, {});
    return message;
  },
};

function createBaseCreateRoomRequest_MetadataEntry(): CreateRoomRequest_MetadataEntry {
  return { key: "", value: "" };
}

export const CreateRoomRequest_MetadataEntry = {
  encode(message: CreateRoomRequest_MetadataEntry, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CreateRoomRequest_MetadataEntry {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateRoomRequest_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateRoomRequest_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: CreateRoomRequest_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateRoomRequest_MetadataEntry>, I>>(base?: I): CreateRoomRequest_MetadataEntry {
    return CreateRoomRequest_MetadataEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateRoomRequest_MetadataEntry>, I>>(
    object: I,
  ): CreateRoomRequest_MetadataEntry {
    const message = createBaseCreateRoomRequest_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseCreateRoomResponse(): CreateRoomResponse {
  return { code: 0, roomId: "", serverTime: 0 };
}

export const CreateRoomResponse = {
  encode(message: CreateRoomResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.roomId !== "") {
      writer.uint32(18).string(message.roomId);
    }
    if (message.serverTime !== 0) {
      writer.uint32(24).int64(message.serverTime);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CreateRoomResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateRoomResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.roomId = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.serverTime = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateRoomResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      roomId: isSet(object.roomId) ? globalThis.String(object.roomId) : "",
      serverTime: isSet(object.serverTime) ? globalThis.Number(object.serverTime) : 0,
    };
  },

  toJSON(message: CreateRoomResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.serverTime !== 0) {
      obj.serverTime = Math.round(message.serverTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateRoomResponse>, I>>(base?: I): CreateRoomResponse {
    return CreateRoomResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateRoomResponse>, I>>(object: I): CreateRoomResponse {
    const message = createBaseCreateRoomResponse();
    message.code = object.code ?? 0;
    message.roomId = object.roomId ?? "";
    message.serverTime = object.serverTime ?? 0;
    return message;
  },
};

function createBaseIceServer(): IceServer {
  return { urls: [], username: "", credential: "" };
}

export const IceServer = {
  encode(message: IceServer, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.urls) {
      writer.uint32(10).string(v!);
    }
    if (message.username !== "") {
      writer.uint32(18).string(message.username);
    }
    if (message.credential !== "") {
      writer.uint32(26).string(message.credential);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): IceServer {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIceServer();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.urls.push(reader.string());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.username = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.credential = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IceServer {
    return {
      urls: globalThis.Array.isArray(object?.urls) ? object.urls.map((e: any) => globalThis.String(e)) : [],
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      credential: isSet(object.credential) ? globalThis.String(object.credential) : "",
    };
  },

  toJSON(message: IceServer): unknown {
    const obj: any = {};
    if (message.urls?.length) {
      obj.urls = message.urls;
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.credential !== "") {
      obj.credential = message.credential;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IceServer>, I>>(base?: I): IceServer {
    return IceServer.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IceServer>, I>>(object: I): IceServer {
    const message = createBaseIceServer();
    message.urls = object.urls?.map((e) => e) || [];
    message.username = object.username ?? "";
    message.credential = object.credential ?? "";
    return message;
  },
};

function createBaseJoinRoomRequest(): JoinRoomRequest {
  return { userId: "", roomId: "", sdpOffer: "" };
}

export const JoinRoomRequest = {
  encode(message: JoinRoomRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.roomId !== "") {
      writer.uint32(18).string(message.roomId);
    }
    if (message.sdpOffer !== "") {
      writer.uint32(26).string(message.sdpOffer);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): JoinRoomRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJoinRoomRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.roomId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.sdpOffer = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JoinRoomRequest {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      roomId: isSet(object.roomId) ? globalThis.String(object.roomId) : "",
      sdpOffer: isSet(object.sdpOffer) ? globalThis.String(object.sdpOffer) : "",
    };
  },

  toJSON(message: JoinRoomRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.sdpOffer !== "") {
      obj.sdpOffer = message.sdpOffer;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JoinRoomRequest>, I>>(base?: I): JoinRoomRequest {
    return JoinRoomRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JoinRoomRequest>, I>>(object: I): JoinRoomRequest {
    const message = createBaseJoinRoomRequest();
    message.userId = object.userId ?? "";
    message.roomId = object.roomId ?? "";
    message.sdpOffer = object.sdpOffer ?? "";
    return message;
  },
};

function createBaseJoinRoomResponse(): JoinRoomResponse {
  return { code: 0, roomId: "", sdpAnswer: "", participantIds: [], serverTime: 0, iceServers: [] };
}

export const JoinRoomResponse = {
  encode(message: JoinRoomResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.roomId !== "") {
      writer.uint32(18).string(message.roomId);
    }
    if (message.sdpAnswer !== "") {
      writer.uint32(26).string(message.sdpAnswer);
    }
    for (const v of message.participantIds) {
      writer.uint32(34).string(v!);
    }
    if (message.serverTime !== 0) {
      writer.uint32(40).int64(message.serverTime);
    }
    for (const v of message.iceServers) {
      IceServer.encode(v!, writer.uint32(50).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): JoinRoomResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJoinRoomResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.roomId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.sdpAnswer = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.participantIds.push(reader.string());
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.serverTime = longToNumber(reader.int64() as Long);
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.iceServers.push(IceServer.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JoinRoomResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      roomId: isSet(object.roomId) ? globalThis.String(object.roomId) : "",
      sdpAnswer: isSet(object.sdpAnswer) ? globalThis.String(object.sdpAnswer) : "",
      participantIds: globalThis.Array.isArray(object?.participantIds)
        ? object.participantIds.map((e: any) => globalThis.String(e))
        : [],
      serverTime: isSet(object.serverTime) ? globalThis.Number(object.serverTime) : 0,
      iceServers: globalThis.Array.isArray(object?.iceServers)
        ? object.iceServers.map((e: any) => IceServer.fromJSON(e))
        : [],
    };
  },

  toJSON(message: JoinRoomResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.sdpAnswer !== "") {
      obj.sdpAnswer = message.sdpAnswer;
    }
    if (message.participantIds?.length) {
      obj.participantIds = message.participantIds;
    }
    if (message.serverTime !== 0) {
      obj.serverTime = Math.round(message.serverTime);
    }
    if (message.iceServers?.length) {
      obj.iceServers = message.iceServers.map((e) => IceServer.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JoinRoomResponse>, I>>(base?: I): JoinRoomResponse {
    return JoinRoomResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JoinRoomResponse>, I>>(object: I): JoinRoomResponse {
    const message = createBaseJoinRoomResponse();
    message.code = object.code ?? 0;
    message.roomId = object.roomId ?? "";
    message.sdpAnswer = object.sdpAnswer ?? "";
    message.participantIds = object.participantIds?.map((e) => e) || [];
    message.serverTime = object.serverTime ?? 0;
    message.iceServers = object.iceServers?.map((e) => IceServer.fromPartial(e)) || [];
    return message;
  },
};

function createBaseLeaveRoomRequest(): LeaveRoomRequest {
  return { userId: "", roomId: "" };
}

export const LeaveRoomRequest = {
  encode(message: LeaveRoomRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.roomId !== "") {
      writer.uint32(18).string(message.roomId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): LeaveRoomRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLeaveRoomRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.roomId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LeaveRoomRequest {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      roomId: isSet(object.roomId) ? globalThis.String(object.roomId) : "",
    };
  },

  toJSON(message: LeaveRoomRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LeaveRoomRequest>, I>>(base?: I): LeaveRoomRequest {
    return LeaveRoomRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LeaveRoomRequest>, I>>(object: I): LeaveRoomRequest {
    const message = createBaseLeaveRoomRequest();
    message.userId = object.userId ?? "";
    message.roomId = object.roomId ?? "";
    return message;
  },
};

function createBaseLeaveRoomResponse(): LeaveRoomResponse {
  return { code: 0, serverTime: 0 };
}

export const LeaveRoomResponse = {
  encode(message: LeaveRoomResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.serverTime !== 0) {
      writer.uint32(16).int64(message.serverTime);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): LeaveRoomResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLeaveRoomResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.serverTime = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LeaveRoomResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      serverTime: isSet(object.serverTime) ? globalThis.Number(object.serverTime) : 0,
    };
  },

  toJSON(message: LeaveRoomResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.serverTime !== 0) {
      obj.serverTime = Math.round(message.serverTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LeaveRoomResponse>, I>>(base?: I): LeaveRoomResponse {
    return LeaveRoomResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LeaveRoomResponse>, I>>(object: I): LeaveRoomResponse {
    const message = createBaseLeaveRoomResponse();
    message.code = object.code ?? 0;
    message.serverTime = object.serverTime ?? 0;
    return message;
  },
};

function createBaseIceCandidate(): IceCandidate {
  return { candidate: "", sdpMid: "", sdpMlineIndex: 0 };
}

export const IceCandidate = {
  encode(message: IceCandidate, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.candidate !== "") {
      writer.uint32(10).string(message.candidate);
    }
    if (message.sdpMid !== "") {
      writer.uint32(18).string(message.sdpMid);
    }
    if (message.sdpMlineIndex !== 0) {
      writer.uint32(24).int32(message.sdpMlineIndex);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): IceCandidate {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIceCandidate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.candidate = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.sdpMid = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.sdpMlineIndex = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IceCandidate {
    return {
      candidate: isSet(object.candidate) ? globalThis.String(object.candidate) : "",
      sdpMid: isSet(object.sdpMid) ? globalThis.String(object.sdpMid) : "",
      sdpMlineIndex: isSet(object.sdpMlineIndex) ? globalThis.Number(object.sdpMlineIndex) : 0,
    };
  },

  toJSON(message: IceCandidate): unknown {
    const obj: any = {};
    if (message.candidate !== "") {
      obj.candidate = message.candidate;
    }
    if (message.sdpMid !== "") {
      obj.sdpMid = message.sdpMid;
    }
    if (message.sdpMlineIndex !== 0) {
      obj.sdpMlineIndex = Math.round(message.sdpMlineIndex);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IceCandidate>, I>>(base?: I): IceCandidate {
    return IceCandidate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IceCandidate>, I>>(object: I): IceCandidate {
    const message = createBaseIceCandidate();
    message.candidate = object.candidate ?? "";
    message.sdpMid = object.sdpMid ?? "";
    message.sdpMlineIndex = object.sdpMlineIndex ?? 0;
    return message;
  },
};

function createBaseIceCandidateMessage(): IceCandidateMessage {
  return { roomId: "", fromUserId: "", toUserId: "", candidate: undefined };
}

export const IceCandidateMessage = {
  encode(message: IceCandidateMessage, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.roomId !== "") {
      writer.uint32(10).string(message.roomId);
    }
    if (message.fromUserId !== "") {
      writer.uint32(18).string(message.fromUserId);
    }
    if (message.toUserId !== "") {
      writer.uint32(26).string(message.toUserId);
    }
    if (message.candidate !== undefined) {
      IceCandidate.encode(message.candidate, writer.uint32(34).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): IceCandidateMessage {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIceCandidateMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.roomId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.fromUserId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.toUserId = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.candidate = IceCandidate.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IceCandidateMessage {
    return {
      roomId: isSet(object.roomId) ? globalThis.String(object.roomId) : "",
      fromUserId: isSet(object.fromUserId) ? globalThis.String(object.fromUserId) : "",
      toUserId: isSet(object.toUserId) ? globalThis.String(object.toUserId) : "",
      candidate: isSet(object.candidate) ? IceCandidate.fromJSON(object.candidate) : undefined,
    };
  },

  toJSON(message: IceCandidateMessage): unknown {
    const obj: any = {};
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.fromUserId !== "") {
      obj.fromUserId = message.fromUserId;
    }
    if (message.toUserId !== "") {
      obj.toUserId = message.toUserId;
    }
    if (message.candidate !== undefined) {
      obj.candidate = IceCandidate.toJSON(message.candidate);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IceCandidateMessage>, I>>(base?: I): IceCandidateMessage {
    return IceCandidateMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IceCandidateMessage>, I>>(object: I): IceCandidateMessage {
    const message = createBaseIceCandidateMessage();
    message.roomId = object.roomId ?? "";
    message.fromUserId = object.fromUserId ?? "";
    message.toUserId = object.toUserId ?? "";
    message.candidate = (object.candidate !== undefined && object.candidate !== null)
      ? IceCandidate.fromPartial(object.candidate)
      : undefined;
    return message;
  },
};

function createBaseSdpOfferMessage(): SdpOfferMessage {
  return { roomId: "", fromUserId: "", toUserId: "", sdpOffer: "" };
}

export const SdpOfferMessage = {
  encode(message: SdpOfferMessage, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.roomId !== "") {
      writer.uint32(10).string(message.roomId);
    }
    if (message.fromUserId !== "") {
      writer.uint32(18).string(message.fromUserId);
    }
    if (message.toUserId !== "") {
      writer.uint32(26).string(message.toUserId);
    }
    if (message.sdpOffer !== "") {
      writer.uint32(34).string(message.sdpOffer);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SdpOfferMessage {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSdpOfferMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.roomId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.fromUserId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.toUserId = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.sdpOffer = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SdpOfferMessage {
    return {
      roomId: isSet(object.roomId) ? globalThis.String(object.roomId) : "",
      fromUserId: isSet(object.fromUserId) ? globalThis.String(object.fromUserId) : "",
      toUserId: isSet(object.toUserId) ? globalThis.String(object.toUserId) : "",
      sdpOffer: isSet(object.sdpOffer) ? globalThis.String(object.sdpOffer) : "",
    };
  },

  toJSON(message: SdpOfferMessage): unknown {
    const obj: any = {};
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.fromUserId !== "") {
      obj.fromUserId = message.fromUserId;
    }
    if (message.toUserId !== "") {
      obj.toUserId = message.toUserId;
    }
    if (message.sdpOffer !== "") {
      obj.sdpOffer = message.sdpOffer;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SdpOfferMessage>, I>>(base?: I): SdpOfferMessage {
    return SdpOfferMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SdpOfferMessage>, I>>(object: I): SdpOfferMessage {
    const message = createBaseSdpOfferMessage();
    message.roomId = object.roomId ?? "";
    message.fromUserId = object.fromUserId ?? "";
    message.toUserId = object.toUserId ?? "";
    message.sdpOffer = object.sdpOffer ?? "";
    return message;
  },
};

function createBaseSdpAnswerMessage(): SdpAnswerMessage {
  return { roomId: "", fromUserId: "", toUserId: "", sdpAnswer: "" };
}

export const SdpAnswerMessage = {
  encode(message: SdpAnswerMessage, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.roomId !== "") {
      writer.uint32(10).string(message.roomId);
    }
    if (message.fromUserId !== "") {
      writer.uint32(18).string(message.fromUserId);
    }
    if (message.toUserId !== "") {
      writer.uint32(26).string(message.toUserId);
    }
    if (message.sdpAnswer !== "") {
      writer.uint32(34).string(message.sdpAnswer);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SdpAnswerMessage {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSdpAnswerMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.roomId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.fromUserId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.toUserId = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.sdpAnswer = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SdpAnswerMessage {
    return {
      roomId: isSet(object.roomId) ? globalThis.String(object.roomId) : "",
      fromUserId: isSet(object.fromUserId) ? globalThis.String(object.fromUserId) : "",
      toUserId: isSet(object.toUserId) ? globalThis.String(object.toUserId) : "",
      sdpAnswer: isSet(object.sdpAnswer) ? globalThis.String(object.sdpAnswer) : "",
    };
  },

  toJSON(message: SdpAnswerMessage): unknown {
    const obj: any = {};
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.fromUserId !== "") {
      obj.fromUserId = message.fromUserId;
    }
    if (message.toUserId !== "") {
      obj.toUserId = message.toUserId;
    }
    if (message.sdpAnswer !== "") {
      obj.sdpAnswer = message.sdpAnswer;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SdpAnswerMessage>, I>>(base?: I): SdpAnswerMessage {
    return SdpAnswerMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SdpAnswerMessage>, I>>(object: I): SdpAnswerMessage {
    const message = createBaseSdpAnswerMessage();
    message.roomId = object.roomId ?? "";
    message.fromUserId = object.fromUserId ?? "";
    message.toUserId = object.toUserId ?? "";
    message.sdpAnswer = object.sdpAnswer ?? "";
    return message;
  },
};

function createBaseParticipantInfo(): ParticipantInfo {
  return { userId: "", username: "", state: 0, joinedAt: 0, isSpeaking: false, muted: false, deafened: false };
}

export const ParticipantInfo = {
  encode(message: ParticipantInfo, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.username !== "") {
      writer.uint32(18).string(message.username);
    }
    if (message.state !== 0) {
      writer.uint32(24).int32(message.state);
    }
    if (message.joinedAt !== 0) {
      writer.uint32(32).int64(message.joinedAt);
    }
    if (message.isSpeaking !== false) {
      writer.uint32(40).bool(message.isSpeaking);
    }
    if (message.muted !== false) {
      writer.uint32(48).bool(message.muted);
    }
    if (message.deafened !== false) {
      writer.uint32(56).bool(message.deafened);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ParticipantInfo {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParticipantInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.username = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.joinedAt = longToNumber(reader.int64() as Long);
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.isSpeaking = reader.bool();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.muted = reader.bool();
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.deafened = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ParticipantInfo {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      state: isSet(object.state) ? participantStateFromJSON(object.state) : 0,
      joinedAt: isSet(object.joinedAt) ? globalThis.Number(object.joinedAt) : 0,
      isSpeaking: isSet(object.isSpeaking) ? globalThis.Boolean(object.isSpeaking) : false,
      muted: isSet(object.muted) ? globalThis.Boolean(object.muted) : false,
      deafened: isSet(object.deafened) ? globalThis.Boolean(object.deafened) : false,
    };
  },

  toJSON(message: ParticipantInfo): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.state !== 0) {
      obj.state = participantStateToJSON(message.state);
    }
    if (message.joinedAt !== 0) {
      obj.joinedAt = Math.round(message.joinedAt);
    }
    if (message.isSpeaking !== false) {
      obj.isSpeaking = message.isSpeaking;
    }
    if (message.muted !== false) {
      obj.muted = message.muted;
    }
    if (message.deafened !== false) {
      obj.deafened = message.deafened;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ParticipantInfo>, I>>(base?: I): ParticipantInfo {
    return ParticipantInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ParticipantInfo>, I>>(object: I): ParticipantInfo {
    const message = createBaseParticipantInfo();
    message.userId = object.userId ?? "";
    message.username = object.username ?? "";
    message.state = object.state ?? 0;
    message.joinedAt = object.joinedAt ?? 0;
    message.isSpeaking = object.isSpeaking ?? false;
    message.muted = object.muted ?? false;
    message.deafened = object.deafened ?? false;
    return message;
  },
};

function createBaseGetRoomInfoRequest(): GetRoomInfoRequest {
  return { roomId: "" };
}

export const GetRoomInfoRequest = {
  encode(message: GetRoomInfoRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.roomId !== "") {
      writer.uint32(10).string(message.roomId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetRoomInfoRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetRoomInfoRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.roomId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetRoomInfoRequest {
    return { roomId: isSet(object.roomId) ? globalThis.String(object.roomId) : "" };
  },

  toJSON(message: GetRoomInfoRequest): unknown {
    const obj: any = {};
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetRoomInfoRequest>, I>>(base?: I): GetRoomInfoRequest {
    return GetRoomInfoRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetRoomInfoRequest>, I>>(object: I): GetRoomInfoRequest {
    const message = createBaseGetRoomInfoRequest();
    message.roomId = object.roomId ?? "";
    return message;
  },
};

function createBaseGetRoomInfoResponse(): GetRoomInfoResponse {
  return { code: 0, roomId: "", roomName: "", roomType: 0, participants: [], maxParticipants: 0 };
}

export const GetRoomInfoResponse = {
  encode(message: GetRoomInfoResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.roomId !== "") {
      writer.uint32(18).string(message.roomId);
    }
    if (message.roomName !== "") {
      writer.uint32(26).string(message.roomName);
    }
    if (message.roomType !== 0) {
      writer.uint32(32).int32(message.roomType);
    }
    for (const v of message.participants) {
      ParticipantInfo.encode(v!, writer.uint32(42).fork()).ldelim();
    }
    if (message.maxParticipants !== 0) {
      writer.uint32(48).int32(message.maxParticipants);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetRoomInfoResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetRoomInfoResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.roomId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.roomName = reader.string();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.roomType = reader.int32() as any;
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.participants.push(ParticipantInfo.decode(reader, reader.uint32()));
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.maxParticipants = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetRoomInfoResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      roomId: isSet(object.roomId) ? globalThis.String(object.roomId) : "",
      roomName: isSet(object.roomName) ? globalThis.String(object.roomName) : "",
      roomType: isSet(object.roomType) ? roomTypeFromJSON(object.roomType) : 0,
      participants: globalThis.Array.isArray(object?.participants)
        ? object.participants.map((e: any) => ParticipantInfo.fromJSON(e))
        : [],
      maxParticipants: isSet(object.maxParticipants) ? globalThis.Number(object.maxParticipants) : 0,
    };
  },

  toJSON(message: GetRoomInfoResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.roomName !== "") {
      obj.roomName = message.roomName;
    }
    if (message.roomType !== 0) {
      obj.roomType = roomTypeToJSON(message.roomType);
    }
    if (message.participants?.length) {
      obj.participants = message.participants.map((e) => ParticipantInfo.toJSON(e));
    }
    if (message.maxParticipants !== 0) {
      obj.maxParticipants = Math.round(message.maxParticipants);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetRoomInfoResponse>, I>>(base?: I): GetRoomInfoResponse {
    return GetRoomInfoResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetRoomInfoResponse>, I>>(object: I): GetRoomInfoResponse {
    const message = createBaseGetRoomInfoResponse();
    message.code = object.code ?? 0;
    message.roomId = object.roomId ?? "";
    message.roomName = object.roomName ?? "";
    message.roomType = object.roomType ?? 0;
    message.participants = object.participants?.map((e) => ParticipantInfo.fromPartial(e)) || [];
    message.maxParticipants = object.maxParticipants ?? 0;
    return message;
  },
};

function createBaseGetUserRoomRequest(): GetUserRoomRequest {
  return { userId: "" };
}

export const GetUserRoomRequest = {
  encode(message: GetUserRoomRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetUserRoomRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUserRoomRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUserRoomRequest {
    return { userId: isSet(object.userId) ? globalThis.String(object.userId) : "" };
  },

  toJSON(message: GetUserRoomRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetUserRoomRequest>, I>>(base?: I): GetUserRoomRequest {
    return GetUserRoomRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetUserRoomRequest>, I>>(object: I): GetUserRoomRequest {
    const message = createBaseGetUserRoomRequest();
    message.userId = object.userId ?? "";
    return message;
  },
};

function createBaseGetUserRoomResponse(): GetUserRoomResponse {
  return { code: 0, roomId: "", participant: undefined };
}

export const GetUserRoomResponse = {
  encode(message: GetUserRoomResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.roomId !== "") {
      writer.uint32(18).string(message.roomId);
    }
    if (message.participant !== undefined) {
      ParticipantInfo.encode(message.participant, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetUserRoomResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUserRoomResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.roomId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.participant = ParticipantInfo.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUserRoomResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      roomId: isSet(object.roomId) ? globalThis.String(object.roomId) : "",
      participant: isSet(object.participant) ? ParticipantInfo.fromJSON(object.participant) : undefined,
    };
  },

  toJSON(message: GetUserRoomResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.participant !== undefined) {
      obj.participant = ParticipantInfo.toJSON(message.participant);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetUserRoomResponse>, I>>(base?: I): GetUserRoomResponse {
    return GetUserRoomResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetUserRoomResponse>, I>>(object: I): GetUserRoomResponse {
    const message = createBaseGetUserRoomResponse();
    message.code = object.code ?? 0;
    message.roomId = object.roomId ?? "";
    message.participant = (object.participant !== undefined && object.participant !== null)
      ? ParticipantInfo.fromPartial(object.participant)
      : undefined;
    return message;
  },
};

function createBaseSetMuteRequest(): SetMuteRequest {
  return { userId: "", roomId: "", muted: false };
}

export const SetMuteRequest = {
  encode(message: SetMuteRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.roomId !== "") {
      writer.uint32(18).string(message.roomId);
    }
    if (message.muted !== false) {
      writer.uint32(24).bool(message.muted);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SetMuteRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetMuteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.roomId = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.muted = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetMuteRequest {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      roomId: isSet(object.roomId) ? globalThis.String(object.roomId) : "",
      muted: isSet(object.muted) ? globalThis.Boolean(object.muted) : false,
    };
  },

  toJSON(message: SetMuteRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.muted !== false) {
      obj.muted = message.muted;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetMuteRequest>, I>>(base?: I): SetMuteRequest {
    return SetMuteRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetMuteRequest>, I>>(object: I): SetMuteRequest {
    const message = createBaseSetMuteRequest();
    message.userId = object.userId ?? "";
    message.roomId = object.roomId ?? "";
    message.muted = object.muted ?? false;
    return message;
  },
};

function createBaseSetMuteResponse(): SetMuteResponse {
  return { code: 0, serverTime: 0 };
}

export const SetMuteResponse = {
  encode(message: SetMuteResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.serverTime !== 0) {
      writer.uint32(16).int64(message.serverTime);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SetMuteResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetMuteResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.serverTime = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetMuteResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      serverTime: isSet(object.serverTime) ? globalThis.Number(object.serverTime) : 0,
    };
  },

  toJSON(message: SetMuteResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.serverTime !== 0) {
      obj.serverTime = Math.round(message.serverTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetMuteResponse>, I>>(base?: I): SetMuteResponse {
    return SetMuteResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetMuteResponse>, I>>(object: I): SetMuteResponse {
    const message = createBaseSetMuteResponse();
    message.code = object.code ?? 0;
    message.serverTime = object.serverTime ?? 0;
    return message;
  },
};

function createBaseSetDeafenRequest(): SetDeafenRequest {
  return { userId: "", roomId: "", deafened: false };
}

export const SetDeafenRequest = {
  encode(message: SetDeafenRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.roomId !== "") {
      writer.uint32(18).string(message.roomId);
    }
    if (message.deafened !== false) {
      writer.uint32(24).bool(message.deafened);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SetDeafenRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetDeafenRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.roomId = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.deafened = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetDeafenRequest {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      roomId: isSet(object.roomId) ? globalThis.String(object.roomId) : "",
      deafened: isSet(object.deafened) ? globalThis.Boolean(object.deafened) : false,
    };
  },

  toJSON(message: SetDeafenRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.deafened !== false) {
      obj.deafened = message.deafened;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetDeafenRequest>, I>>(base?: I): SetDeafenRequest {
    return SetDeafenRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetDeafenRequest>, I>>(object: I): SetDeafenRequest {
    const message = createBaseSetDeafenRequest();
    message.userId = object.userId ?? "";
    message.roomId = object.roomId ?? "";
    message.deafened = object.deafened ?? false;
    return message;
  },
};

function createBaseSetDeafenResponse(): SetDeafenResponse {
  return { code: 0, serverTime: 0 };
}

export const SetDeafenResponse = {
  encode(message: SetDeafenResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.serverTime !== 0) {
      writer.uint32(16).int64(message.serverTime);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SetDeafenResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetDeafenResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.serverTime = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetDeafenResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      serverTime: isSet(object.serverTime) ? globalThis.Number(object.serverTime) : 0,
    };
  },

  toJSON(message: SetDeafenResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.serverTime !== 0) {
      obj.serverTime = Math.round(message.serverTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetDeafenResponse>, I>>(base?: I): SetDeafenResponse {
    return SetDeafenResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetDeafenResponse>, I>>(object: I): SetDeafenResponse {
    const message = createBaseSetDeafenResponse();
    message.code = object.code ?? 0;
    message.serverTime = object.serverTime ?? 0;
    return message;
  },
};

function createBaseParticipantJoinedNotify(): ParticipantJoinedNotify {
  return { roomId: "", participant: undefined, timestamp: 0 };
}

export const ParticipantJoinedNotify = {
  encode(message: ParticipantJoinedNotify, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.roomId !== "") {
      writer.uint32(10).string(message.roomId);
    }
    if (message.participant !== undefined) {
      ParticipantInfo.encode(message.participant, writer.uint32(18).fork()).ldelim();
    }
    if (message.timestamp !== 0) {
      writer.uint32(24).int64(message.timestamp);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ParticipantJoinedNotify {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParticipantJoinedNotify();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.roomId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.participant = ParticipantInfo.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.timestamp = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ParticipantJoinedNotify {
    return {
      roomId: isSet(object.roomId) ? globalThis.String(object.roomId) : "",
      participant: isSet(object.participant) ? ParticipantInfo.fromJSON(object.participant) : undefined,
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
    };
  },

  toJSON(message: ParticipantJoinedNotify): unknown {
    const obj: any = {};
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.participant !== undefined) {
      obj.participant = ParticipantInfo.toJSON(message.participant);
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ParticipantJoinedNotify>, I>>(base?: I): ParticipantJoinedNotify {
    return ParticipantJoinedNotify.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ParticipantJoinedNotify>, I>>(object: I): ParticipantJoinedNotify {
    const message = createBaseParticipantJoinedNotify();
    message.roomId = object.roomId ?? "";
    message.participant = (object.participant !== undefined && object.participant !== null)
      ? ParticipantInfo.fromPartial(object.participant)
      : undefined;
    message.timestamp = object.timestamp ?? 0;
    return message;
  },
};

function createBaseParticipantLeftNotify(): ParticipantLeftNotify {
  return { roomId: "", userId: "", timestamp: 0 };
}

export const ParticipantLeftNotify = {
  encode(message: ParticipantLeftNotify, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.roomId !== "") {
      writer.uint32(10).string(message.roomId);
    }
    if (message.userId !== "") {
      writer.uint32(18).string(message.userId);
    }
    if (message.timestamp !== 0) {
      writer.uint32(24).int64(message.timestamp);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ParticipantLeftNotify {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParticipantLeftNotify();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.roomId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.timestamp = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ParticipantLeftNotify {
    return {
      roomId: isSet(object.roomId) ? globalThis.String(object.roomId) : "",
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
    };
  },

  toJSON(message: ParticipantLeftNotify): unknown {
    const obj: any = {};
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ParticipantLeftNotify>, I>>(base?: I): ParticipantLeftNotify {
    return ParticipantLeftNotify.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ParticipantLeftNotify>, I>>(object: I): ParticipantLeftNotify {
    const message = createBaseParticipantLeftNotify();
    message.roomId = object.roomId ?? "";
    message.userId = object.userId ?? "";
    message.timestamp = object.timestamp ?? 0;
    return message;
  },
};

function createBaseParticipantStateChangedNotify(): ParticipantStateChangedNotify {
  return { roomId: "", userId: "", state: 0, timestamp: 0 };
}

export const ParticipantStateChangedNotify = {
  encode(message: ParticipantStateChangedNotify, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.roomId !== "") {
      writer.uint32(10).string(message.roomId);
    }
    if (message.userId !== "") {
      writer.uint32(18).string(message.userId);
    }
    if (message.state !== 0) {
      writer.uint32(24).int32(message.state);
    }
    if (message.timestamp !== 0) {
      writer.uint32(32).int64(message.timestamp);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ParticipantStateChangedNotify {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParticipantStateChangedNotify();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.roomId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.timestamp = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ParticipantStateChangedNotify {
    return {
      roomId: isSet(object.roomId) ? globalThis.String(object.roomId) : "",
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      state: isSet(object.state) ? participantStateFromJSON(object.state) : 0,
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
    };
  },

  toJSON(message: ParticipantStateChangedNotify): unknown {
    const obj: any = {};
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.state !== 0) {
      obj.state = participantStateToJSON(message.state);
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ParticipantStateChangedNotify>, I>>(base?: I): ParticipantStateChangedNotify {
    return ParticipantStateChangedNotify.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ParticipantStateChangedNotify>, I>>(
    object: I,
  ): ParticipantStateChangedNotify {
    const message = createBaseParticipantStateChangedNotify();
    message.roomId = object.roomId ?? "";
    message.userId = object.userId ?? "";
    message.state = object.state ?? 0;
    message.timestamp = object.timestamp ?? 0;
    return message;
  },
};

function createBaseSpeakingNotify(): SpeakingNotify {
  return { roomId: "", userId: "", speaking: false, timestamp: 0 };
}

export const SpeakingNotify = {
  encode(message: SpeakingNotify, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.roomId !== "") {
      writer.uint32(10).string(message.roomId);
    }
    if (message.userId !== "") {
      writer.uint32(18).string(message.userId);
    }
    if (message.speaking !== false) {
      writer.uint32(24).bool(message.speaking);
    }
    if (message.timestamp !== 0) {
      writer.uint32(32).int64(message.timestamp);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SpeakingNotify {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSpeakingNotify();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.roomId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.speaking = reader.bool();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.timestamp = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SpeakingNotify {
    return {
      roomId: isSet(object.roomId) ? globalThis.String(object.roomId) : "",
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      speaking: isSet(object.speaking) ? globalThis.Boolean(object.speaking) : false,
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
    };
  },

  toJSON(message: SpeakingNotify): unknown {
    const obj: any = {};
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.speaking !== false) {
      obj.speaking = message.speaking;
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SpeakingNotify>, I>>(base?: I): SpeakingNotify {
    return SpeakingNotify.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SpeakingNotify>, I>>(object: I): SpeakingNotify {
    const message = createBaseSpeakingNotify();
    message.roomId = object.roomId ?? "";
    message.userId = object.userId ?? "";
    message.speaking = object.speaking ?? false;
    message.timestamp = object.timestamp ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

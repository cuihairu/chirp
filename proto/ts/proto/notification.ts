// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v7.34.1
// source: proto/notification.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";
import { ErrorCode, errorCodeFromJSON, errorCodeToJSON } from "./common";

export const protobufPackage = "chirp.notification";

/** Notification priority */
export enum NotificationPriority {
  LOW = 0,
  NORMAL = 1,
  HIGH = 2,
  URGENT = 3,
  UNRECOGNIZED = -1,
}

export function notificationPriorityFromJSON(object: any): NotificationPriority {
  switch (object) {
    case 0:
    case "LOW":
      return NotificationPriority.LOW;
    case 1:
    case "NORMAL":
      return NotificationPriority.NORMAL;
    case 2:
    case "HIGH":
      return NotificationPriority.HIGH;
    case 3:
    case "URGENT":
      return NotificationPriority.URGENT;
    case -1:
    case "UNRECOGNIZED":
    default:
      return NotificationPriority.UNRECOGNIZED;
  }
}

export function notificationPriorityToJSON(object: NotificationPriority): string {
  switch (object) {
    case NotificationPriority.LOW:
      return "LOW";
    case NotificationPriority.NORMAL:
      return "NORMAL";
    case NotificationPriority.HIGH:
      return "HIGH";
    case NotificationPriority.URGENT:
      return "URGENT";
    case NotificationPriority.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Notification type */
export enum NotificationType {
  /** MESSAGE - New message */
  MESSAGE = 0,
  /** MENTION - @mention */
  MENTION = 1,
  /** REACTION - Reaction to message */
  REACTION = 2,
  /** FRIEND_REQUEST - New friend request */
  FRIEND_REQUEST = 3,
  /** FRIEND_ACCEPTED - Friend request accepted */
  FRIEND_ACCEPTED = 4,
  /** VOICE_CALL - Incoming voice call */
  VOICE_CALL = 5,
  /** VOICE_INVITE - Voice room invite */
  VOICE_INVITE = 6,
  /** SYSTEM - System notification */
  SYSTEM = 99,
  UNRECOGNIZED = -1,
}

export function notificationTypeFromJSON(object: any): NotificationType {
  switch (object) {
    case 0:
    case "MESSAGE":
      return NotificationType.MESSAGE;
    case 1:
    case "MENTION":
      return NotificationType.MENTION;
    case 2:
    case "REACTION":
      return NotificationType.REACTION;
    case 3:
    case "FRIEND_REQUEST":
      return NotificationType.FRIEND_REQUEST;
    case 4:
    case "FRIEND_ACCEPTED":
      return NotificationType.FRIEND_ACCEPTED;
    case 5:
    case "VOICE_CALL":
      return NotificationType.VOICE_CALL;
    case 6:
    case "VOICE_INVITE":
      return NotificationType.VOICE_INVITE;
    case 99:
    case "SYSTEM":
      return NotificationType.SYSTEM;
    case -1:
    case "UNRECOGNIZED":
    default:
      return NotificationType.UNRECOGNIZED;
  }
}

export function notificationTypeToJSON(object: NotificationType): string {
  switch (object) {
    case NotificationType.MESSAGE:
      return "MESSAGE";
    case NotificationType.MENTION:
      return "MENTION";
    case NotificationType.REACTION:
      return "REACTION";
    case NotificationType.FRIEND_REQUEST:
      return "FRIEND_REQUEST";
    case NotificationType.FRIEND_ACCEPTED:
      return "FRIEND_ACCEPTED";
    case NotificationType.VOICE_CALL:
      return "VOICE_CALL";
    case NotificationType.VOICE_INVITE:
      return "VOICE_INVITE";
    case NotificationType.SYSTEM:
      return "SYSTEM";
    case NotificationType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Push notification request */
export interface PushNotificationRequest {
  userId: string;
  type: NotificationType;
  priority: NotificationPriority;
  title: string;
  body: string;
  icon: string;
  image: string;
  sound: string;
  tag: string;
  /** Custom key-value data */
  data: { [key: string]: string };
  /** iOS badge count */
  badge: number;
  /** Deep link */
  clickAction: string;
  /** Time to live */
  ttlMs: number;
  /** Collapse duplicate notifications */
  collapseKey: boolean;
}

export interface PushNotificationRequest_DataEntry {
  key: string;
  value: string;
}

/** Push notification response */
export interface PushNotificationResponse {
  code: ErrorCode;
  notificationId: string;
  serverTime: number;
}

/** Register device request */
export interface RegisterDeviceRequest {
  userId: string;
  deviceId: string;
  /** "ios", "android", "web" */
  platform: string;
  /** FCM/APNs tokens */
  fcmToken: string;
  /** iOS */
  apnsToken: string;
  /** iOS VoIP */
  pushKitToken: string;
  appVersion: string;
  osVersion: string;
  deviceName: string;
}

export interface RegisterDeviceResponse {
  code: ErrorCode;
  serverTime: number;
}

/** Unregister device request */
export interface UnregisterDeviceRequest {
  userId: string;
  deviceId: string;
}

export interface UnregisterDeviceResponse {
  code: ErrorCode;
  serverTime: number;
}

/** Update device token request */
export interface UpdateDeviceTokenRequest {
  deviceId: string;
  fcmToken: string;
  apnsToken: string;
  pushKitToken: string;
}

export interface UpdateDeviceTokenResponse {
  code: ErrorCode;
  serverTime: number;
}

/** Get user devices request */
export interface GetUserDevicesRequest {
  userId: string;
}

export interface GetUserDevicesResponse {
  code: ErrorCode;
  devices: DeviceInfo[];
}

/** Device info */
export interface DeviceInfo {
  deviceId: string;
  userId: string;
  platform: string;
  appVersion: string;
  osVersion: string;
  deviceName: string;
  registeredAt: number;
  isActive: boolean;
}

/** Set badge count request */
export interface SetBadgeCountRequest {
  userId: string;
  count: number;
}

export interface SetBadgeCountResponse {
  code: ErrorCode;
  serverTime: number;
}

/** Silent notification (data only) */
export interface SilentNotificationRequest {
  userId: string;
  data: { [key: string]: string };
  ttlMs: number;
}

export interface SilentNotificationRequest_DataEntry {
  key: string;
  value: string;
}

/** Notification preferences per user */
export interface NotificationPreferences {
  enabled: boolean;
  soundEnabled: boolean;
  vibrationEnabled: boolean;
  showPreview: boolean;
  allowMessages: boolean;
  allowMentions: boolean;
  allowFriendRequests: boolean;
  allowVoiceCalls: boolean;
  /** Do Not Disturb */
  dndEnabled: boolean;
  /** 0-23 */
  dndStartHour: number;
  dndEndHour: number;
  /** 0=Sunday, 6=Saturday */
  dndDays: number[];
  /** Per-channel settings */
  channelSettings: { [key: string]: ChannelNotificationSettings };
}

export interface NotificationPreferences_ChannelSettingsEntry {
  key: string;
  value: ChannelNotificationSettings | undefined;
}

/** Channel notification settings */
export interface ChannelNotificationSettings {
  muted: boolean;
  onlyMentions: boolean;
  notifyOnMentions: boolean;
}

/** Set notification preferences request */
export interface SetPreferencesRequest {
  userId: string;
  preferences: NotificationPreferences | undefined;
}

export interface SetPreferencesResponse {
  code: ErrorCode;
  serverTime: number;
}

/** Get preferences request */
export interface GetPreferencesRequest {
  userId: string;
}

export interface GetPreferencesResponse {
  code: ErrorCode;
  preferences: NotificationPreferences | undefined;
}

function createBasePushNotificationRequest(): PushNotificationRequest {
  return {
    userId: "",
    type: 0,
    priority: 0,
    title: "",
    body: "",
    icon: "",
    image: "",
    sound: "",
    tag: "",
    data: {},
    badge: 0,
    clickAction: "",
    ttlMs: 0,
    collapseKey: false,
  };
}

export const PushNotificationRequest = {
  encode(message: PushNotificationRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.type !== 0) {
      writer.uint32(16).int32(message.type);
    }
    if (message.priority !== 0) {
      writer.uint32(24).int32(message.priority);
    }
    if (message.title !== "") {
      writer.uint32(34).string(message.title);
    }
    if (message.body !== "") {
      writer.uint32(42).string(message.body);
    }
    if (message.icon !== "") {
      writer.uint32(50).string(message.icon);
    }
    if (message.image !== "") {
      writer.uint32(58).string(message.image);
    }
    if (message.sound !== "") {
      writer.uint32(66).string(message.sound);
    }
    if (message.tag !== "") {
      writer.uint32(74).string(message.tag);
    }
    Object.entries(message.data).forEach(([key, value]) => {
      PushNotificationRequest_DataEntry.encode({ key: key as any, value }, writer.uint32(82).fork()).ldelim();
    });
    if (message.badge !== 0) {
      writer.uint32(88).int32(message.badge);
    }
    if (message.clickAction !== "") {
      writer.uint32(98).string(message.clickAction);
    }
    if (message.ttlMs !== 0) {
      writer.uint32(104).int64(message.ttlMs);
    }
    if (message.collapseKey !== false) {
      writer.uint32(112).bool(message.collapseKey);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PushNotificationRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePushNotificationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.priority = reader.int32() as any;
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.title = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.body = reader.string();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.icon = reader.string();
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.image = reader.string();
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.sound = reader.string();
          continue;
        case 9:
          if (tag !== 74) {
            break;
          }

          message.tag = reader.string();
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          const entry10 = PushNotificationRequest_DataEntry.decode(reader, reader.uint32());
          if (entry10.value !== undefined) {
            message.data[entry10.key] = entry10.value;
          }
          continue;
        case 11:
          if (tag !== 88) {
            break;
          }

          message.badge = reader.int32();
          continue;
        case 12:
          if (tag !== 98) {
            break;
          }

          message.clickAction = reader.string();
          continue;
        case 13:
          if (tag !== 104) {
            break;
          }

          message.ttlMs = longToNumber(reader.int64() as Long);
          continue;
        case 14:
          if (tag !== 112) {
            break;
          }

          message.collapseKey = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PushNotificationRequest {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      type: isSet(object.type) ? notificationTypeFromJSON(object.type) : 0,
      priority: isSet(object.priority) ? notificationPriorityFromJSON(object.priority) : 0,
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      body: isSet(object.body) ? globalThis.String(object.body) : "",
      icon: isSet(object.icon) ? globalThis.String(object.icon) : "",
      image: isSet(object.image) ? globalThis.String(object.image) : "",
      sound: isSet(object.sound) ? globalThis.String(object.sound) : "",
      tag: isSet(object.tag) ? globalThis.String(object.tag) : "",
      data: isObject(object.data)
        ? Object.entries(object.data).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
      badge: isSet(object.badge) ? globalThis.Number(object.badge) : 0,
      clickAction: isSet(object.clickAction) ? globalThis.String(object.clickAction) : "",
      ttlMs: isSet(object.ttlMs) ? globalThis.Number(object.ttlMs) : 0,
      collapseKey: isSet(object.collapseKey) ? globalThis.Boolean(object.collapseKey) : false,
    };
  },

  toJSON(message: PushNotificationRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.type !== 0) {
      obj.type = notificationTypeToJSON(message.type);
    }
    if (message.priority !== 0) {
      obj.priority = notificationPriorityToJSON(message.priority);
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.body !== "") {
      obj.body = message.body;
    }
    if (message.icon !== "") {
      obj.icon = message.icon;
    }
    if (message.image !== "") {
      obj.image = message.image;
    }
    if (message.sound !== "") {
      obj.sound = message.sound;
    }
    if (message.tag !== "") {
      obj.tag = message.tag;
    }
    if (message.data) {
      const entries = Object.entries(message.data);
      if (entries.length > 0) {
        obj.data = {};
        entries.forEach(([k, v]) => {
          obj.data[k] = v;
        });
      }
    }
    if (message.badge !== 0) {
      obj.badge = Math.round(message.badge);
    }
    if (message.clickAction !== "") {
      obj.clickAction = message.clickAction;
    }
    if (message.ttlMs !== 0) {
      obj.ttlMs = Math.round(message.ttlMs);
    }
    if (message.collapseKey !== false) {
      obj.collapseKey = message.collapseKey;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PushNotificationRequest>, I>>(base?: I): PushNotificationRequest {
    return PushNotificationRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PushNotificationRequest>, I>>(object: I): PushNotificationRequest {
    const message = createBasePushNotificationRequest();
    message.userId = object.userId ?? "";
    message.type = object.type ?? 0;
    message.priority = object.priority ?? 0;
    message.title = object.title ?? "";
    message.body = object.body ?? "";
    message.icon = object.icon ?? "";
    message.image = object.image ?? "";
    message.sound = object.sound ?? "";
    message.tag = object.tag ?? "";
    message.data = Object.entries(object.data ?? {}).reduce<{ [key: string]: string }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = globalThis.String(value);
      }
      return acc;
    }, {});
    message.badge = object.badge ?? 0;
    message.clickAction = object.clickAction ?? "";
    message.ttlMs = object.ttlMs ?? 0;
    message.collapseKey = object.collapseKey ?? false;
    return message;
  },
};

function createBasePushNotificationRequest_DataEntry(): PushNotificationRequest_DataEntry {
  return { key: "", value: "" };
}

export const PushNotificationRequest_DataEntry = {
  encode(message: PushNotificationRequest_DataEntry, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PushNotificationRequest_DataEntry {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePushNotificationRequest_DataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PushNotificationRequest_DataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: PushNotificationRequest_DataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PushNotificationRequest_DataEntry>, I>>(
    base?: I,
  ): PushNotificationRequest_DataEntry {
    return PushNotificationRequest_DataEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PushNotificationRequest_DataEntry>, I>>(
    object: I,
  ): PushNotificationRequest_DataEntry {
    const message = createBasePushNotificationRequest_DataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBasePushNotificationResponse(): PushNotificationResponse {
  return { code: 0, notificationId: "", serverTime: 0 };
}

export const PushNotificationResponse = {
  encode(message: PushNotificationResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.notificationId !== "") {
      writer.uint32(18).string(message.notificationId);
    }
    if (message.serverTime !== 0) {
      writer.uint32(24).int64(message.serverTime);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PushNotificationResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePushNotificationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.notificationId = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.serverTime = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PushNotificationResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      notificationId: isSet(object.notificationId) ? globalThis.String(object.notificationId) : "",
      serverTime: isSet(object.serverTime) ? globalThis.Number(object.serverTime) : 0,
    };
  },

  toJSON(message: PushNotificationResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.notificationId !== "") {
      obj.notificationId = message.notificationId;
    }
    if (message.serverTime !== 0) {
      obj.serverTime = Math.round(message.serverTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PushNotificationResponse>, I>>(base?: I): PushNotificationResponse {
    return PushNotificationResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PushNotificationResponse>, I>>(object: I): PushNotificationResponse {
    const message = createBasePushNotificationResponse();
    message.code = object.code ?? 0;
    message.notificationId = object.notificationId ?? "";
    message.serverTime = object.serverTime ?? 0;
    return message;
  },
};

function createBaseRegisterDeviceRequest(): RegisterDeviceRequest {
  return {
    userId: "",
    deviceId: "",
    platform: "",
    fcmToken: "",
    apnsToken: "",
    pushKitToken: "",
    appVersion: "",
    osVersion: "",
    deviceName: "",
  };
}

export const RegisterDeviceRequest = {
  encode(message: RegisterDeviceRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.deviceId !== "") {
      writer.uint32(18).string(message.deviceId);
    }
    if (message.platform !== "") {
      writer.uint32(26).string(message.platform);
    }
    if (message.fcmToken !== "") {
      writer.uint32(34).string(message.fcmToken);
    }
    if (message.apnsToken !== "") {
      writer.uint32(42).string(message.apnsToken);
    }
    if (message.pushKitToken !== "") {
      writer.uint32(50).string(message.pushKitToken);
    }
    if (message.appVersion !== "") {
      writer.uint32(58).string(message.appVersion);
    }
    if (message.osVersion !== "") {
      writer.uint32(66).string(message.osVersion);
    }
    if (message.deviceName !== "") {
      writer.uint32(74).string(message.deviceName);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RegisterDeviceRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterDeviceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.deviceId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.platform = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.fcmToken = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.apnsToken = reader.string();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.pushKitToken = reader.string();
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.appVersion = reader.string();
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.osVersion = reader.string();
          continue;
        case 9:
          if (tag !== 74) {
            break;
          }

          message.deviceName = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterDeviceRequest {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      deviceId: isSet(object.deviceId) ? globalThis.String(object.deviceId) : "",
      platform: isSet(object.platform) ? globalThis.String(object.platform) : "",
      fcmToken: isSet(object.fcmToken) ? globalThis.String(object.fcmToken) : "",
      apnsToken: isSet(object.apnsToken) ? globalThis.String(object.apnsToken) : "",
      pushKitToken: isSet(object.pushKitToken) ? globalThis.String(object.pushKitToken) : "",
      appVersion: isSet(object.appVersion) ? globalThis.String(object.appVersion) : "",
      osVersion: isSet(object.osVersion) ? globalThis.String(object.osVersion) : "",
      deviceName: isSet(object.deviceName) ? globalThis.String(object.deviceName) : "",
    };
  },

  toJSON(message: RegisterDeviceRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.deviceId !== "") {
      obj.deviceId = message.deviceId;
    }
    if (message.platform !== "") {
      obj.platform = message.platform;
    }
    if (message.fcmToken !== "") {
      obj.fcmToken = message.fcmToken;
    }
    if (message.apnsToken !== "") {
      obj.apnsToken = message.apnsToken;
    }
    if (message.pushKitToken !== "") {
      obj.pushKitToken = message.pushKitToken;
    }
    if (message.appVersion !== "") {
      obj.appVersion = message.appVersion;
    }
    if (message.osVersion !== "") {
      obj.osVersion = message.osVersion;
    }
    if (message.deviceName !== "") {
      obj.deviceName = message.deviceName;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterDeviceRequest>, I>>(base?: I): RegisterDeviceRequest {
    return RegisterDeviceRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterDeviceRequest>, I>>(object: I): RegisterDeviceRequest {
    const message = createBaseRegisterDeviceRequest();
    message.userId = object.userId ?? "";
    message.deviceId = object.deviceId ?? "";
    message.platform = object.platform ?? "";
    message.fcmToken = object.fcmToken ?? "";
    message.apnsToken = object.apnsToken ?? "";
    message.pushKitToken = object.pushKitToken ?? "";
    message.appVersion = object.appVersion ?? "";
    message.osVersion = object.osVersion ?? "";
    message.deviceName = object.deviceName ?? "";
    return message;
  },
};

function createBaseRegisterDeviceResponse(): RegisterDeviceResponse {
  return { code: 0, serverTime: 0 };
}

export const RegisterDeviceResponse = {
  encode(message: RegisterDeviceResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.serverTime !== 0) {
      writer.uint32(16).int64(message.serverTime);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RegisterDeviceResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterDeviceResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.serverTime = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterDeviceResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      serverTime: isSet(object.serverTime) ? globalThis.Number(object.serverTime) : 0,
    };
  },

  toJSON(message: RegisterDeviceResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.serverTime !== 0) {
      obj.serverTime = Math.round(message.serverTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterDeviceResponse>, I>>(base?: I): RegisterDeviceResponse {
    return RegisterDeviceResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterDeviceResponse>, I>>(object: I): RegisterDeviceResponse {
    const message = createBaseRegisterDeviceResponse();
    message.code = object.code ?? 0;
    message.serverTime = object.serverTime ?? 0;
    return message;
  },
};

function createBaseUnregisterDeviceRequest(): UnregisterDeviceRequest {
  return { userId: "", deviceId: "" };
}

export const UnregisterDeviceRequest = {
  encode(message: UnregisterDeviceRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.deviceId !== "") {
      writer.uint32(18).string(message.deviceId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): UnregisterDeviceRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnregisterDeviceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.deviceId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UnregisterDeviceRequest {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      deviceId: isSet(object.deviceId) ? globalThis.String(object.deviceId) : "",
    };
  },

  toJSON(message: UnregisterDeviceRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.deviceId !== "") {
      obj.deviceId = message.deviceId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UnregisterDeviceRequest>, I>>(base?: I): UnregisterDeviceRequest {
    return UnregisterDeviceRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UnregisterDeviceRequest>, I>>(object: I): UnregisterDeviceRequest {
    const message = createBaseUnregisterDeviceRequest();
    message.userId = object.userId ?? "";
    message.deviceId = object.deviceId ?? "";
    return message;
  },
};

function createBaseUnregisterDeviceResponse(): UnregisterDeviceResponse {
  return { code: 0, serverTime: 0 };
}

export const UnregisterDeviceResponse = {
  encode(message: UnregisterDeviceResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.serverTime !== 0) {
      writer.uint32(16).int64(message.serverTime);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): UnregisterDeviceResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnregisterDeviceResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.serverTime = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UnregisterDeviceResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      serverTime: isSet(object.serverTime) ? globalThis.Number(object.serverTime) : 0,
    };
  },

  toJSON(message: UnregisterDeviceResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.serverTime !== 0) {
      obj.serverTime = Math.round(message.serverTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UnregisterDeviceResponse>, I>>(base?: I): UnregisterDeviceResponse {
    return UnregisterDeviceResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UnregisterDeviceResponse>, I>>(object: I): UnregisterDeviceResponse {
    const message = createBaseUnregisterDeviceResponse();
    message.code = object.code ?? 0;
    message.serverTime = object.serverTime ?? 0;
    return message;
  },
};

function createBaseUpdateDeviceTokenRequest(): UpdateDeviceTokenRequest {
  return { deviceId: "", fcmToken: "", apnsToken: "", pushKitToken: "" };
}

export const UpdateDeviceTokenRequest = {
  encode(message: UpdateDeviceTokenRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.deviceId !== "") {
      writer.uint32(10).string(message.deviceId);
    }
    if (message.fcmToken !== "") {
      writer.uint32(18).string(message.fcmToken);
    }
    if (message.apnsToken !== "") {
      writer.uint32(26).string(message.apnsToken);
    }
    if (message.pushKitToken !== "") {
      writer.uint32(34).string(message.pushKitToken);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): UpdateDeviceTokenRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateDeviceTokenRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.deviceId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.fcmToken = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.apnsToken = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.pushKitToken = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateDeviceTokenRequest {
    return {
      deviceId: isSet(object.deviceId) ? globalThis.String(object.deviceId) : "",
      fcmToken: isSet(object.fcmToken) ? globalThis.String(object.fcmToken) : "",
      apnsToken: isSet(object.apnsToken) ? globalThis.String(object.apnsToken) : "",
      pushKitToken: isSet(object.pushKitToken) ? globalThis.String(object.pushKitToken) : "",
    };
  },

  toJSON(message: UpdateDeviceTokenRequest): unknown {
    const obj: any = {};
    if (message.deviceId !== "") {
      obj.deviceId = message.deviceId;
    }
    if (message.fcmToken !== "") {
      obj.fcmToken = message.fcmToken;
    }
    if (message.apnsToken !== "") {
      obj.apnsToken = message.apnsToken;
    }
    if (message.pushKitToken !== "") {
      obj.pushKitToken = message.pushKitToken;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateDeviceTokenRequest>, I>>(base?: I): UpdateDeviceTokenRequest {
    return UpdateDeviceTokenRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateDeviceTokenRequest>, I>>(object: I): UpdateDeviceTokenRequest {
    const message = createBaseUpdateDeviceTokenRequest();
    message.deviceId = object.deviceId ?? "";
    message.fcmToken = object.fcmToken ?? "";
    message.apnsToken = object.apnsToken ?? "";
    message.pushKitToken = object.pushKitToken ?? "";
    return message;
  },
};

function createBaseUpdateDeviceTokenResponse(): UpdateDeviceTokenResponse {
  return { code: 0, serverTime: 0 };
}

export const UpdateDeviceTokenResponse = {
  encode(message: UpdateDeviceTokenResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.serverTime !== 0) {
      writer.uint32(16).int64(message.serverTime);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): UpdateDeviceTokenResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateDeviceTokenResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.serverTime = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateDeviceTokenResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      serverTime: isSet(object.serverTime) ? globalThis.Number(object.serverTime) : 0,
    };
  },

  toJSON(message: UpdateDeviceTokenResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.serverTime !== 0) {
      obj.serverTime = Math.round(message.serverTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateDeviceTokenResponse>, I>>(base?: I): UpdateDeviceTokenResponse {
    return UpdateDeviceTokenResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateDeviceTokenResponse>, I>>(object: I): UpdateDeviceTokenResponse {
    const message = createBaseUpdateDeviceTokenResponse();
    message.code = object.code ?? 0;
    message.serverTime = object.serverTime ?? 0;
    return message;
  },
};

function createBaseGetUserDevicesRequest(): GetUserDevicesRequest {
  return { userId: "" };
}

export const GetUserDevicesRequest = {
  encode(message: GetUserDevicesRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetUserDevicesRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUserDevicesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUserDevicesRequest {
    return { userId: isSet(object.userId) ? globalThis.String(object.userId) : "" };
  },

  toJSON(message: GetUserDevicesRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetUserDevicesRequest>, I>>(base?: I): GetUserDevicesRequest {
    return GetUserDevicesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetUserDevicesRequest>, I>>(object: I): GetUserDevicesRequest {
    const message = createBaseGetUserDevicesRequest();
    message.userId = object.userId ?? "";
    return message;
  },
};

function createBaseGetUserDevicesResponse(): GetUserDevicesResponse {
  return { code: 0, devices: [] };
}

export const GetUserDevicesResponse = {
  encode(message: GetUserDevicesResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    for (const v of message.devices) {
      DeviceInfo.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetUserDevicesResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUserDevicesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.devices.push(DeviceInfo.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUserDevicesResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      devices: globalThis.Array.isArray(object?.devices) ? object.devices.map((e: any) => DeviceInfo.fromJSON(e)) : [],
    };
  },

  toJSON(message: GetUserDevicesResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.devices?.length) {
      obj.devices = message.devices.map((e) => DeviceInfo.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetUserDevicesResponse>, I>>(base?: I): GetUserDevicesResponse {
    return GetUserDevicesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetUserDevicesResponse>, I>>(object: I): GetUserDevicesResponse {
    const message = createBaseGetUserDevicesResponse();
    message.code = object.code ?? 0;
    message.devices = object.devices?.map((e) => DeviceInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseDeviceInfo(): DeviceInfo {
  return {
    deviceId: "",
    userId: "",
    platform: "",
    appVersion: "",
    osVersion: "",
    deviceName: "",
    registeredAt: 0,
    isActive: false,
  };
}

export const DeviceInfo = {
  encode(message: DeviceInfo, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.deviceId !== "") {
      writer.uint32(10).string(message.deviceId);
    }
    if (message.userId !== "") {
      writer.uint32(18).string(message.userId);
    }
    if (message.platform !== "") {
      writer.uint32(26).string(message.platform);
    }
    if (message.appVersion !== "") {
      writer.uint32(34).string(message.appVersion);
    }
    if (message.osVersion !== "") {
      writer.uint32(42).string(message.osVersion);
    }
    if (message.deviceName !== "") {
      writer.uint32(50).string(message.deviceName);
    }
    if (message.registeredAt !== 0) {
      writer.uint32(56).int64(message.registeredAt);
    }
    if (message.isActive !== false) {
      writer.uint32(64).bool(message.isActive);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DeviceInfo {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeviceInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.deviceId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.platform = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.appVersion = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.osVersion = reader.string();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.deviceName = reader.string();
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.registeredAt = longToNumber(reader.int64() as Long);
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.isActive = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeviceInfo {
    return {
      deviceId: isSet(object.deviceId) ? globalThis.String(object.deviceId) : "",
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      platform: isSet(object.platform) ? globalThis.String(object.platform) : "",
      appVersion: isSet(object.appVersion) ? globalThis.String(object.appVersion) : "",
      osVersion: isSet(object.osVersion) ? globalThis.String(object.osVersion) : "",
      deviceName: isSet(object.deviceName) ? globalThis.String(object.deviceName) : "",
      registeredAt: isSet(object.registeredAt) ? globalThis.Number(object.registeredAt) : 0,
      isActive: isSet(object.isActive) ? globalThis.Boolean(object.isActive) : false,
    };
  },

  toJSON(message: DeviceInfo): unknown {
    const obj: any = {};
    if (message.deviceId !== "") {
      obj.deviceId = message.deviceId;
    }
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.platform !== "") {
      obj.platform = message.platform;
    }
    if (message.appVersion !== "") {
      obj.appVersion = message.appVersion;
    }
    if (message.osVersion !== "") {
      obj.osVersion = message.osVersion;
    }
    if (message.deviceName !== "") {
      obj.deviceName = message.deviceName;
    }
    if (message.registeredAt !== 0) {
      obj.registeredAt = Math.round(message.registeredAt);
    }
    if (message.isActive !== false) {
      obj.isActive = message.isActive;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeviceInfo>, I>>(base?: I): DeviceInfo {
    return DeviceInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeviceInfo>, I>>(object: I): DeviceInfo {
    const message = createBaseDeviceInfo();
    message.deviceId = object.deviceId ?? "";
    message.userId = object.userId ?? "";
    message.platform = object.platform ?? "";
    message.appVersion = object.appVersion ?? "";
    message.osVersion = object.osVersion ?? "";
    message.deviceName = object.deviceName ?? "";
    message.registeredAt = object.registeredAt ?? 0;
    message.isActive = object.isActive ?? false;
    return message;
  },
};

function createBaseSetBadgeCountRequest(): SetBadgeCountRequest {
  return { userId: "", count: 0 };
}

export const SetBadgeCountRequest = {
  encode(message: SetBadgeCountRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.count !== 0) {
      writer.uint32(16).int32(message.count);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SetBadgeCountRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetBadgeCountRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.count = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetBadgeCountRequest {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      count: isSet(object.count) ? globalThis.Number(object.count) : 0,
    };
  },

  toJSON(message: SetBadgeCountRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.count !== 0) {
      obj.count = Math.round(message.count);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetBadgeCountRequest>, I>>(base?: I): SetBadgeCountRequest {
    return SetBadgeCountRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetBadgeCountRequest>, I>>(object: I): SetBadgeCountRequest {
    const message = createBaseSetBadgeCountRequest();
    message.userId = object.userId ?? "";
    message.count = object.count ?? 0;
    return message;
  },
};

function createBaseSetBadgeCountResponse(): SetBadgeCountResponse {
  return { code: 0, serverTime: 0 };
}

export const SetBadgeCountResponse = {
  encode(message: SetBadgeCountResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.serverTime !== 0) {
      writer.uint32(16).int64(message.serverTime);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SetBadgeCountResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetBadgeCountResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.serverTime = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetBadgeCountResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      serverTime: isSet(object.serverTime) ? globalThis.Number(object.serverTime) : 0,
    };
  },

  toJSON(message: SetBadgeCountResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.serverTime !== 0) {
      obj.serverTime = Math.round(message.serverTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetBadgeCountResponse>, I>>(base?: I): SetBadgeCountResponse {
    return SetBadgeCountResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetBadgeCountResponse>, I>>(object: I): SetBadgeCountResponse {
    const message = createBaseSetBadgeCountResponse();
    message.code = object.code ?? 0;
    message.serverTime = object.serverTime ?? 0;
    return message;
  },
};

function createBaseSilentNotificationRequest(): SilentNotificationRequest {
  return { userId: "", data: {}, ttlMs: 0 };
}

export const SilentNotificationRequest = {
  encode(message: SilentNotificationRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    Object.entries(message.data).forEach(([key, value]) => {
      SilentNotificationRequest_DataEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).ldelim();
    });
    if (message.ttlMs !== 0) {
      writer.uint32(24).int64(message.ttlMs);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SilentNotificationRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSilentNotificationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          const entry2 = SilentNotificationRequest_DataEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.data[entry2.key] = entry2.value;
          }
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.ttlMs = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SilentNotificationRequest {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      data: isObject(object.data)
        ? Object.entries(object.data).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
      ttlMs: isSet(object.ttlMs) ? globalThis.Number(object.ttlMs) : 0,
    };
  },

  toJSON(message: SilentNotificationRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.data) {
      const entries = Object.entries(message.data);
      if (entries.length > 0) {
        obj.data = {};
        entries.forEach(([k, v]) => {
          obj.data[k] = v;
        });
      }
    }
    if (message.ttlMs !== 0) {
      obj.ttlMs = Math.round(message.ttlMs);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SilentNotificationRequest>, I>>(base?: I): SilentNotificationRequest {
    return SilentNotificationRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SilentNotificationRequest>, I>>(object: I): SilentNotificationRequest {
    const message = createBaseSilentNotificationRequest();
    message.userId = object.userId ?? "";
    message.data = Object.entries(object.data ?? {}).reduce<{ [key: string]: string }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = globalThis.String(value);
      }
      return acc;
    }, {});
    message.ttlMs = object.ttlMs ?? 0;
    return message;
  },
};

function createBaseSilentNotificationRequest_DataEntry(): SilentNotificationRequest_DataEntry {
  return { key: "", value: "" };
}

export const SilentNotificationRequest_DataEntry = {
  encode(message: SilentNotificationRequest_DataEntry, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SilentNotificationRequest_DataEntry {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSilentNotificationRequest_DataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SilentNotificationRequest_DataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: SilentNotificationRequest_DataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SilentNotificationRequest_DataEntry>, I>>(
    base?: I,
  ): SilentNotificationRequest_DataEntry {
    return SilentNotificationRequest_DataEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SilentNotificationRequest_DataEntry>, I>>(
    object: I,
  ): SilentNotificationRequest_DataEntry {
    const message = createBaseSilentNotificationRequest_DataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseNotificationPreferences(): NotificationPreferences {
  return {
    enabled: false,
    soundEnabled: false,
    vibrationEnabled: false,
    showPreview: false,
    allowMessages: false,
    allowMentions: false,
    allowFriendRequests: false,
    allowVoiceCalls: false,
    dndEnabled: false,
    dndStartHour: 0,
    dndEndHour: 0,
    dndDays: [],
    channelSettings: {},
  };
}

export const NotificationPreferences = {
  encode(message: NotificationPreferences, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.enabled !== false) {
      writer.uint32(8).bool(message.enabled);
    }
    if (message.soundEnabled !== false) {
      writer.uint32(16).bool(message.soundEnabled);
    }
    if (message.vibrationEnabled !== false) {
      writer.uint32(24).bool(message.vibrationEnabled);
    }
    if (message.showPreview !== false) {
      writer.uint32(32).bool(message.showPreview);
    }
    if (message.allowMessages !== false) {
      writer.uint32(40).bool(message.allowMessages);
    }
    if (message.allowMentions !== false) {
      writer.uint32(48).bool(message.allowMentions);
    }
    if (message.allowFriendRequests !== false) {
      writer.uint32(56).bool(message.allowFriendRequests);
    }
    if (message.allowVoiceCalls !== false) {
      writer.uint32(64).bool(message.allowVoiceCalls);
    }
    if (message.dndEnabled !== false) {
      writer.uint32(72).bool(message.dndEnabled);
    }
    if (message.dndStartHour !== 0) {
      writer.uint32(80).int32(message.dndStartHour);
    }
    if (message.dndEndHour !== 0) {
      writer.uint32(88).int32(message.dndEndHour);
    }
    writer.uint32(98).fork();
    for (const v of message.dndDays) {
      writer.int32(v);
    }
    writer.ldelim();
    Object.entries(message.channelSettings).forEach(([key, value]) => {
      NotificationPreferences_ChannelSettingsEntry.encode({ key: key as any, value }, writer.uint32(106).fork())
        .ldelim();
    });
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): NotificationPreferences {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNotificationPreferences();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.soundEnabled = reader.bool();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.vibrationEnabled = reader.bool();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.showPreview = reader.bool();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.allowMessages = reader.bool();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.allowMentions = reader.bool();
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.allowFriendRequests = reader.bool();
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.allowVoiceCalls = reader.bool();
          continue;
        case 9:
          if (tag !== 72) {
            break;
          }

          message.dndEnabled = reader.bool();
          continue;
        case 10:
          if (tag !== 80) {
            break;
          }

          message.dndStartHour = reader.int32();
          continue;
        case 11:
          if (tag !== 88) {
            break;
          }

          message.dndEndHour = reader.int32();
          continue;
        case 12:
          if (tag === 96) {
            message.dndDays.push(reader.int32());

            continue;
          }

          if (tag === 98) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.dndDays.push(reader.int32());
            }

            continue;
          }

          break;
        case 13:
          if (tag !== 106) {
            break;
          }

          const entry13 = NotificationPreferences_ChannelSettingsEntry.decode(reader, reader.uint32());
          if (entry13.value !== undefined) {
            message.channelSettings[entry13.key] = entry13.value;
          }
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NotificationPreferences {
    return {
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
      soundEnabled: isSet(object.soundEnabled) ? globalThis.Boolean(object.soundEnabled) : false,
      vibrationEnabled: isSet(object.vibrationEnabled) ? globalThis.Boolean(object.vibrationEnabled) : false,
      showPreview: isSet(object.showPreview) ? globalThis.Boolean(object.showPreview) : false,
      allowMessages: isSet(object.allowMessages) ? globalThis.Boolean(object.allowMessages) : false,
      allowMentions: isSet(object.allowMentions) ? globalThis.Boolean(object.allowMentions) : false,
      allowFriendRequests: isSet(object.allowFriendRequests) ? globalThis.Boolean(object.allowFriendRequests) : false,
      allowVoiceCalls: isSet(object.allowVoiceCalls) ? globalThis.Boolean(object.allowVoiceCalls) : false,
      dndEnabled: isSet(object.dndEnabled) ? globalThis.Boolean(object.dndEnabled) : false,
      dndStartHour: isSet(object.dndStartHour) ? globalThis.Number(object.dndStartHour) : 0,
      dndEndHour: isSet(object.dndEndHour) ? globalThis.Number(object.dndEndHour) : 0,
      dndDays: globalThis.Array.isArray(object?.dndDays) ? object.dndDays.map((e: any) => globalThis.Number(e)) : [],
      channelSettings: isObject(object.channelSettings)
        ? Object.entries(object.channelSettings).reduce<{ [key: string]: ChannelNotificationSettings }>(
          (acc, [key, value]) => {
            acc[key] = ChannelNotificationSettings.fromJSON(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: NotificationPreferences): unknown {
    const obj: any = {};
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    if (message.soundEnabled !== false) {
      obj.soundEnabled = message.soundEnabled;
    }
    if (message.vibrationEnabled !== false) {
      obj.vibrationEnabled = message.vibrationEnabled;
    }
    if (message.showPreview !== false) {
      obj.showPreview = message.showPreview;
    }
    if (message.allowMessages !== false) {
      obj.allowMessages = message.allowMessages;
    }
    if (message.allowMentions !== false) {
      obj.allowMentions = message.allowMentions;
    }
    if (message.allowFriendRequests !== false) {
      obj.allowFriendRequests = message.allowFriendRequests;
    }
    if (message.allowVoiceCalls !== false) {
      obj.allowVoiceCalls = message.allowVoiceCalls;
    }
    if (message.dndEnabled !== false) {
      obj.dndEnabled = message.dndEnabled;
    }
    if (message.dndStartHour !== 0) {
      obj.dndStartHour = Math.round(message.dndStartHour);
    }
    if (message.dndEndHour !== 0) {
      obj.dndEndHour = Math.round(message.dndEndHour);
    }
    if (message.dndDays?.length) {
      obj.dndDays = message.dndDays.map((e) => Math.round(e));
    }
    if (message.channelSettings) {
      const entries = Object.entries(message.channelSettings);
      if (entries.length > 0) {
        obj.channelSettings = {};
        entries.forEach(([k, v]) => {
          obj.channelSettings[k] = ChannelNotificationSettings.toJSON(v);
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NotificationPreferences>, I>>(base?: I): NotificationPreferences {
    return NotificationPreferences.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NotificationPreferences>, I>>(object: I): NotificationPreferences {
    const message = createBaseNotificationPreferences();
    message.enabled = object.enabled ?? false;
    message.soundEnabled = object.soundEnabled ?? false;
    message.vibrationEnabled = object.vibrationEnabled ?? false;
    message.showPreview = object.showPreview ?? false;
    message.allowMessages = object.allowMessages ?? false;
    message.allowMentions = object.allowMentions ?? false;
    message.allowFriendRequests = object.allowFriendRequests ?? false;
    message.allowVoiceCalls = object.allowVoiceCalls ?? false;
    message.dndEnabled = object.dndEnabled ?? false;
    message.dndStartHour = object.dndStartHour ?? 0;
    message.dndEndHour = object.dndEndHour ?? 0;
    message.dndDays = object.dndDays?.map((e) => e) || [];
    message.channelSettings = Object.entries(object.channelSettings ?? {}).reduce<
      { [key: string]: ChannelNotificationSettings }
    >((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = ChannelNotificationSettings.fromPartial(value);
      }
      return acc;
    }, {});
    return message;
  },
};

function createBaseNotificationPreferences_ChannelSettingsEntry(): NotificationPreferences_ChannelSettingsEntry {
  return { key: "", value: undefined };
}

export const NotificationPreferences_ChannelSettingsEntry = {
  encode(message: NotificationPreferences_ChannelSettingsEntry, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      ChannelNotificationSettings.encode(message.value, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): NotificationPreferences_ChannelSettingsEntry {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNotificationPreferences_ChannelSettingsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.value = ChannelNotificationSettings.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NotificationPreferences_ChannelSettingsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? ChannelNotificationSettings.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: NotificationPreferences_ChannelSettingsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = ChannelNotificationSettings.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NotificationPreferences_ChannelSettingsEntry>, I>>(
    base?: I,
  ): NotificationPreferences_ChannelSettingsEntry {
    return NotificationPreferences_ChannelSettingsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NotificationPreferences_ChannelSettingsEntry>, I>>(
    object: I,
  ): NotificationPreferences_ChannelSettingsEntry {
    const message = createBaseNotificationPreferences_ChannelSettingsEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? ChannelNotificationSettings.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBaseChannelNotificationSettings(): ChannelNotificationSettings {
  return { muted: false, onlyMentions: false, notifyOnMentions: false };
}

export const ChannelNotificationSettings = {
  encode(message: ChannelNotificationSettings, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.muted !== false) {
      writer.uint32(8).bool(message.muted);
    }
    if (message.onlyMentions !== false) {
      writer.uint32(16).bool(message.onlyMentions);
    }
    if (message.notifyOnMentions !== false) {
      writer.uint32(24).bool(message.notifyOnMentions);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ChannelNotificationSettings {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChannelNotificationSettings();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.muted = reader.bool();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.onlyMentions = reader.bool();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.notifyOnMentions = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ChannelNotificationSettings {
    return {
      muted: isSet(object.muted) ? globalThis.Boolean(object.muted) : false,
      onlyMentions: isSet(object.onlyMentions) ? globalThis.Boolean(object.onlyMentions) : false,
      notifyOnMentions: isSet(object.notifyOnMentions) ? globalThis.Boolean(object.notifyOnMentions) : false,
    };
  },

  toJSON(message: ChannelNotificationSettings): unknown {
    const obj: any = {};
    if (message.muted !== false) {
      obj.muted = message.muted;
    }
    if (message.onlyMentions !== false) {
      obj.onlyMentions = message.onlyMentions;
    }
    if (message.notifyOnMentions !== false) {
      obj.notifyOnMentions = message.notifyOnMentions;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ChannelNotificationSettings>, I>>(base?: I): ChannelNotificationSettings {
    return ChannelNotificationSettings.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ChannelNotificationSettings>, I>>(object: I): ChannelNotificationSettings {
    const message = createBaseChannelNotificationSettings();
    message.muted = object.muted ?? false;
    message.onlyMentions = object.onlyMentions ?? false;
    message.notifyOnMentions = object.notifyOnMentions ?? false;
    return message;
  },
};

function createBaseSetPreferencesRequest(): SetPreferencesRequest {
  return { userId: "", preferences: undefined };
}

export const SetPreferencesRequest = {
  encode(message: SetPreferencesRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.preferences !== undefined) {
      NotificationPreferences.encode(message.preferences, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SetPreferencesRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetPreferencesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.preferences = NotificationPreferences.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetPreferencesRequest {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      preferences: isSet(object.preferences) ? NotificationPreferences.fromJSON(object.preferences) : undefined,
    };
  },

  toJSON(message: SetPreferencesRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.preferences !== undefined) {
      obj.preferences = NotificationPreferences.toJSON(message.preferences);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetPreferencesRequest>, I>>(base?: I): SetPreferencesRequest {
    return SetPreferencesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetPreferencesRequest>, I>>(object: I): SetPreferencesRequest {
    const message = createBaseSetPreferencesRequest();
    message.userId = object.userId ?? "";
    message.preferences = (object.preferences !== undefined && object.preferences !== null)
      ? NotificationPreferences.fromPartial(object.preferences)
      : undefined;
    return message;
  },
};

function createBaseSetPreferencesResponse(): SetPreferencesResponse {
  return { code: 0, serverTime: 0 };
}

export const SetPreferencesResponse = {
  encode(message: SetPreferencesResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.serverTime !== 0) {
      writer.uint32(16).int64(message.serverTime);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SetPreferencesResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetPreferencesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.serverTime = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetPreferencesResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      serverTime: isSet(object.serverTime) ? globalThis.Number(object.serverTime) : 0,
    };
  },

  toJSON(message: SetPreferencesResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.serverTime !== 0) {
      obj.serverTime = Math.round(message.serverTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetPreferencesResponse>, I>>(base?: I): SetPreferencesResponse {
    return SetPreferencesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetPreferencesResponse>, I>>(object: I): SetPreferencesResponse {
    const message = createBaseSetPreferencesResponse();
    message.code = object.code ?? 0;
    message.serverTime = object.serverTime ?? 0;
    return message;
  },
};

function createBaseGetPreferencesRequest(): GetPreferencesRequest {
  return { userId: "" };
}

export const GetPreferencesRequest = {
  encode(message: GetPreferencesRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetPreferencesRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPreferencesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPreferencesRequest {
    return { userId: isSet(object.userId) ? globalThis.String(object.userId) : "" };
  },

  toJSON(message: GetPreferencesRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPreferencesRequest>, I>>(base?: I): GetPreferencesRequest {
    return GetPreferencesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPreferencesRequest>, I>>(object: I): GetPreferencesRequest {
    const message = createBaseGetPreferencesRequest();
    message.userId = object.userId ?? "";
    return message;
  },
};

function createBaseGetPreferencesResponse(): GetPreferencesResponse {
  return { code: 0, preferences: undefined };
}

export const GetPreferencesResponse = {
  encode(message: GetPreferencesResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    if (message.preferences !== undefined) {
      NotificationPreferences.encode(message.preferences, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetPreferencesResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPreferencesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.preferences = NotificationPreferences.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPreferencesResponse {
    return {
      code: isSet(object.code) ? errorCodeFromJSON(object.code) : 0,
      preferences: isSet(object.preferences) ? NotificationPreferences.fromJSON(object.preferences) : undefined,
    };
  },

  toJSON(message: GetPreferencesResponse): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = errorCodeToJSON(message.code);
    }
    if (message.preferences !== undefined) {
      obj.preferences = NotificationPreferences.toJSON(message.preferences);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPreferencesResponse>, I>>(base?: I): GetPreferencesResponse {
    return GetPreferencesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPreferencesResponse>, I>>(object: I): GetPreferencesResponse {
    const message = createBaseGetPreferencesResponse();
    message.code = object.code ?? 0;
    message.preferences = (object.preferences !== undefined && object.preferences !== null)
      ? NotificationPreferences.fromPartial(object.preferences)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

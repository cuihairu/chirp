{
  "name": "@chirp/protocol",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Framework-agnostic TypeScript chat protocol core for chirp: connection state machine, the five hook interfaces + ChatPipeline, and the sensitive-word pre-check interceptor. No React/DOM dependencies — shared by the web companion and reusable by future LayaBox/Cocos targets.",
  "exports": {
    "./*": "./src/*"
  },
  "scripts": {
    "typecheck": "tsc -b",
    "test": "vitest run"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.11",
    "jsdom": "^24.0.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.11"
  }
}

ARG UBUNTU_VERSION=22.04

FROM ubuntu:${UBUNTU_VERSION} AS build

ARG SERVICE=gateway

# libmariadb-dev (MariaDB Connector/C): same mysql_* C API and mysql/mysql.h
# layout as libmysqlclient, with a much lighter dependency footprint.
RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
    ca-certificates \
    cmake \
    libasio-dev \
    git \
    libmariadb-dev \
    pkg-config \
    protobuf-compiler \
    libprotobuf-dev \
    python3 \
  && rm -rf /var/lib/apt/lists/*

WORKDIR /src
COPY . .

RUN ./gen_proto.sh
RUN cmake -S . -B build -DCMAKE_BUILD_TYPE=Release -DENABLE_TESTS=OFF
RUN cmake --build build --target chirp_${SERVICE} chirp_chat_mysql_exporter -j"$(nproc)"
RUN case "${SERVICE}" in \
      chat|chat_distributed) cp "/src/build/services/chat/chirp_${SERVICE}" /tmp/chirp_bin ;; \
      chat_enhanced) cp "/src/build/services/chat/chirp_chat" /tmp/chirp_bin ;; \
      auth_enhanced) cp "/src/build/services/auth/chirp_auth" /tmp/chirp_bin ;; \
      npc_dialog) cp "/src/build/services/npc_dialog/chirp_npc_dialog" /tmp/chirp_bin ;; \
      gateway|auth|social|voice|party|notification|search|app_gateway) cp "/src/build/services/${SERVICE}/chirp_${SERVICE}" /tmp/chirp_bin ;; \
      *) echo "Unsupported service target: ${SERVICE}" >&2; exit 1 ;; \
    esac

FROM ubuntu:${UBUNTU_VERSION} AS runtime

ARG SERVICE=gateway

# Runtime only: the mariadb client .so (default-mysql-client is the MariaDB
# CLI on Ubuntu/Debian, kept for manual database inspection).
RUN apt-get update && apt-get install -y --no-install-recommends \
    ca-certificates \
    libmariadb3 \
    libprotobuf23 \
    default-mysql-client \
    redis-tools \
  && rm -rf /var/lib/apt/lists/*

COPY --from=build /tmp/chirp_bin /usr/local/bin/chirp_${SERVICE}
COPY --from=build /src/build/tools/benchmark/chirp_chat_mysql_exporter /usr/local/bin/chirp_chat_mysql_exporter
COPY docker/docker-entrypoint.sh /usr/local/bin/docker-entrypoint.sh
COPY tools/archive_chat_redis.sh /usr/local/bin/archive_chat_redis.sh
COPY tools/archive_chat_redis_loop.sh /usr/local/bin/archive_chat_redis_loop.sh
RUN chmod 0755 /usr/local/bin/docker-entrypoint.sh /usr/local/bin/archive_chat_redis.sh /usr/local/bin/archive_chat_redis_loop.sh

ENV CHIRP_BIN=/usr/local/bin/chirp_${SERVICE}

EXPOSE 5000 5001 6000 7000
ENTRYPOINT ["/usr/local/bin/docker-entrypoint.sh"]

# CI runs `flutter analyze --fatal-infos`, so infos count. Keep this file
# minimal: the flutter_lints default set plus nothing app-specific.
analyzer:
  exclude:
    - build/**
    - android/**
    - ios/**
    - windows/**
    - macos/**
    - linux/**
include: package:flutter_lints/flutter.yaml

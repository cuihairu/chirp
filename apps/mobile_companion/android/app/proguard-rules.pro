# Chirp consumer ProGuard/R8 rules.
# minifyEnabled is false, so these rules are currently inert; they exist
# so the proguardFiles reference resolves and future minification has a
# home for keep rules.
